{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"funstruct","text":"<p>A helpful collection of functional utilities.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install funstruct || uv add funstruct\n</code></pre>"},{"location":"#functional-primer","title":"Functional Primer","text":""},{"location":"#type-class-hierarchy","title":"Type Class Hierarchy","text":"<pre><code>Semigroup              Functor\n    \u2502                      \u2502\n Monoid              Applicative\n                        \u2502\n                       Monad\n</code></pre>"},{"location":"#diagrams","title":"Diagrams","text":"<p>Functor \u2014 transform the value inside a context</p> <pre><code>F[A] ---( f: A -&gt; B )---&gt; F[B]\n</code></pre> <p>Applicative \u2014 combine independent computations</p> <pre><code>F[A] \u2500\u2510\n       \u251c\u2500\u2500&gt; F[(A, B)]\nF[B] \u2500\u2518\n</code></pre> <p>Monad \u2014 sequence computations that produce new contexts</p> <pre><code>F[A] ---( f: A -&gt; F[B] )---&gt; F[B]\n</code></pre> <p>Semigroup \u2014 associative combine (any type with <code>+</code>)</p> <pre><code>A \u2500\u2510\n    \u251c\u2500\u2500( + )\u2500\u2500&gt; A\nA \u2500\u2518\n</code></pre> <p>Monoid \u2014 semigroup with an identity element</p> <pre><code>A \u2500\u2510\n    \u251c\u2500\u2500( + )\u2500\u2500&gt; A       (+ identity = A)\nA \u2500\u2518\n</code></pre> <pre><code>@dataclass(frozen=True)\nclass Semigroup:\n    typ: type\n    combine: Callable  # (A, A) -&gt; A\n\n@dataclass(frozen=True)\nclass Monoid(Semigroup):\n    typ: type\n    combine: Callable  # (A, A) -&gt; A\n    empty: object      # identity element\n\nclass Functor(ABC):\n    def map(self, f) -&gt; Functor: ...\n\nclass Applicative(Functor):\n    def pure(cls, value) -&gt; Applicative: ...\n    def ap(self, other) -&gt; Applicative: ...\n    def __add__ = ap  # alias\n\nclass Monad(Applicative):\n    def bind(self, f) -&gt; Monad: ...\n    def __rshift__ = bind  # &gt;&gt;\n</code></pre> <pre><code># Multiple semigroups for the same type:\nint_add = Monoid(typ=int, combine=lambda a, b: a + b, empty=0)\nint_mul = Monoid(typ=int, combine=lambda a, b: a * b, empty=1)\n</code></pre>"},{"location":"#scala-equivalent","title":"~ Scala equivalent","text":"<pre><code>trait Semigroup[A] {\n  def combine(x: A, y: A): A\n}\n\ntrait Monoid[A] extends Semigroup[A] {\n  def empty: A\n}\n\ntrait Monoid[A] extends Semigroup[A] {\n  def empty: A\n}\n\ntrait Functor[F[_]] {\n  def map[A, B](fa: F[A])(f: A =&gt; B): F[B]\n}\n\ntrait Applicative[F[_]] extends Functor[F] {\n  def pure[A](a: A): F[A]\n  def ap[A, B](ff: F[A =&gt; B])(fa: F[A]): F[B]\n}\n\ntrait Monad[F[_]] extends Applicative[F] {\n  def bind(fa: F[A])(f: A =&gt; F[B]): F[B]\n}\n</code></pre>"},{"location":"#laws","title":"Laws","text":"<p>Every implementation must satisfy these mathematical laws:</p> <p>Semigroup</p> <ul> <li>Associativity: <code>(a + b) + c == a + (b + c)</code></li> </ul> <p>Monoid</p> <ul> <li>Left identity: <code>empty + a == a</code></li> <li>Right identity: <code>a + empty == a</code></li> </ul> <p>Functor</p> <ul> <li>Identity: <code>fa.map(id) == fa</code></li> <li>Composition: <code>fa.map(f).map(g) == fa.map(g \u2218 f)</code></li> </ul> <p>Applicative</p> <ul> <li>Identity: <code>pure(id).ap(v) == v</code></li> <li>Homomorphism: <code>pure(f).ap(pure(x)) == pure(f(x))</code></li> <li>Interchange: <code>u.ap(pure(y)) == pure(\u03bbf. f(y)).ap(u)</code></li> <li>Composition: <code>pure(\u2218).ap(u).ap(v).ap(w) == u.ap(v.ap(w))</code></li> </ul> <p>Monad</p> <ul> <li>Left identity: <code>pure(a).bind(f) == f(a)</code></li> <li>Right identity: <code>m.bind(pure) == m</code></li> <li>Associativity: <code>m.bind(f).bind(g) == m.bind(\u03bbx. f(x).bind(g))</code></li> </ul>"},{"location":"applicative/","title":"Applicatives","text":"<p>Applicative functors allow combining independent computations. Unlike Monad (where each step can depend on the previous), Applicative combines values that don't depend on each other \u2014 which enables error accumulation.</p> <ul> <li>Validated \u2014 accumulate all errors instead of short-circuiting</li> </ul>"},{"location":"applicative/#funstruct.applicative","title":"<code>funstruct.applicative</code>","text":"<p>Applicative types</p>"},{"location":"applicative/validated/","title":"Validated","text":""},{"location":"applicative/validated/#funstruct.applicative.validated","title":"<code>funstruct.applicative.validated</code>","text":"<p>Validated: applicative error-accumulating functor.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.applicative.validated import Validated, Valid, Invalid\n&gt;&gt;&gt; Validated.cond(True, 42, \"err\")\nValid(value=42)\n&gt;&gt;&gt; Validated.cond(False, 42, \"err\")\nInvalid(errors=Cons('err', Nil()))\n&gt;&gt;&gt; Valid(1) + Valid(2)\nValid(value=(1, 2))\n&gt;&gt;&gt; Invalid(\"a:\") + Invalid(\"b\")\nInvalid(errors='a:b')\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated","title":"<code>Validated</code>","text":"<p>               Bases: <code>Applicative</code></p> <p>Base class for Valid/Invalid \u2014 provides constructors and supports + operator.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>class Validated(Applicative):\n    \"\"\"Base class for Valid/Invalid \u2014 provides constructors and supports + operator.\"\"\"\n\n    @abstractmethod\n    def ap(self, other) -&gt; Validated: ...\n\n    def __add__(self, other) -&gt; Validated:\n        return self.ap(other)\n\n    @property\n    @abstractmethod\n    def is_valid(self) -&gt; bool: ...\n\n    @abstractmethod\n    def fold(self, on_invalid: Callable, on_valid: Callable):\n        \"\"\"Eliminate the Validated \u2014 apply on_invalid or on_valid.\"\"\"\n        ...\n\n    @abstractmethod\n    def to_result(self) -&gt; Result: ...\n\n    @abstractmethod\n    def to_result_or(self, exc_cls, combine=None) -&gt; Result: ...\n\n    @classmethod\n    def pure(cls, value) -&gt; Validated:\n        \"\"\"Lift a value into Valid.\"\"\"\n        return Valid(value)\n\n    @staticmethod\n    def valid(value: _A) -&gt; Validated:\n        \"\"\"Alias for pure.\"\"\"\n        return Valid(value)\n\n    @staticmethod\n    def invalid(error: _E) -&gt; Validated:\n        \"\"\"Lift a single error into Invalid.\n\n        Default semigroup: CList (cons list over +/append).\n        For a custom semigroup, construct Invalid(your_value) directly.\n        \"\"\"\n        return Invalid(Cons.pure(error))\n\n    @staticmethod\n    def cond(test: bool, value: _A, error: _E) -&gt; Validated:\n        \"\"\"Conditional \u2014 Valid(value) if test, else Invalid(Cons(error)).\"\"\"\n        if test:\n            return Valid(value)\n        return Invalid(Cons.pure(error))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.fold","title":"<code>fold(on_invalid, on_valid)</code>  <code>abstractmethod</code>","text":"<p>Eliminate the Validated \u2014 apply on_invalid or on_valid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@abstractmethod\ndef fold(self, on_invalid: Callable, on_valid: Callable):\n    \"\"\"Eliminate the Validated \u2014 apply on_invalid or on_valid.\"\"\"\n    ...\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a value into Valid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@classmethod\ndef pure(cls, value) -&gt; Validated:\n    \"\"\"Lift a value into Valid.\"\"\"\n    return Valid(value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.valid","title":"<code>valid(value)</code>  <code>staticmethod</code>","text":"<p>Alias for pure.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@staticmethod\ndef valid(value: _A) -&gt; Validated:\n    \"\"\"Alias for pure.\"\"\"\n    return Valid(value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.invalid","title":"<code>invalid(error)</code>  <code>staticmethod</code>","text":"<p>Lift a single error into Invalid.</p> <p>Default semigroup: CList (cons list over +/append). For a custom semigroup, construct Invalid(your_value) directly.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@staticmethod\ndef invalid(error: _E) -&gt; Validated:\n    \"\"\"Lift a single error into Invalid.\n\n    Default semigroup: CList (cons list over +/append).\n    For a custom semigroup, construct Invalid(your_value) directly.\n    \"\"\"\n    return Invalid(Cons.pure(error))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.cond","title":"<code>cond(test, value, error)</code>  <code>staticmethod</code>","text":"<p>Conditional \u2014 Valid(value) if test, else Invalid(Cons(error)).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@staticmethod\ndef cond(test: bool, value: _A, error: _E) -&gt; Validated:\n    \"\"\"Conditional \u2014 Valid(value) if test, else Invalid(Cons(error)).\"\"\"\n    if test:\n        return Valid(value)\n    return Invalid(Cons.pure(error))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid","title":"<code>Valid</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Validated</code>, <code>Generic[_A]</code></p> <p>Success case.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@dataclass(frozen=True)\nclass Valid(Validated, Generic[_A]):\n    \"\"\"Success case.\"\"\"\n\n    value: _A\n\n    @property\n    def is_valid(self) -&gt; bool:\n        return True\n\n    def __bool__(self) -&gt; bool:\n        return True\n\n    def fold(self, on_invalid: Callable, on_valid: Callable):\n        \"\"\"Eliminate \u2014 applies on_valid to the value.\"\"\"\n        return on_valid(self.value)\n\n    def map(self, f: Callable[[_A], _B]) -&gt; Valid[_B]:\n        \"\"\"Transform the success value.\"\"\"\n        return Valid(f(self.value))\n\n    def ap(self, other) -&gt; Validated:\n        \"\"\"Combine with another Validated (applicative).\n\n        Accumulates errors from both sides.\n        On success, tuples the values.\n        \"\"\"\n        match other:\n            case Valid(val):\n                return Valid((self.value, val))\n            case _:\n                return other\n\n    def to_result(self):\n        \"\"\"Convert to Ok(value).\"\"\"\n        from funstruct.monad.result import Ok\n\n        return Ok(self.value)\n\n    def to_result_or(self, exc_cls):\n        \"\"\"Convert to Ok(value) \u2014 exc_cls unused on Valid.\"\"\"\n        from funstruct.monad.result import Ok\n\n        return Ok(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.fold","title":"<code>fold(on_invalid, on_valid)</code>","text":"<p>Eliminate \u2014 applies on_valid to the value.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def fold(self, on_invalid: Callable, on_valid: Callable):\n    \"\"\"Eliminate \u2014 applies on_valid to the value.\"\"\"\n    return on_valid(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.map","title":"<code>map(f)</code>","text":"<p>Transform the success value.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; Valid[_B]:\n    \"\"\"Transform the success value.\"\"\"\n    return Valid(f(self.value))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.ap","title":"<code>ap(other)</code>","text":"<p>Combine with another Validated (applicative).</p> <p>Accumulates errors from both sides. On success, tuples the values.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def ap(self, other) -&gt; Validated:\n    \"\"\"Combine with another Validated (applicative).\n\n    Accumulates errors from both sides.\n    On success, tuples the values.\n    \"\"\"\n    match other:\n        case Valid(val):\n            return Valid((self.value, val))\n        case _:\n            return other\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.to_result","title":"<code>to_result()</code>","text":"<p>Convert to Ok(value).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result(self):\n    \"\"\"Convert to Ok(value).\"\"\"\n    from funstruct.monad.result import Ok\n\n    return Ok(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.to_result_or","title":"<code>to_result_or(exc_cls)</code>","text":"<p>Convert to Ok(value) \u2014 exc_cls unused on Valid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result_or(self, exc_cls):\n    \"\"\"Convert to Ok(value) \u2014 exc_cls unused on Valid.\"\"\"\n    from funstruct.monad.result import Ok\n\n    return Ok(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid","title":"<code>Invalid</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Validated</code>, <code>Generic[_E]</code></p> <p>Failure case \u2014 accumulated errors.</p> <p><code>errors</code> can be any Semigroup (supports +): list, str, tuple, or custom.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@dataclass(frozen=True)\nclass Invalid(Validated, Generic[_E]):\n    \"\"\"Failure case \u2014 accumulated errors.\n\n    `errors` can be any Semigroup (supports +): list, str, tuple, or custom.\n    \"\"\"\n\n    errors: _E\n\n    @property\n    def is_valid(self) -&gt; bool:\n        return False\n\n    def __bool__(self) -&gt; bool:\n        return False\n\n    def map(self, f) -&gt; Invalid[_E]:\n        \"\"\"No-op on Invalid.\"\"\"\n        return self\n\n    def fold(self, on_invalid: Callable, on_valid: Callable):\n        \"\"\"Eliminate \u2014 applies on_invalid to the errors.\"\"\"\n        return on_invalid(self.errors)\n\n    def ap(self, other) -&gt; Validated:\n        \"\"\"Combine \u2014 accumulates errors from both sides.\"\"\"\n        match other:\n            case Invalid(errs):\n                return Invalid(self.errors + errs)\n            case _:\n                return self\n\n    def to_result(self):\n        \"\"\"Convert to Err(errors).\"\"\"\n        from funstruct.monad.result import Err\n\n        return Err(self.errors)\n\n    def to_result_or(\n        self, exc_cls, combine=lambda errs: \"; \".join(str(e) for e in errs)\n    ):\n        \"\"\"Convert to Err(exc_cls(combined_errors)).\"\"\"\n        from funstruct.monad.result import Err\n\n        return Err(exc_cls(combine(self.errors)))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.map","title":"<code>map(f)</code>","text":"<p>No-op on Invalid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def map(self, f) -&gt; Invalid[_E]:\n    \"\"\"No-op on Invalid.\"\"\"\n    return self\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.fold","title":"<code>fold(on_invalid, on_valid)</code>","text":"<p>Eliminate \u2014 applies on_invalid to the errors.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def fold(self, on_invalid: Callable, on_valid: Callable):\n    \"\"\"Eliminate \u2014 applies on_invalid to the errors.\"\"\"\n    return on_invalid(self.errors)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.ap","title":"<code>ap(other)</code>","text":"<p>Combine \u2014 accumulates errors from both sides.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def ap(self, other) -&gt; Validated:\n    \"\"\"Combine \u2014 accumulates errors from both sides.\"\"\"\n    match other:\n        case Invalid(errs):\n            return Invalid(self.errors + errs)\n        case _:\n            return self\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.to_result","title":"<code>to_result()</code>","text":"<p>Convert to Err(errors).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result(self):\n    \"\"\"Convert to Err(errors).\"\"\"\n    from funstruct.monad.result import Err\n\n    return Err(self.errors)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.to_result_or","title":"<code>to_result_or(exc_cls, combine=lambda errs: '; '.join(str(e) for e in errs))</code>","text":"<p>Convert to Err(exc_cls(combined_errors)).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result_or(\n    self, exc_cls, combine=lambda errs: \"; \".join(str(e) for e in errs)\n):\n    \"\"\"Convert to Err(exc_cls(combined_errors)).\"\"\"\n    from funstruct.monad.result import Err\n\n    return Err(exc_cls(combine(self.errors)))\n</code></pre>"},{"location":"applicative/validated/#api-reference","title":"API Reference","text":""},{"location":"applicative/validated/#funstruct.applicative.validated.Validated","title":"<code>funstruct.applicative.validated.Validated</code>","text":"<p>               Bases: <code>Applicative</code></p> <p>Base class for Valid/Invalid \u2014 provides constructors and supports + operator.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>class Validated(Applicative):\n    \"\"\"Base class for Valid/Invalid \u2014 provides constructors and supports + operator.\"\"\"\n\n    @abstractmethod\n    def ap(self, other) -&gt; Validated: ...\n\n    def __add__(self, other) -&gt; Validated:\n        return self.ap(other)\n\n    @property\n    @abstractmethod\n    def is_valid(self) -&gt; bool: ...\n\n    @abstractmethod\n    def fold(self, on_invalid: Callable, on_valid: Callable):\n        \"\"\"Eliminate the Validated \u2014 apply on_invalid or on_valid.\"\"\"\n        ...\n\n    @abstractmethod\n    def to_result(self) -&gt; Result: ...\n\n    @abstractmethod\n    def to_result_or(self, exc_cls, combine=None) -&gt; Result: ...\n\n    @classmethod\n    def pure(cls, value) -&gt; Validated:\n        \"\"\"Lift a value into Valid.\"\"\"\n        return Valid(value)\n\n    @staticmethod\n    def valid(value: _A) -&gt; Validated:\n        \"\"\"Alias for pure.\"\"\"\n        return Valid(value)\n\n    @staticmethod\n    def invalid(error: _E) -&gt; Validated:\n        \"\"\"Lift a single error into Invalid.\n\n        Default semigroup: CList (cons list over +/append).\n        For a custom semigroup, construct Invalid(your_value) directly.\n        \"\"\"\n        return Invalid(Cons.pure(error))\n\n    @staticmethod\n    def cond(test: bool, value: _A, error: _E) -&gt; Validated:\n        \"\"\"Conditional \u2014 Valid(value) if test, else Invalid(Cons(error)).\"\"\"\n        if test:\n            return Valid(value)\n        return Invalid(Cons.pure(error))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.fold","title":"<code>fold(on_invalid, on_valid)</code>  <code>abstractmethod</code>","text":"<p>Eliminate the Validated \u2014 apply on_invalid or on_valid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@abstractmethod\ndef fold(self, on_invalid: Callable, on_valid: Callable):\n    \"\"\"Eliminate the Validated \u2014 apply on_invalid or on_valid.\"\"\"\n    ...\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a value into Valid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@classmethod\ndef pure(cls, value) -&gt; Validated:\n    \"\"\"Lift a value into Valid.\"\"\"\n    return Valid(value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.valid","title":"<code>valid(value)</code>  <code>staticmethod</code>","text":"<p>Alias for pure.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@staticmethod\ndef valid(value: _A) -&gt; Validated:\n    \"\"\"Alias for pure.\"\"\"\n    return Valid(value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.invalid","title":"<code>invalid(error)</code>  <code>staticmethod</code>","text":"<p>Lift a single error into Invalid.</p> <p>Default semigroup: CList (cons list over +/append). For a custom semigroup, construct Invalid(your_value) directly.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@staticmethod\ndef invalid(error: _E) -&gt; Validated:\n    \"\"\"Lift a single error into Invalid.\n\n    Default semigroup: CList (cons list over +/append).\n    For a custom semigroup, construct Invalid(your_value) directly.\n    \"\"\"\n    return Invalid(Cons.pure(error))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Validated.cond","title":"<code>cond(test, value, error)</code>  <code>staticmethod</code>","text":"<p>Conditional \u2014 Valid(value) if test, else Invalid(Cons(error)).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@staticmethod\ndef cond(test: bool, value: _A, error: _E) -&gt; Validated:\n    \"\"\"Conditional \u2014 Valid(value) if test, else Invalid(Cons(error)).\"\"\"\n    if test:\n        return Valid(value)\n    return Invalid(Cons.pure(error))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid","title":"<code>funstruct.applicative.validated.Valid</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Validated</code>, <code>Generic[_A]</code></p> <p>Success case.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@dataclass(frozen=True)\nclass Valid(Validated, Generic[_A]):\n    \"\"\"Success case.\"\"\"\n\n    value: _A\n\n    @property\n    def is_valid(self) -&gt; bool:\n        return True\n\n    def __bool__(self) -&gt; bool:\n        return True\n\n    def fold(self, on_invalid: Callable, on_valid: Callable):\n        \"\"\"Eliminate \u2014 applies on_valid to the value.\"\"\"\n        return on_valid(self.value)\n\n    def map(self, f: Callable[[_A], _B]) -&gt; Valid[_B]:\n        \"\"\"Transform the success value.\"\"\"\n        return Valid(f(self.value))\n\n    def ap(self, other) -&gt; Validated:\n        \"\"\"Combine with another Validated (applicative).\n\n        Accumulates errors from both sides.\n        On success, tuples the values.\n        \"\"\"\n        match other:\n            case Valid(val):\n                return Valid((self.value, val))\n            case _:\n                return other\n\n    def to_result(self):\n        \"\"\"Convert to Ok(value).\"\"\"\n        from funstruct.monad.result import Ok\n\n        return Ok(self.value)\n\n    def to_result_or(self, exc_cls):\n        \"\"\"Convert to Ok(value) \u2014 exc_cls unused on Valid.\"\"\"\n        from funstruct.monad.result import Ok\n\n        return Ok(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.fold","title":"<code>fold(on_invalid, on_valid)</code>","text":"<p>Eliminate \u2014 applies on_valid to the value.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def fold(self, on_invalid: Callable, on_valid: Callable):\n    \"\"\"Eliminate \u2014 applies on_valid to the value.\"\"\"\n    return on_valid(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.map","title":"<code>map(f)</code>","text":"<p>Transform the success value.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; Valid[_B]:\n    \"\"\"Transform the success value.\"\"\"\n    return Valid(f(self.value))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.ap","title":"<code>ap(other)</code>","text":"<p>Combine with another Validated (applicative).</p> <p>Accumulates errors from both sides. On success, tuples the values.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def ap(self, other) -&gt; Validated:\n    \"\"\"Combine with another Validated (applicative).\n\n    Accumulates errors from both sides.\n    On success, tuples the values.\n    \"\"\"\n    match other:\n        case Valid(val):\n            return Valid((self.value, val))\n        case _:\n            return other\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.to_result","title":"<code>to_result()</code>","text":"<p>Convert to Ok(value).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result(self):\n    \"\"\"Convert to Ok(value).\"\"\"\n    from funstruct.monad.result import Ok\n\n    return Ok(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Valid.to_result_or","title":"<code>to_result_or(exc_cls)</code>","text":"<p>Convert to Ok(value) \u2014 exc_cls unused on Valid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result_or(self, exc_cls):\n    \"\"\"Convert to Ok(value) \u2014 exc_cls unused on Valid.\"\"\"\n    from funstruct.monad.result import Ok\n\n    return Ok(self.value)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid","title":"<code>funstruct.applicative.validated.Invalid</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Validated</code>, <code>Generic[_E]</code></p> <p>Failure case \u2014 accumulated errors.</p> <p><code>errors</code> can be any Semigroup (supports +): list, str, tuple, or custom.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>@dataclass(frozen=True)\nclass Invalid(Validated, Generic[_E]):\n    \"\"\"Failure case \u2014 accumulated errors.\n\n    `errors` can be any Semigroup (supports +): list, str, tuple, or custom.\n    \"\"\"\n\n    errors: _E\n\n    @property\n    def is_valid(self) -&gt; bool:\n        return False\n\n    def __bool__(self) -&gt; bool:\n        return False\n\n    def map(self, f) -&gt; Invalid[_E]:\n        \"\"\"No-op on Invalid.\"\"\"\n        return self\n\n    def fold(self, on_invalid: Callable, on_valid: Callable):\n        \"\"\"Eliminate \u2014 applies on_invalid to the errors.\"\"\"\n        return on_invalid(self.errors)\n\n    def ap(self, other) -&gt; Validated:\n        \"\"\"Combine \u2014 accumulates errors from both sides.\"\"\"\n        match other:\n            case Invalid(errs):\n                return Invalid(self.errors + errs)\n            case _:\n                return self\n\n    def to_result(self):\n        \"\"\"Convert to Err(errors).\"\"\"\n        from funstruct.monad.result import Err\n\n        return Err(self.errors)\n\n    def to_result_or(\n        self, exc_cls, combine=lambda errs: \"; \".join(str(e) for e in errs)\n    ):\n        \"\"\"Convert to Err(exc_cls(combined_errors)).\"\"\"\n        from funstruct.monad.result import Err\n\n        return Err(exc_cls(combine(self.errors)))\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.map","title":"<code>map(f)</code>","text":"<p>No-op on Invalid.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def map(self, f) -&gt; Invalid[_E]:\n    \"\"\"No-op on Invalid.\"\"\"\n    return self\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.fold","title":"<code>fold(on_invalid, on_valid)</code>","text":"<p>Eliminate \u2014 applies on_invalid to the errors.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def fold(self, on_invalid: Callable, on_valid: Callable):\n    \"\"\"Eliminate \u2014 applies on_invalid to the errors.\"\"\"\n    return on_invalid(self.errors)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.ap","title":"<code>ap(other)</code>","text":"<p>Combine \u2014 accumulates errors from both sides.</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def ap(self, other) -&gt; Validated:\n    \"\"\"Combine \u2014 accumulates errors from both sides.\"\"\"\n    match other:\n        case Invalid(errs):\n            return Invalid(self.errors + errs)\n        case _:\n            return self\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.to_result","title":"<code>to_result()</code>","text":"<p>Convert to Err(errors).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result(self):\n    \"\"\"Convert to Err(errors).\"\"\"\n    from funstruct.monad.result import Err\n\n    return Err(self.errors)\n</code></pre>"},{"location":"applicative/validated/#funstruct.applicative.validated.Invalid.to_result_or","title":"<code>to_result_or(exc_cls, combine=lambda errs: '; '.join(str(e) for e in errs))</code>","text":"<p>Convert to Err(exc_cls(combined_errors)).</p> Source code in <code>funstruct/applicative/validated.py</code> <pre><code>def to_result_or(\n    self, exc_cls, combine=lambda errs: \"; \".join(str(e) for e in errs)\n):\n    \"\"\"Convert to Err(exc_cls(combined_errors)).\"\"\"\n    from funstruct.monad.result import Err\n\n    return Err(exc_cls(combine(self.errors)))\n</code></pre>"},{"location":"collections/","title":"Collections","text":"<p>Persistent, immutable collection types. All are Functors (support <code>.map()</code>), and most are Semigroups or Monoids (support <code>+</code> and have an empty value).</p> <ul> <li>Cons \u2014 singly-linked list</li> <li>frozendict \u2014 immutable dictionary</li> <li>Tree \u2014 rose tree</li> </ul>"},{"location":"collections/#funstruct.collections","title":"<code>funstruct.collections</code>","text":"<p>Persistent and immutable collection types.</p>"},{"location":"collections/cons/","title":"Cons","text":""},{"location":"collections/cons/#funstruct.collections.cons","title":"<code>funstruct.collections.cons</code>","text":"<p>A singly linked list.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.collections.cons import Cons, Nil\n&gt;&gt;&gt; xs = Cons(1, Cons(2, Cons(3, Nil())))\n&gt;&gt;&gt; xs.map(lambda x: x * 2)\nCons(2, Cons(4, Cons(6, Nil())))\n&gt;&gt;&gt; xs.filter(lambda x: x &gt; 1)\nCons(2, Cons(3, Nil()))\n&gt;&gt;&gt; xs &gt;&gt; (lambda x: Cons(x, Cons(x, Nil())))\nCons(1, Cons(1, Cons(2, Cons(2, Cons(3, Cons(3, Nil()))))))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList","title":"<code>CList</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[A]</code></p> <p>A Lisp/ML/Scala style singly linked list (cons list).</p> Performance characteristics <ul> <li>prepend (&lt;&lt;):  O(1) \u2014 just creates a new head node</li> <li>head/tail:     O(1) \u2014 direct field access</li> <li>append (+):    O(n) \u2014 must traverse to the end</li> <li>length:        O(n) \u2014 traverses entire list</li> <li>drop(k):       O(k) \u2014 follows k tail pointers</li> <li>take(k):       O(k) \u2014 copies k nodes</li> <li>map/filter:    O(n) \u2014 traverses and rebuilds</li> <li>bind/flatMap:  O(n*m) \u2014 maps then flattens</li> <li>reversed:      O(n) \u2014 builds new list via fold</li> <li>index access:  O(n) \u2014 no random access (use Python list for that)</li> </ul> <p>This is a persistent, immutable, singly-linked structure. Prepend is cheap; append is expensive. Best for recursive algorithms and stack-like access patterns.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>class CList(Monad, Generic[A]):\n    \"\"\"A Lisp/ML/Scala style singly linked list (cons list).\n\n    Performance characteristics:\n        - prepend (&lt;&lt;):  O(1) \u2014 just creates a new head node\n        - head/tail:     O(1) \u2014 direct field access\n        - append (+):    O(n) \u2014 must traverse to the end\n        - length:        O(n) \u2014 traverses entire list\n        - drop(k):       O(k) \u2014 follows k tail pointers\n        - take(k):       O(k) \u2014 copies k nodes\n        - map/filter:    O(n) \u2014 traverses and rebuilds\n        - bind/flatMap:  O(n*m) \u2014 maps then flattens\n        - reversed:      O(n) \u2014 builds new list via fold\n        - index access:  O(n) \u2014 no random access (use Python list for that)\n\n    This is a persistent, immutable, singly-linked structure. Prepend is cheap;\n    append is expensive. Best for recursive algorithms and stack-like access patterns.\n    \"\"\"\n\n    @abstractmethod\n    def append(self, other: CList) -&gt; CList:\n        \"\"\"Append another list to the end of this list.\n\n        Args:\n            other: The list to append.\n\n        Returns:\n            A new list with the elements of `other` appended to this list.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n        \"\"\"Fold the list from right to left.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking an element and the current accumulator.\n\n        Returns:\n            The result of folding the list from right to left.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n        \"\"\"Fold the list from left to right.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking the current accumulator and an element.\n\n        Returns:\n            The result of folding the list from left to right.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def drop(self, n: int) -&gt; CList:\n        \"\"\"Drop the first `n` elements from the list.\n\n        Args:\n            n: The number of elements to drop.\n\n        Returns:\n            A new list with the first `n` elements removed.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Drop elements from the list as long as the predicate function `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements removed while `f` is true.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def take(self, n: int) -&gt; CList:\n        \"\"\"Take the first `n` elements from the list.\n\n        Args:\n            n: The number of elements to take.\n\n        Returns:\n            A new list containing the first `n` elements.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Take elements from the list as long as the predicate function `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements taken while `f` is true.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def split_at(self, i: int) -&gt; tuple[CList, CList]:\n        \"\"\"Split the list into two lists at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            A tuple of two lists: the first containing elements up to `i`,\n            and the second containing the rest.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def insert_at(self, i: int, value: A) -&gt; CList:\n        \"\"\"Insert an element at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            The new list\n        \"\"\"\n        ...\n\n    def partition(self, f: Callable[[A], bool]) -&gt; tuple[CList, CList]:\n        \"\"\"Partition the list into two lists based on a predicate function.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A tuple of two lists: the first containing elements that satisfy `f`,\n            and the second containing the rest.\n        \"\"\"\n        accum = lambda a, x: (a &lt;&lt; x[0], x[1]) if f(a) else (x[0], a &lt;&lt; x[1])\n        return self.fold_right((Nil(), Nil()), accum)\n\n    def length(self) -&gt; int:\n        \"\"\"Compute the length of the list.\n\n        Returns:\n            The number of elements in the list.\n        \"\"\"\n        return self.fold_right(0, lambda _, acc: acc + 1)\n\n    def prepend(self, new_head: A) -&gt; CList:\n        \"\"\"Prepend an element to the list.\n\n        Args:\n            new_head: The element to prepend.\n\n        Returns:\n            A new list with `new_head` added to the beginning.\n        \"\"\"\n        return Cons(new_head, self)\n\n    def reversed(self) -&gt; CList:\n        \"\"\"Reverse the order of the elements in the list.\n\n        Returns:\n            A new list with the elements in reversed order.\n        \"\"\"\n        return self.fold_left(Nil(), lambda acc, h: Cons(h, acc))\n\n    def map(self, f: Callable) -&gt; CList:\n        \"\"\"Apply a function to each element of the list, producing a new list\n        with the results.\n\n        Args:\n            f: A function to apply to each element.\n\n        Returns:\n            A new list with the results of applying `f` to each element.\n        \"\"\"\n        return self.fold_right(Nil(), lambda a, acc: Cons(f(a), acc))\n\n    def ap(self, other) -&gt; CList:\n        \"\"\"Cartesian product \u2014 pair each element of self with each element of other.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def filter(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Filter the elements of the list based on a predicate function.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list containing only the elements that satisfy `f`.\n        \"\"\"\n        return self.fold_right(Nil(), lambda a, acc: Cons(a, acc) if f(a) else acc)\n\n    def flatten(self) -&gt; CList:\n        \"\"\"Flatten a list of lists into a single list.\n\n        Returns:\n            A new list with all nested lists flattened into a single list.\n        \"\"\"\n        return CList.flatten_(self)\n\n    def bind(self, f: Callable[[A], CList]) -&gt; CList:\n        \"\"\"Apply a function to each element and flatten the results.\n\n        Args:\n            f: A function that returns a list for each element.\n\n        Returns:\n            A new list with the results of applying `f` to each element,\n            flattened into a single list.\n        \"\"\"\n        return self.map(f).flatten()\n\n    def sorted(self, cmp: Callable[[A, A], int]) -&gt; CList:\n        \"\"\"Sort the list using a comparison function.\n\n        Args:\n            cmp: A comparison function to use for sorting.\n\n        Returns:\n            A new list with the elements sorted according to `cmp`.\n        \"\"\"\n\n        def merge(left: CList, right: CList) -&gt; CList:\n            match left, right:\n                case Nil(), r:\n                    return r\n                case l, Nil():\n                    return l\n                case Cons(lh, lt), Cons(rh, rt):\n                    if cmp(lh, rh) &lt;= 0:\n                        return lh &lt;&lt; merge(lt, right)\n                    return rh &lt;&lt; merge(left, rt)\n                case _:\n                    return Nil()\n\n        length = len(self)\n        if length &lt;= 1:\n            return self\n        left, right = self.split_at(length // 2)\n        return merge(left.sorted(cmp), right.sorted(cmp))\n\n    @staticmethod\n    def flatten_(lst: CList[CList[A]]) -&gt; CList:\n        \"\"\"Flatten a nested list of lists into a single list.\n\n        Args:\n            lst: A list of lists to be flattened.\n\n        Returns:\n            A new list with all nested lists flattened into a single list.\n        \"\"\"\n\n        def concat(left, right):\n            match left:\n                case Nil():\n                    return right\n                case Cons(h, t):\n                    return Cons(h, concat(t, right))\n                case _:\n                    return Nil()\n\n        def flatten(lst: CList[CList[A]]) -&gt; CList[A]:\n            match lst:\n                case Nil():\n                    return Nil()\n                case Cons(h, t):\n                    match h:\n                        case Cons(_, _):\n                            return concat(flatten(h), flatten(t))\n                        case _:\n                            return Cons(h, flatten(t))\n                case _:\n                    return Nil()\n\n        return flatten(lst)\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; CList:\n        \"\"\"Do-notation for CList. Collects all yielded results via flatMap.\"\"\"\n\n        def _collect():\n            gen = gen_fn()\n            try:\n                first = next(gen)\n                result = first.bind(lambda v: _send(gen, v))\n                return result\n            except StopIteration as e:\n                return Cons.pure(e.value)\n\n        def _send(gen, value):\n            try:\n                next_val = gen.send(value)\n                return next_val.bind(lambda v: _send(gen, v))\n            except StopIteration as e:\n                return Cons.pure(e.value)\n\n        return _collect()\n\n    @classmethod\n    def pure(cls, value) -&gt; CList:\n        \"\"\"Lift a value into a single-element list.\"\"\"\n        return Cons(value)\n\n    @staticmethod\n    def cons(a: A) -&gt; CList:\n        \"\"\"Create a new list with a single element.\n\n        Args:\n            a: The element to add to the list.\n\n        Returns:\n            A new list containing the single element `a`.\n        \"\"\"\n        return Cons(a)\n\n    @staticmethod\n    def empty() -&gt; CList:\n        \"\"\"Create an empty list.\n\n        Returns:\n            An empty list.\n        \"\"\"\n        return Nil()\n\n    @staticmethod\n    def new(*xs: A) -&gt; CList:\n        \"\"\"Create a new list from the given elements.\n\n        Args:\n            *xs: The elements to add to the list.\n\n        Returns:\n            A new list containing the elements `xs`.\n        \"\"\"\n        from funstruct.util.tailrec import tail_call, tco\n\n        @tco\n        def _go(items, idx, acc):\n            if idx &lt; 0:\n                return acc\n            return tail_call(_go)(items, idx - 1, Cons(items[idx], acc))\n\n        return _go(xs, len(xs) - 1, Nil())\n\n    @staticmethod\n    def from_iterable(iterable: Iterable[A]) -&gt; CList:\n        \"\"\"Create a new list from an iterable of elements.\n\n        &gt;&gt;&gt; CList.from_iterable([1, 2, 3]).to_list()\n        [1, 2, 3]\n        &gt;&gt;&gt; CList.from_iterable([]).to_list()\n        []\n        \"\"\"\n        return CList.new(*iterable)\n\n    def __rlshift__(self, other: A) -&gt; CList:\n        \"\"\"Prepend an element to the list using the `&lt;&lt;` operator.\n        Ex:\n        1 &lt;&lt; Nil() == Cons(1)\n\n        Args:\n            other: The element to prepend.\n\n        Returns:\n            A new list with `other` added to the beginning.\n        \"\"\"\n        return self.prepend(other)\n\n    def __add__(self, other: CList) -&gt; CList:\n        \"\"\"Concatenate two lists (Monoid append, not Applicative ap).\"\"\"\n        return self.append(other)\n\n    def __len__(self) -&gt; int:\n        \"\"\"Compute the length of the list.\n\n        Returns:\n            The number of elements in the list.\n        \"\"\"\n        return self.fold_right(0, lambda _, acc: acc + 1)\n\n    def to_list(self) -&gt; list[A]:\n        \"\"\"Convert to a Python list.\n\n        &gt;&gt;&gt; Cons(1, Cons(2, Cons(3, Nil()))).to_list()\n        [1, 2, 3]\n        &gt;&gt;&gt; Nil().to_list()\n        []\n        \"\"\"\n        return list(self)\n\n    def __iter__(self) -&gt; Iterator[A]:\n        current = self\n        while isinstance(current, Cons):\n            yield current.head\n            current = current.tail\n\n    def __eq__(self, other: object) -&gt; bool:\n        \"\"\"Check if this list is equal to another list.\n\n        Args:\n            other: The list to compare with.\n\n        Returns:\n            True if the lists are equal, False otherwise.\n        \"\"\"\n        match other:\n            case list():\n                return list(self) == other\n            case _:\n                pass\n        match self, other:\n            case Cons(sh, st), Cons(oh, ot):\n                return sh == oh and st == ot\n            case Nil(), Nil():\n                return True\n            case _:\n                return False\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.append","title":"<code>append(other)</code>  <code>abstractmethod</code>","text":"<p>Append another list to the end of this list.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>CList</code> <p>The list to append.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the elements of <code>other</code> appended to this list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef append(self, other: CList) -&gt; CList:\n    \"\"\"Append another list to the end of this list.\n\n    Args:\n        other: The list to append.\n\n    Returns:\n        A new list with the elements of `other` appended to this list.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.fold_right","title":"<code>fold_right(acc, f)</code>  <code>abstractmethod</code>","text":"<p>Fold the list from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[A, B], B]</code> <p>A function to apply, taking an element and the current accumulator.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The result of folding the list from right to left.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n    \"\"\"Fold the list from right to left.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking an element and the current accumulator.\n\n    Returns:\n        The result of folding the list from right to left.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.fold_left","title":"<code>fold_left(acc, f)</code>  <code>abstractmethod</code>","text":"<p>Fold the list from left to right.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[B, A], B]</code> <p>A function to apply, taking the current accumulator and an element.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The result of folding the list from left to right.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n    \"\"\"Fold the list from left to right.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking the current accumulator and an element.\n\n    Returns:\n        The result of folding the list from left to right.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.drop","title":"<code>drop(n)</code>  <code>abstractmethod</code>","text":"<p>Drop the first <code>n</code> elements from the list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to drop.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the first <code>n</code> elements removed.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef drop(self, n: int) -&gt; CList:\n    \"\"\"Drop the first `n` elements from the list.\n\n    Args:\n        n: The number of elements to drop.\n\n    Returns:\n        A new list with the first `n` elements removed.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.drop_while","title":"<code>drop_while(f)</code>  <code>abstractmethod</code>","text":"<p>Drop elements from the list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements removed while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Drop elements from the list as long as the predicate function `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements removed while `f` is true.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.take","title":"<code>take(n)</code>  <code>abstractmethod</code>","text":"<p>Take the first <code>n</code> elements from the list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to take.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the first <code>n</code> elements.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef take(self, n: int) -&gt; CList:\n    \"\"\"Take the first `n` elements from the list.\n\n    Args:\n        n: The number of elements to take.\n\n    Returns:\n        A new list containing the first `n` elements.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.take_while","title":"<code>take_while(f)</code>  <code>abstractmethod</code>","text":"<p>Take elements from the list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements taken while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef take_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Take elements from the list as long as the predicate function `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements taken while `f` is true.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.split_at","title":"<code>split_at(i)</code>  <code>abstractmethod</code>","text":"<p>Split the list into two lists at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A tuple of two lists: the first containing elements up to <code>i</code>,</p> <code>CList</code> <p>and the second containing the rest.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef split_at(self, i: int) -&gt; tuple[CList, CList]:\n    \"\"\"Split the list into two lists at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        A tuple of two lists: the first containing elements up to `i`,\n        and the second containing the rest.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.insert_at","title":"<code>insert_at(i, value)</code>  <code>abstractmethod</code>","text":"<p>Insert an element at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The new list</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef insert_at(self, i: int, value: A) -&gt; CList:\n    \"\"\"Insert an element at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        The new list\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.partition","title":"<code>partition(f)</code>","text":"<p>Partition the list into two lists based on a predicate function.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A tuple of two lists: the first containing elements that satisfy <code>f</code>,</p> <code>CList</code> <p>and the second containing the rest.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def partition(self, f: Callable[[A], bool]) -&gt; tuple[CList, CList]:\n    \"\"\"Partition the list into two lists based on a predicate function.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A tuple of two lists: the first containing elements that satisfy `f`,\n        and the second containing the rest.\n    \"\"\"\n    accum = lambda a, x: (a &lt;&lt; x[0], x[1]) if f(a) else (x[0], a &lt;&lt; x[1])\n    return self.fold_right((Nil(), Nil()), accum)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.length","title":"<code>length()</code>","text":"<p>Compute the length of the list.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of elements in the list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def length(self) -&gt; int:\n    \"\"\"Compute the length of the list.\n\n    Returns:\n        The number of elements in the list.\n    \"\"\"\n    return self.fold_right(0, lambda _, acc: acc + 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.prepend","title":"<code>prepend(new_head)</code>","text":"<p>Prepend an element to the list.</p> <p>Parameters:</p> Name Type Description Default <code>new_head</code> <code>A</code> <p>The element to prepend.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with <code>new_head</code> added to the beginning.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def prepend(self, new_head: A) -&gt; CList:\n    \"\"\"Prepend an element to the list.\n\n    Args:\n        new_head: The element to prepend.\n\n    Returns:\n        A new list with `new_head` added to the beginning.\n    \"\"\"\n    return Cons(new_head, self)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.reversed","title":"<code>reversed()</code>","text":"<p>Reverse the order of the elements in the list.</p> <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the elements in reversed order.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def reversed(self) -&gt; CList:\n    \"\"\"Reverse the order of the elements in the list.\n\n    Returns:\n        A new list with the elements in reversed order.\n    \"\"\"\n    return self.fold_left(Nil(), lambda acc, h: Cons(h, acc))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.map","title":"<code>map(f)</code>","text":"<p>Apply a function to each element of the list, producing a new list with the results.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable</code> <p>A function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the results of applying <code>f</code> to each element.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def map(self, f: Callable) -&gt; CList:\n    \"\"\"Apply a function to each element of the list, producing a new list\n    with the results.\n\n    Args:\n        f: A function to apply to each element.\n\n    Returns:\n        A new list with the results of applying `f` to each element.\n    \"\"\"\n    return self.fold_right(Nil(), lambda a, acc: Cons(f(a), acc))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.ap","title":"<code>ap(other)</code>","text":"<p>Cartesian product \u2014 pair each element of self with each element of other.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def ap(self, other) -&gt; CList:\n    \"\"\"Cartesian product \u2014 pair each element of self with each element of other.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.filter","title":"<code>filter(f)</code>","text":"<p>Filter the elements of the list based on a predicate function.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing only the elements that satisfy <code>f</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def filter(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Filter the elements of the list based on a predicate function.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list containing only the elements that satisfy `f`.\n    \"\"\"\n    return self.fold_right(Nil(), lambda a, acc: Cons(a, acc) if f(a) else acc)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.flatten","title":"<code>flatten()</code>","text":"<p>Flatten a list of lists into a single list.</p> <p>Returns:</p> Type Description <code>CList</code> <p>A new list with all nested lists flattened into a single list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def flatten(self) -&gt; CList:\n    \"\"\"Flatten a list of lists into a single list.\n\n    Returns:\n        A new list with all nested lists flattened into a single list.\n    \"\"\"\n    return CList.flatten_(self)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.bind","title":"<code>bind(f)</code>","text":"<p>Apply a function to each element and flatten the results.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], CList]</code> <p>A function that returns a list for each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the results of applying <code>f</code> to each element,</p> <code>CList</code> <p>flattened into a single list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def bind(self, f: Callable[[A], CList]) -&gt; CList:\n    \"\"\"Apply a function to each element and flatten the results.\n\n    Args:\n        f: A function that returns a list for each element.\n\n    Returns:\n        A new list with the results of applying `f` to each element,\n        flattened into a single list.\n    \"\"\"\n    return self.map(f).flatten()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.sorted","title":"<code>sorted(cmp)</code>","text":"<p>Sort the list using a comparison function.</p> <p>Parameters:</p> Name Type Description Default <code>cmp</code> <code>Callable[[A, A], int]</code> <p>A comparison function to use for sorting.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the elements sorted according to <code>cmp</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def sorted(self, cmp: Callable[[A, A], int]) -&gt; CList:\n    \"\"\"Sort the list using a comparison function.\n\n    Args:\n        cmp: A comparison function to use for sorting.\n\n    Returns:\n        A new list with the elements sorted according to `cmp`.\n    \"\"\"\n\n    def merge(left: CList, right: CList) -&gt; CList:\n        match left, right:\n            case Nil(), r:\n                return r\n            case l, Nil():\n                return l\n            case Cons(lh, lt), Cons(rh, rt):\n                if cmp(lh, rh) &lt;= 0:\n                    return lh &lt;&lt; merge(lt, right)\n                return rh &lt;&lt; merge(left, rt)\n            case _:\n                return Nil()\n\n    length = len(self)\n    if length &lt;= 1:\n        return self\n    left, right = self.split_at(length // 2)\n    return merge(left.sorted(cmp), right.sorted(cmp))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.flatten_","title":"<code>flatten_(lst)</code>  <code>staticmethod</code>","text":"<p>Flatten a nested list of lists into a single list.</p> <p>Parameters:</p> Name Type Description Default <code>lst</code> <code>CList[CList[A]]</code> <p>A list of lists to be flattened.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with all nested lists flattened into a single list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef flatten_(lst: CList[CList[A]]) -&gt; CList:\n    \"\"\"Flatten a nested list of lists into a single list.\n\n    Args:\n        lst: A list of lists to be flattened.\n\n    Returns:\n        A new list with all nested lists flattened into a single list.\n    \"\"\"\n\n    def concat(left, right):\n        match left:\n            case Nil():\n                return right\n            case Cons(h, t):\n                return Cons(h, concat(t, right))\n            case _:\n                return Nil()\n\n    def flatten(lst: CList[CList[A]]) -&gt; CList[A]:\n        match lst:\n            case Nil():\n                return Nil()\n            case Cons(h, t):\n                match h:\n                    case Cons(_, _):\n                        return concat(flatten(h), flatten(t))\n                    case _:\n                        return Cons(h, flatten(t))\n            case _:\n                return Nil()\n\n    return flatten(lst)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation for CList. Collects all yielded results via flatMap.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; CList:\n    \"\"\"Do-notation for CList. Collects all yielded results via flatMap.\"\"\"\n\n    def _collect():\n        gen = gen_fn()\n        try:\n            first = next(gen)\n            result = first.bind(lambda v: _send(gen, v))\n            return result\n        except StopIteration as e:\n            return Cons.pure(e.value)\n\n    def _send(gen, value):\n        try:\n            next_val = gen.send(value)\n            return next_val.bind(lambda v: _send(gen, v))\n        except StopIteration as e:\n            return Cons.pure(e.value)\n\n    return _collect()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a value into a single-element list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@classmethod\ndef pure(cls, value) -&gt; CList:\n    \"\"\"Lift a value into a single-element list.\"\"\"\n    return Cons(value)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.cons","title":"<code>cons(a)</code>  <code>staticmethod</code>","text":"<p>Create a new list with a single element.</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>A</code> <p>The element to add to the list.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the single element <code>a</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef cons(a: A) -&gt; CList:\n    \"\"\"Create a new list with a single element.\n\n    Args:\n        a: The element to add to the list.\n\n    Returns:\n        A new list containing the single element `a`.\n    \"\"\"\n    return Cons(a)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.empty","title":"<code>empty()</code>  <code>staticmethod</code>","text":"<p>Create an empty list.</p> <p>Returns:</p> Type Description <code>CList</code> <p>An empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef empty() -&gt; CList:\n    \"\"\"Create an empty list.\n\n    Returns:\n        An empty list.\n    \"\"\"\n    return Nil()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.new","title":"<code>new(*xs)</code>  <code>staticmethod</code>","text":"<p>Create a new list from the given elements.</p> <p>Parameters:</p> Name Type Description Default <code>*xs</code> <code>A</code> <p>The elements to add to the list.</p> <code>()</code> <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the elements <code>xs</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef new(*xs: A) -&gt; CList:\n    \"\"\"Create a new list from the given elements.\n\n    Args:\n        *xs: The elements to add to the list.\n\n    Returns:\n        A new list containing the elements `xs`.\n    \"\"\"\n    from funstruct.util.tailrec import tail_call, tco\n\n    @tco\n    def _go(items, idx, acc):\n        if idx &lt; 0:\n            return acc\n        return tail_call(_go)(items, idx - 1, Cons(items[idx], acc))\n\n    return _go(xs, len(xs) - 1, Nil())\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.from_iterable","title":"<code>from_iterable(iterable)</code>  <code>staticmethod</code>","text":"<p>Create a new list from an iterable of elements.</p> <p>CList.from_iterable([1, 2, 3]).to_list() [1, 2, 3] CList.from_iterable([]).to_list() []</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef from_iterable(iterable: Iterable[A]) -&gt; CList:\n    \"\"\"Create a new list from an iterable of elements.\n\n    &gt;&gt;&gt; CList.from_iterable([1, 2, 3]).to_list()\n    [1, 2, 3]\n    &gt;&gt;&gt; CList.from_iterable([]).to_list()\n    []\n    \"\"\"\n    return CList.new(*iterable)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__rlshift__","title":"<code>__rlshift__(other)</code>","text":"<p>Prepend an element to the list using the <code>&lt;&lt;</code> operator. Ex: 1 &lt;&lt; Nil() == Cons(1)</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>A</code> <p>The element to prepend.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with <code>other</code> added to the beginning.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __rlshift__(self, other: A) -&gt; CList:\n    \"\"\"Prepend an element to the list using the `&lt;&lt;` operator.\n    Ex:\n    1 &lt;&lt; Nil() == Cons(1)\n\n    Args:\n        other: The element to prepend.\n\n    Returns:\n        A new list with `other` added to the beginning.\n    \"\"\"\n    return self.prepend(other)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__add__","title":"<code>__add__(other)</code>","text":"<p>Concatenate two lists (Monoid append, not Applicative ap).</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __add__(self, other: CList) -&gt; CList:\n    \"\"\"Concatenate two lists (Monoid append, not Applicative ap).\"\"\"\n    return self.append(other)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__len__","title":"<code>__len__()</code>","text":"<p>Compute the length of the list.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of elements in the list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Compute the length of the list.\n\n    Returns:\n        The number of elements in the list.\n    \"\"\"\n    return self.fold_right(0, lambda _, acc: acc + 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.to_list","title":"<code>to_list()</code>","text":"<p>Convert to a Python list.</p> <p>Cons(1, Cons(2, Cons(3, Nil()))).to_list() [1, 2, 3] Nil().to_list() []</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def to_list(self) -&gt; list[A]:\n    \"\"\"Convert to a Python list.\n\n    &gt;&gt;&gt; Cons(1, Cons(2, Cons(3, Nil()))).to_list()\n    [1, 2, 3]\n    &gt;&gt;&gt; Nil().to_list()\n    []\n    \"\"\"\n    return list(self)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__eq__","title":"<code>__eq__(other)</code>","text":"<p>Check if this list is equal to another list.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>The list to compare with.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the lists are equal, False otherwise.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"Check if this list is equal to another list.\n\n    Args:\n        other: The list to compare with.\n\n    Returns:\n        True if the lists are equal, False otherwise.\n    \"\"\"\n    match other:\n        case list():\n            return list(self) == other\n        case _:\n            pass\n    match self, other:\n        case Cons(sh, st), Cons(oh, ot):\n            return sh == oh and st == ot\n        case Nil(), Nil():\n            return True\n        case _:\n            return False\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil","title":"<code>Nil</code>","text":"<p>               Bases: <code>CList</code></p> <p>A singleton representing the empty list/end of a singly linked list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>class Nil(CList):\n    \"\"\"A singleton representing the empty list/end of a singly linked list.\"\"\"\n\n    _instance = None\n\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n\n    def __repr__(self) -&gt; str:\n        return \"Nil()\"\n\n    def __str__(self) -&gt; str:\n        return \"Nil\"\n\n    def __bool__(self) -&gt; bool:\n        return False\n\n    def append(self, other: CList) -&gt; CList:\n        \"\"\"Append another list to the empty list.\n\n        Args:\n            other: The list to append.\n\n        Returns:\n            The list `other`, since appending to an empty list results in `other`.\n        \"\"\"\n        return other\n\n    def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n        \"\"\"Fold the empty list from right to left.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking an element and the current accumulator.\n\n        Returns:\n            The accumulator value, since folding an empty list results\n            in the initial value.\n        \"\"\"\n        return acc\n\n    def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n        \"\"\"Fold the empty list from left to right.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking the current accumulator and an element.\n\n        Returns:\n            The accumulator value, since folding an empty list results\n            in the initial value.\n        \"\"\"\n        return acc\n\n    def drop(self, n: int) -&gt; CList:\n        \"\"\"Drop the first `n` elements from the empty list.\n\n        Args:\n            n: The number of elements to drop.\n\n        Returns:\n            The empty list, since dropping elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Drop elements from the empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            The empty list, since dropping elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def take(self, n: int) -&gt; CList:\n        \"\"\"Take the first `n` elements from the empty list.\n\n        Args:\n            n: The number of elements to take.\n\n        Returns:\n            The empty list, since taking elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Take elements from the empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            The empty list, since taking elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def split_at(self, i: int) -&gt; tuple[CList, CList]:\n        \"\"\"Split the empty list into two lists at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            A tuple of two empty lists.\n        \"\"\"\n        return self, self\n\n    def insert_at(self, i: int, value: A) -&gt; CList:\n        \"\"\"Insert an element at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            The new list\n        \"\"\"\n        return Cons(value, Nil())\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.append","title":"<code>append(other)</code>","text":"<p>Append another list to the empty list.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>CList</code> <p>The list to append.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The list <code>other</code>, since appending to an empty list results in <code>other</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def append(self, other: CList) -&gt; CList:\n    \"\"\"Append another list to the empty list.\n\n    Args:\n        other: The list to append.\n\n    Returns:\n        The list `other`, since appending to an empty list results in `other`.\n    \"\"\"\n    return other\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.fold_right","title":"<code>fold_right(acc, f)</code>","text":"<p>Fold the empty list from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[A, B], B]</code> <p>A function to apply, taking an element and the current accumulator.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The accumulator value, since folding an empty list results</p> <code>B</code> <p>in the initial value.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n    \"\"\"Fold the empty list from right to left.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking an element and the current accumulator.\n\n    Returns:\n        The accumulator value, since folding an empty list results\n        in the initial value.\n    \"\"\"\n    return acc\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.fold_left","title":"<code>fold_left(acc, f)</code>","text":"<p>Fold the empty list from left to right.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[B, A], B]</code> <p>A function to apply, taking the current accumulator and an element.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The accumulator value, since folding an empty list results</p> <code>B</code> <p>in the initial value.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n    \"\"\"Fold the empty list from left to right.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking the current accumulator and an element.\n\n    Returns:\n        The accumulator value, since folding an empty list results\n        in the initial value.\n    \"\"\"\n    return acc\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.drop","title":"<code>drop(n)</code>","text":"<p>Drop the first <code>n</code> elements from the empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to drop.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since dropping elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop(self, n: int) -&gt; CList:\n    \"\"\"Drop the first `n` elements from the empty list.\n\n    Args:\n        n: The number of elements to drop.\n\n    Returns:\n        The empty list, since dropping elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.drop_while","title":"<code>drop_while(f)</code>","text":"<p>Drop elements from the empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since dropping elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Drop elements from the empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        The empty list, since dropping elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.take","title":"<code>take(n)</code>","text":"<p>Take the first <code>n</code> elements from the empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to take.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since taking elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take(self, n: int) -&gt; CList:\n    \"\"\"Take the first `n` elements from the empty list.\n\n    Args:\n        n: The number of elements to take.\n\n    Returns:\n        The empty list, since taking elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.take_while","title":"<code>take_while(f)</code>","text":"<p>Take elements from the empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since taking elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Take elements from the empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        The empty list, since taking elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.split_at","title":"<code>split_at(i)</code>","text":"<p>Split the empty list into two lists at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>tuple[CList, CList]</code> <p>A tuple of two empty lists.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def split_at(self, i: int) -&gt; tuple[CList, CList]:\n    \"\"\"Split the empty list into two lists at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        A tuple of two empty lists.\n    \"\"\"\n    return self, self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.insert_at","title":"<code>insert_at(i, value)</code>","text":"<p>Insert an element at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The new list</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def insert_at(self, i: int, value: A) -&gt; CList:\n    \"\"\"Insert an element at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        The new list\n    \"\"\"\n    return Cons(value, Nil())\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons","title":"<code>Cons</code>  <code>dataclass</code>","text":"<p>               Bases: <code>CList[A]</code></p> <p>Represents a non-empty list with a head element and a tail list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Cons(CList[A]):\n    \"\"\"Represents a non-empty list with a head element and a tail list.\"\"\"\n\n    head: A\n    tail: CList[A] = field(default_factory=Nil)\n\n    def __bool__(self) -&gt; bool:\n        return True\n\n    def __repr__(self) -&gt; str:\n        return f\"Cons({repr(self.head)}, {repr(self.tail)})\"\n\n    def __str__(self) -&gt; str:\n        def _fmt(elem) -&gt; str:\n            match elem:\n                case CList():\n                    return f\"[{', '.join(_fmt(e) for e in elem)}]\"\n                case _:\n                    return str(elem)\n\n        return f\"CList([{', '.join(_fmt(e) for e in self)}])\"\n\n    def append(self, other: CList) -&gt; CList:\n        \"\"\"Append another list to the end of this non-empty list.\"\"\"\n        return self.reversed().fold_left(other, lambda acc, h: Cons(h, acc))\n\n    def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n        \"\"\"Fold from right to left. Stack-safe via reverse + fold_left.\"\"\"\n        return self.reversed().fold_left(acc, lambda a, b: f(b, a))\n\n    def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n        \"\"\"Fold from left to right. Tail-recursive via @tco.\"\"\"\n        from funstruct.util.tailrec import tail_call, tco\n\n        @tco\n        def _go(current, result):\n            match current:\n                case Nil():\n                    return result\n                case Cons(h, t):\n                    return tail_call(_go)(t, f(result, h))\n\n        return _go(self, acc)\n\n    def drop(self, n: int) -&gt; CList:\n        \"\"\"Drop the first `n` elements from the non-empty list.\n\n        Args:\n            n: The number of elements to drop.\n\n        Returns:\n            A new list with the first `n` elements removed.\n        \"\"\"\n        return self if n &lt;= 0 else self.tail.drop(n - 1)\n\n    def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Drop elements from the non-empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements removed while `f` is true.\n        \"\"\"\n        return self if not f(self.head) else self.tail.drop_while(f)\n\n    def take(self, n: int) -&gt; CList:\n        \"\"\"Take the first `n` elements from the non-empty list.\n\n        Args:\n            n: The number of elements to take.\n\n        Returns:\n            A new list containing the first `n` elements.\n        \"\"\"\n        return Cons(self.head) if n &lt;= 1 else self.head &lt;&lt; self.tail.take(n - 1)\n\n    def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Take elements from the non-empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements taken while `f` is true.\n        \"\"\"\n        return self.head &lt;&lt; self.tail.take_while(f) if f(self.head) else Nil()\n\n    def split_at(self, i: int) -&gt; tuple[CList, CList]:\n        \"\"\"Split the non-empty list into two lists at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            A tuple of two lists: the first containing elements up to `i`,\n            and the second containing the rest.\n        \"\"\"\n        return self.take(i), self.drop(i)\n\n    def insert_at(self, i: int, value: A) -&gt; CList:\n        \"\"\"Insert an element at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            The new list\n        \"\"\"\n        if i &lt;= 0:\n            return Cons(value, self)\n        return Cons(self.head, self.tail.insert_at(i - 1, value))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.append","title":"<code>append(other)</code>","text":"<p>Append another list to the end of this non-empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def append(self, other: CList) -&gt; CList:\n    \"\"\"Append another list to the end of this non-empty list.\"\"\"\n    return self.reversed().fold_left(other, lambda acc, h: Cons(h, acc))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.fold_right","title":"<code>fold_right(acc, f)</code>","text":"<p>Fold from right to left. Stack-safe via reverse + fold_left.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n    \"\"\"Fold from right to left. Stack-safe via reverse + fold_left.\"\"\"\n    return self.reversed().fold_left(acc, lambda a, b: f(b, a))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.fold_left","title":"<code>fold_left(acc, f)</code>","text":"<p>Fold from left to right. Tail-recursive via @tco.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n    \"\"\"Fold from left to right. Tail-recursive via @tco.\"\"\"\n    from funstruct.util.tailrec import tail_call, tco\n\n    @tco\n    def _go(current, result):\n        match current:\n            case Nil():\n                return result\n            case Cons(h, t):\n                return tail_call(_go)(t, f(result, h))\n\n    return _go(self, acc)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.drop","title":"<code>drop(n)</code>","text":"<p>Drop the first <code>n</code> elements from the non-empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to drop.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the first <code>n</code> elements removed.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop(self, n: int) -&gt; CList:\n    \"\"\"Drop the first `n` elements from the non-empty list.\n\n    Args:\n        n: The number of elements to drop.\n\n    Returns:\n        A new list with the first `n` elements removed.\n    \"\"\"\n    return self if n &lt;= 0 else self.tail.drop(n - 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.drop_while","title":"<code>drop_while(f)</code>","text":"<p>Drop elements from the non-empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements removed while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Drop elements from the non-empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements removed while `f` is true.\n    \"\"\"\n    return self if not f(self.head) else self.tail.drop_while(f)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.take","title":"<code>take(n)</code>","text":"<p>Take the first <code>n</code> elements from the non-empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to take.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the first <code>n</code> elements.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take(self, n: int) -&gt; CList:\n    \"\"\"Take the first `n` elements from the non-empty list.\n\n    Args:\n        n: The number of elements to take.\n\n    Returns:\n        A new list containing the first `n` elements.\n    \"\"\"\n    return Cons(self.head) if n &lt;= 1 else self.head &lt;&lt; self.tail.take(n - 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.take_while","title":"<code>take_while(f)</code>","text":"<p>Take elements from the non-empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements taken while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Take elements from the non-empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements taken while `f` is true.\n    \"\"\"\n    return self.head &lt;&lt; self.tail.take_while(f) if f(self.head) else Nil()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.split_at","title":"<code>split_at(i)</code>","text":"<p>Split the non-empty list into two lists at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A tuple of two lists: the first containing elements up to <code>i</code>,</p> <code>CList</code> <p>and the second containing the rest.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def split_at(self, i: int) -&gt; tuple[CList, CList]:\n    \"\"\"Split the non-empty list into two lists at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        A tuple of two lists: the first containing elements up to `i`,\n        and the second containing the rest.\n    \"\"\"\n    return self.take(i), self.drop(i)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.insert_at","title":"<code>insert_at(i, value)</code>","text":"<p>Insert an element at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The new list</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def insert_at(self, i: int, value: A) -&gt; CList:\n    \"\"\"Insert an element at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        The new list\n    \"\"\"\n    if i &lt;= 0:\n        return Cons(value, self)\n    return Cons(self.head, self.tail.insert_at(i - 1, value))\n</code></pre>"},{"location":"collections/cons/#api-reference","title":"API Reference","text":""},{"location":"collections/cons/#funstruct.collections.cons.CList","title":"<code>funstruct.collections.cons.CList</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[A]</code></p> <p>A Lisp/ML/Scala style singly linked list (cons list).</p> Performance characteristics <ul> <li>prepend (&lt;&lt;):  O(1) \u2014 just creates a new head node</li> <li>head/tail:     O(1) \u2014 direct field access</li> <li>append (+):    O(n) \u2014 must traverse to the end</li> <li>length:        O(n) \u2014 traverses entire list</li> <li>drop(k):       O(k) \u2014 follows k tail pointers</li> <li>take(k):       O(k) \u2014 copies k nodes</li> <li>map/filter:    O(n) \u2014 traverses and rebuilds</li> <li>bind/flatMap:  O(n*m) \u2014 maps then flattens</li> <li>reversed:      O(n) \u2014 builds new list via fold</li> <li>index access:  O(n) \u2014 no random access (use Python list for that)</li> </ul> <p>This is a persistent, immutable, singly-linked structure. Prepend is cheap; append is expensive. Best for recursive algorithms and stack-like access patterns.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>class CList(Monad, Generic[A]):\n    \"\"\"A Lisp/ML/Scala style singly linked list (cons list).\n\n    Performance characteristics:\n        - prepend (&lt;&lt;):  O(1) \u2014 just creates a new head node\n        - head/tail:     O(1) \u2014 direct field access\n        - append (+):    O(n) \u2014 must traverse to the end\n        - length:        O(n) \u2014 traverses entire list\n        - drop(k):       O(k) \u2014 follows k tail pointers\n        - take(k):       O(k) \u2014 copies k nodes\n        - map/filter:    O(n) \u2014 traverses and rebuilds\n        - bind/flatMap:  O(n*m) \u2014 maps then flattens\n        - reversed:      O(n) \u2014 builds new list via fold\n        - index access:  O(n) \u2014 no random access (use Python list for that)\n\n    This is a persistent, immutable, singly-linked structure. Prepend is cheap;\n    append is expensive. Best for recursive algorithms and stack-like access patterns.\n    \"\"\"\n\n    @abstractmethod\n    def append(self, other: CList) -&gt; CList:\n        \"\"\"Append another list to the end of this list.\n\n        Args:\n            other: The list to append.\n\n        Returns:\n            A new list with the elements of `other` appended to this list.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n        \"\"\"Fold the list from right to left.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking an element and the current accumulator.\n\n        Returns:\n            The result of folding the list from right to left.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n        \"\"\"Fold the list from left to right.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking the current accumulator and an element.\n\n        Returns:\n            The result of folding the list from left to right.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def drop(self, n: int) -&gt; CList:\n        \"\"\"Drop the first `n` elements from the list.\n\n        Args:\n            n: The number of elements to drop.\n\n        Returns:\n            A new list with the first `n` elements removed.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Drop elements from the list as long as the predicate function `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements removed while `f` is true.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def take(self, n: int) -&gt; CList:\n        \"\"\"Take the first `n` elements from the list.\n\n        Args:\n            n: The number of elements to take.\n\n        Returns:\n            A new list containing the first `n` elements.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Take elements from the list as long as the predicate function `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements taken while `f` is true.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def split_at(self, i: int) -&gt; tuple[CList, CList]:\n        \"\"\"Split the list into two lists at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            A tuple of two lists: the first containing elements up to `i`,\n            and the second containing the rest.\n        \"\"\"\n        ...\n\n    @abstractmethod\n    def insert_at(self, i: int, value: A) -&gt; CList:\n        \"\"\"Insert an element at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            The new list\n        \"\"\"\n        ...\n\n    def partition(self, f: Callable[[A], bool]) -&gt; tuple[CList, CList]:\n        \"\"\"Partition the list into two lists based on a predicate function.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A tuple of two lists: the first containing elements that satisfy `f`,\n            and the second containing the rest.\n        \"\"\"\n        accum = lambda a, x: (a &lt;&lt; x[0], x[1]) if f(a) else (x[0], a &lt;&lt; x[1])\n        return self.fold_right((Nil(), Nil()), accum)\n\n    def length(self) -&gt; int:\n        \"\"\"Compute the length of the list.\n\n        Returns:\n            The number of elements in the list.\n        \"\"\"\n        return self.fold_right(0, lambda _, acc: acc + 1)\n\n    def prepend(self, new_head: A) -&gt; CList:\n        \"\"\"Prepend an element to the list.\n\n        Args:\n            new_head: The element to prepend.\n\n        Returns:\n            A new list with `new_head` added to the beginning.\n        \"\"\"\n        return Cons(new_head, self)\n\n    def reversed(self) -&gt; CList:\n        \"\"\"Reverse the order of the elements in the list.\n\n        Returns:\n            A new list with the elements in reversed order.\n        \"\"\"\n        return self.fold_left(Nil(), lambda acc, h: Cons(h, acc))\n\n    def map(self, f: Callable) -&gt; CList:\n        \"\"\"Apply a function to each element of the list, producing a new list\n        with the results.\n\n        Args:\n            f: A function to apply to each element.\n\n        Returns:\n            A new list with the results of applying `f` to each element.\n        \"\"\"\n        return self.fold_right(Nil(), lambda a, acc: Cons(f(a), acc))\n\n    def ap(self, other) -&gt; CList:\n        \"\"\"Cartesian product \u2014 pair each element of self with each element of other.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def filter(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Filter the elements of the list based on a predicate function.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list containing only the elements that satisfy `f`.\n        \"\"\"\n        return self.fold_right(Nil(), lambda a, acc: Cons(a, acc) if f(a) else acc)\n\n    def flatten(self) -&gt; CList:\n        \"\"\"Flatten a list of lists into a single list.\n\n        Returns:\n            A new list with all nested lists flattened into a single list.\n        \"\"\"\n        return CList.flatten_(self)\n\n    def bind(self, f: Callable[[A], CList]) -&gt; CList:\n        \"\"\"Apply a function to each element and flatten the results.\n\n        Args:\n            f: A function that returns a list for each element.\n\n        Returns:\n            A new list with the results of applying `f` to each element,\n            flattened into a single list.\n        \"\"\"\n        return self.map(f).flatten()\n\n    def sorted(self, cmp: Callable[[A, A], int]) -&gt; CList:\n        \"\"\"Sort the list using a comparison function.\n\n        Args:\n            cmp: A comparison function to use for sorting.\n\n        Returns:\n            A new list with the elements sorted according to `cmp`.\n        \"\"\"\n\n        def merge(left: CList, right: CList) -&gt; CList:\n            match left, right:\n                case Nil(), r:\n                    return r\n                case l, Nil():\n                    return l\n                case Cons(lh, lt), Cons(rh, rt):\n                    if cmp(lh, rh) &lt;= 0:\n                        return lh &lt;&lt; merge(lt, right)\n                    return rh &lt;&lt; merge(left, rt)\n                case _:\n                    return Nil()\n\n        length = len(self)\n        if length &lt;= 1:\n            return self\n        left, right = self.split_at(length // 2)\n        return merge(left.sorted(cmp), right.sorted(cmp))\n\n    @staticmethod\n    def flatten_(lst: CList[CList[A]]) -&gt; CList:\n        \"\"\"Flatten a nested list of lists into a single list.\n\n        Args:\n            lst: A list of lists to be flattened.\n\n        Returns:\n            A new list with all nested lists flattened into a single list.\n        \"\"\"\n\n        def concat(left, right):\n            match left:\n                case Nil():\n                    return right\n                case Cons(h, t):\n                    return Cons(h, concat(t, right))\n                case _:\n                    return Nil()\n\n        def flatten(lst: CList[CList[A]]) -&gt; CList[A]:\n            match lst:\n                case Nil():\n                    return Nil()\n                case Cons(h, t):\n                    match h:\n                        case Cons(_, _):\n                            return concat(flatten(h), flatten(t))\n                        case _:\n                            return Cons(h, flatten(t))\n                case _:\n                    return Nil()\n\n        return flatten(lst)\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; CList:\n        \"\"\"Do-notation for CList. Collects all yielded results via flatMap.\"\"\"\n\n        def _collect():\n            gen = gen_fn()\n            try:\n                first = next(gen)\n                result = first.bind(lambda v: _send(gen, v))\n                return result\n            except StopIteration as e:\n                return Cons.pure(e.value)\n\n        def _send(gen, value):\n            try:\n                next_val = gen.send(value)\n                return next_val.bind(lambda v: _send(gen, v))\n            except StopIteration as e:\n                return Cons.pure(e.value)\n\n        return _collect()\n\n    @classmethod\n    def pure(cls, value) -&gt; CList:\n        \"\"\"Lift a value into a single-element list.\"\"\"\n        return Cons(value)\n\n    @staticmethod\n    def cons(a: A) -&gt; CList:\n        \"\"\"Create a new list with a single element.\n\n        Args:\n            a: The element to add to the list.\n\n        Returns:\n            A new list containing the single element `a`.\n        \"\"\"\n        return Cons(a)\n\n    @staticmethod\n    def empty() -&gt; CList:\n        \"\"\"Create an empty list.\n\n        Returns:\n            An empty list.\n        \"\"\"\n        return Nil()\n\n    @staticmethod\n    def new(*xs: A) -&gt; CList:\n        \"\"\"Create a new list from the given elements.\n\n        Args:\n            *xs: The elements to add to the list.\n\n        Returns:\n            A new list containing the elements `xs`.\n        \"\"\"\n        from funstruct.util.tailrec import tail_call, tco\n\n        @tco\n        def _go(items, idx, acc):\n            if idx &lt; 0:\n                return acc\n            return tail_call(_go)(items, idx - 1, Cons(items[idx], acc))\n\n        return _go(xs, len(xs) - 1, Nil())\n\n    @staticmethod\n    def from_iterable(iterable: Iterable[A]) -&gt; CList:\n        \"\"\"Create a new list from an iterable of elements.\n\n        &gt;&gt;&gt; CList.from_iterable([1, 2, 3]).to_list()\n        [1, 2, 3]\n        &gt;&gt;&gt; CList.from_iterable([]).to_list()\n        []\n        \"\"\"\n        return CList.new(*iterable)\n\n    def __rlshift__(self, other: A) -&gt; CList:\n        \"\"\"Prepend an element to the list using the `&lt;&lt;` operator.\n        Ex:\n        1 &lt;&lt; Nil() == Cons(1)\n\n        Args:\n            other: The element to prepend.\n\n        Returns:\n            A new list with `other` added to the beginning.\n        \"\"\"\n        return self.prepend(other)\n\n    def __add__(self, other: CList) -&gt; CList:\n        \"\"\"Concatenate two lists (Monoid append, not Applicative ap).\"\"\"\n        return self.append(other)\n\n    def __len__(self) -&gt; int:\n        \"\"\"Compute the length of the list.\n\n        Returns:\n            The number of elements in the list.\n        \"\"\"\n        return self.fold_right(0, lambda _, acc: acc + 1)\n\n    def to_list(self) -&gt; list[A]:\n        \"\"\"Convert to a Python list.\n\n        &gt;&gt;&gt; Cons(1, Cons(2, Cons(3, Nil()))).to_list()\n        [1, 2, 3]\n        &gt;&gt;&gt; Nil().to_list()\n        []\n        \"\"\"\n        return list(self)\n\n    def __iter__(self) -&gt; Iterator[A]:\n        current = self\n        while isinstance(current, Cons):\n            yield current.head\n            current = current.tail\n\n    def __eq__(self, other: object) -&gt; bool:\n        \"\"\"Check if this list is equal to another list.\n\n        Args:\n            other: The list to compare with.\n\n        Returns:\n            True if the lists are equal, False otherwise.\n        \"\"\"\n        match other:\n            case list():\n                return list(self) == other\n            case _:\n                pass\n        match self, other:\n            case Cons(sh, st), Cons(oh, ot):\n                return sh == oh and st == ot\n            case Nil(), Nil():\n                return True\n            case _:\n                return False\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.append","title":"<code>append(other)</code>  <code>abstractmethod</code>","text":"<p>Append another list to the end of this list.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>CList</code> <p>The list to append.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the elements of <code>other</code> appended to this list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef append(self, other: CList) -&gt; CList:\n    \"\"\"Append another list to the end of this list.\n\n    Args:\n        other: The list to append.\n\n    Returns:\n        A new list with the elements of `other` appended to this list.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.fold_right","title":"<code>fold_right(acc, f)</code>  <code>abstractmethod</code>","text":"<p>Fold the list from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[A, B], B]</code> <p>A function to apply, taking an element and the current accumulator.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The result of folding the list from right to left.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n    \"\"\"Fold the list from right to left.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking an element and the current accumulator.\n\n    Returns:\n        The result of folding the list from right to left.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.fold_left","title":"<code>fold_left(acc, f)</code>  <code>abstractmethod</code>","text":"<p>Fold the list from left to right.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[B, A], B]</code> <p>A function to apply, taking the current accumulator and an element.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The result of folding the list from left to right.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n    \"\"\"Fold the list from left to right.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking the current accumulator and an element.\n\n    Returns:\n        The result of folding the list from left to right.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.drop","title":"<code>drop(n)</code>  <code>abstractmethod</code>","text":"<p>Drop the first <code>n</code> elements from the list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to drop.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the first <code>n</code> elements removed.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef drop(self, n: int) -&gt; CList:\n    \"\"\"Drop the first `n` elements from the list.\n\n    Args:\n        n: The number of elements to drop.\n\n    Returns:\n        A new list with the first `n` elements removed.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.drop_while","title":"<code>drop_while(f)</code>  <code>abstractmethod</code>","text":"<p>Drop elements from the list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements removed while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Drop elements from the list as long as the predicate function `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements removed while `f` is true.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.take","title":"<code>take(n)</code>  <code>abstractmethod</code>","text":"<p>Take the first <code>n</code> elements from the list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to take.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the first <code>n</code> elements.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef take(self, n: int) -&gt; CList:\n    \"\"\"Take the first `n` elements from the list.\n\n    Args:\n        n: The number of elements to take.\n\n    Returns:\n        A new list containing the first `n` elements.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.take_while","title":"<code>take_while(f)</code>  <code>abstractmethod</code>","text":"<p>Take elements from the list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements taken while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef take_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Take elements from the list as long as the predicate function `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements taken while `f` is true.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.split_at","title":"<code>split_at(i)</code>  <code>abstractmethod</code>","text":"<p>Split the list into two lists at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A tuple of two lists: the first containing elements up to <code>i</code>,</p> <code>CList</code> <p>and the second containing the rest.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef split_at(self, i: int) -&gt; tuple[CList, CList]:\n    \"\"\"Split the list into two lists at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        A tuple of two lists: the first containing elements up to `i`,\n        and the second containing the rest.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.insert_at","title":"<code>insert_at(i, value)</code>  <code>abstractmethod</code>","text":"<p>Insert an element at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The new list</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@abstractmethod\ndef insert_at(self, i: int, value: A) -&gt; CList:\n    \"\"\"Insert an element at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        The new list\n    \"\"\"\n    ...\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.partition","title":"<code>partition(f)</code>","text":"<p>Partition the list into two lists based on a predicate function.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A tuple of two lists: the first containing elements that satisfy <code>f</code>,</p> <code>CList</code> <p>and the second containing the rest.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def partition(self, f: Callable[[A], bool]) -&gt; tuple[CList, CList]:\n    \"\"\"Partition the list into two lists based on a predicate function.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A tuple of two lists: the first containing elements that satisfy `f`,\n        and the second containing the rest.\n    \"\"\"\n    accum = lambda a, x: (a &lt;&lt; x[0], x[1]) if f(a) else (x[0], a &lt;&lt; x[1])\n    return self.fold_right((Nil(), Nil()), accum)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.length","title":"<code>length()</code>","text":"<p>Compute the length of the list.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of elements in the list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def length(self) -&gt; int:\n    \"\"\"Compute the length of the list.\n\n    Returns:\n        The number of elements in the list.\n    \"\"\"\n    return self.fold_right(0, lambda _, acc: acc + 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.prepend","title":"<code>prepend(new_head)</code>","text":"<p>Prepend an element to the list.</p> <p>Parameters:</p> Name Type Description Default <code>new_head</code> <code>A</code> <p>The element to prepend.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with <code>new_head</code> added to the beginning.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def prepend(self, new_head: A) -&gt; CList:\n    \"\"\"Prepend an element to the list.\n\n    Args:\n        new_head: The element to prepend.\n\n    Returns:\n        A new list with `new_head` added to the beginning.\n    \"\"\"\n    return Cons(new_head, self)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.reversed","title":"<code>reversed()</code>","text":"<p>Reverse the order of the elements in the list.</p> <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the elements in reversed order.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def reversed(self) -&gt; CList:\n    \"\"\"Reverse the order of the elements in the list.\n\n    Returns:\n        A new list with the elements in reversed order.\n    \"\"\"\n    return self.fold_left(Nil(), lambda acc, h: Cons(h, acc))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.map","title":"<code>map(f)</code>","text":"<p>Apply a function to each element of the list, producing a new list with the results.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable</code> <p>A function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the results of applying <code>f</code> to each element.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def map(self, f: Callable) -&gt; CList:\n    \"\"\"Apply a function to each element of the list, producing a new list\n    with the results.\n\n    Args:\n        f: A function to apply to each element.\n\n    Returns:\n        A new list with the results of applying `f` to each element.\n    \"\"\"\n    return self.fold_right(Nil(), lambda a, acc: Cons(f(a), acc))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.ap","title":"<code>ap(other)</code>","text":"<p>Cartesian product \u2014 pair each element of self with each element of other.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def ap(self, other) -&gt; CList:\n    \"\"\"Cartesian product \u2014 pair each element of self with each element of other.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.filter","title":"<code>filter(f)</code>","text":"<p>Filter the elements of the list based on a predicate function.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing only the elements that satisfy <code>f</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def filter(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Filter the elements of the list based on a predicate function.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list containing only the elements that satisfy `f`.\n    \"\"\"\n    return self.fold_right(Nil(), lambda a, acc: Cons(a, acc) if f(a) else acc)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.flatten","title":"<code>flatten()</code>","text":"<p>Flatten a list of lists into a single list.</p> <p>Returns:</p> Type Description <code>CList</code> <p>A new list with all nested lists flattened into a single list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def flatten(self) -&gt; CList:\n    \"\"\"Flatten a list of lists into a single list.\n\n    Returns:\n        A new list with all nested lists flattened into a single list.\n    \"\"\"\n    return CList.flatten_(self)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.bind","title":"<code>bind(f)</code>","text":"<p>Apply a function to each element and flatten the results.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], CList]</code> <p>A function that returns a list for each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the results of applying <code>f</code> to each element,</p> <code>CList</code> <p>flattened into a single list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def bind(self, f: Callable[[A], CList]) -&gt; CList:\n    \"\"\"Apply a function to each element and flatten the results.\n\n    Args:\n        f: A function that returns a list for each element.\n\n    Returns:\n        A new list with the results of applying `f` to each element,\n        flattened into a single list.\n    \"\"\"\n    return self.map(f).flatten()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.sorted","title":"<code>sorted(cmp)</code>","text":"<p>Sort the list using a comparison function.</p> <p>Parameters:</p> Name Type Description Default <code>cmp</code> <code>Callable[[A, A], int]</code> <p>A comparison function to use for sorting.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the elements sorted according to <code>cmp</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def sorted(self, cmp: Callable[[A, A], int]) -&gt; CList:\n    \"\"\"Sort the list using a comparison function.\n\n    Args:\n        cmp: A comparison function to use for sorting.\n\n    Returns:\n        A new list with the elements sorted according to `cmp`.\n    \"\"\"\n\n    def merge(left: CList, right: CList) -&gt; CList:\n        match left, right:\n            case Nil(), r:\n                return r\n            case l, Nil():\n                return l\n            case Cons(lh, lt), Cons(rh, rt):\n                if cmp(lh, rh) &lt;= 0:\n                    return lh &lt;&lt; merge(lt, right)\n                return rh &lt;&lt; merge(left, rt)\n            case _:\n                return Nil()\n\n    length = len(self)\n    if length &lt;= 1:\n        return self\n    left, right = self.split_at(length // 2)\n    return merge(left.sorted(cmp), right.sorted(cmp))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.flatten_","title":"<code>flatten_(lst)</code>  <code>staticmethod</code>","text":"<p>Flatten a nested list of lists into a single list.</p> <p>Parameters:</p> Name Type Description Default <code>lst</code> <code>CList[CList[A]]</code> <p>A list of lists to be flattened.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with all nested lists flattened into a single list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef flatten_(lst: CList[CList[A]]) -&gt; CList:\n    \"\"\"Flatten a nested list of lists into a single list.\n\n    Args:\n        lst: A list of lists to be flattened.\n\n    Returns:\n        A new list with all nested lists flattened into a single list.\n    \"\"\"\n\n    def concat(left, right):\n        match left:\n            case Nil():\n                return right\n            case Cons(h, t):\n                return Cons(h, concat(t, right))\n            case _:\n                return Nil()\n\n    def flatten(lst: CList[CList[A]]) -&gt; CList[A]:\n        match lst:\n            case Nil():\n                return Nil()\n            case Cons(h, t):\n                match h:\n                    case Cons(_, _):\n                        return concat(flatten(h), flatten(t))\n                    case _:\n                        return Cons(h, flatten(t))\n            case _:\n                return Nil()\n\n    return flatten(lst)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation for CList. Collects all yielded results via flatMap.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; CList:\n    \"\"\"Do-notation for CList. Collects all yielded results via flatMap.\"\"\"\n\n    def _collect():\n        gen = gen_fn()\n        try:\n            first = next(gen)\n            result = first.bind(lambda v: _send(gen, v))\n            return result\n        except StopIteration as e:\n            return Cons.pure(e.value)\n\n    def _send(gen, value):\n        try:\n            next_val = gen.send(value)\n            return next_val.bind(lambda v: _send(gen, v))\n        except StopIteration as e:\n            return Cons.pure(e.value)\n\n    return _collect()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a value into a single-element list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@classmethod\ndef pure(cls, value) -&gt; CList:\n    \"\"\"Lift a value into a single-element list.\"\"\"\n    return Cons(value)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.cons","title":"<code>cons(a)</code>  <code>staticmethod</code>","text":"<p>Create a new list with a single element.</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>A</code> <p>The element to add to the list.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the single element <code>a</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef cons(a: A) -&gt; CList:\n    \"\"\"Create a new list with a single element.\n\n    Args:\n        a: The element to add to the list.\n\n    Returns:\n        A new list containing the single element `a`.\n    \"\"\"\n    return Cons(a)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.empty","title":"<code>empty()</code>  <code>staticmethod</code>","text":"<p>Create an empty list.</p> <p>Returns:</p> Type Description <code>CList</code> <p>An empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef empty() -&gt; CList:\n    \"\"\"Create an empty list.\n\n    Returns:\n        An empty list.\n    \"\"\"\n    return Nil()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.new","title":"<code>new(*xs)</code>  <code>staticmethod</code>","text":"<p>Create a new list from the given elements.</p> <p>Parameters:</p> Name Type Description Default <code>*xs</code> <code>A</code> <p>The elements to add to the list.</p> <code>()</code> <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the elements <code>xs</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef new(*xs: A) -&gt; CList:\n    \"\"\"Create a new list from the given elements.\n\n    Args:\n        *xs: The elements to add to the list.\n\n    Returns:\n        A new list containing the elements `xs`.\n    \"\"\"\n    from funstruct.util.tailrec import tail_call, tco\n\n    @tco\n    def _go(items, idx, acc):\n        if idx &lt; 0:\n            return acc\n        return tail_call(_go)(items, idx - 1, Cons(items[idx], acc))\n\n    return _go(xs, len(xs) - 1, Nil())\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.from_iterable","title":"<code>from_iterable(iterable)</code>  <code>staticmethod</code>","text":"<p>Create a new list from an iterable of elements.</p> <p>CList.from_iterable([1, 2, 3]).to_list() [1, 2, 3] CList.from_iterable([]).to_list() []</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@staticmethod\ndef from_iterable(iterable: Iterable[A]) -&gt; CList:\n    \"\"\"Create a new list from an iterable of elements.\n\n    &gt;&gt;&gt; CList.from_iterable([1, 2, 3]).to_list()\n    [1, 2, 3]\n    &gt;&gt;&gt; CList.from_iterable([]).to_list()\n    []\n    \"\"\"\n    return CList.new(*iterable)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__rlshift__","title":"<code>__rlshift__(other)</code>","text":"<p>Prepend an element to the list using the <code>&lt;&lt;</code> operator. Ex: 1 &lt;&lt; Nil() == Cons(1)</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>A</code> <p>The element to prepend.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with <code>other</code> added to the beginning.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __rlshift__(self, other: A) -&gt; CList:\n    \"\"\"Prepend an element to the list using the `&lt;&lt;` operator.\n    Ex:\n    1 &lt;&lt; Nil() == Cons(1)\n\n    Args:\n        other: The element to prepend.\n\n    Returns:\n        A new list with `other` added to the beginning.\n    \"\"\"\n    return self.prepend(other)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__add__","title":"<code>__add__(other)</code>","text":"<p>Concatenate two lists (Monoid append, not Applicative ap).</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __add__(self, other: CList) -&gt; CList:\n    \"\"\"Concatenate two lists (Monoid append, not Applicative ap).\"\"\"\n    return self.append(other)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__len__","title":"<code>__len__()</code>","text":"<p>Compute the length of the list.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of elements in the list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Compute the length of the list.\n\n    Returns:\n        The number of elements in the list.\n    \"\"\"\n    return self.fold_right(0, lambda _, acc: acc + 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.to_list","title":"<code>to_list()</code>","text":"<p>Convert to a Python list.</p> <p>Cons(1, Cons(2, Cons(3, Nil()))).to_list() [1, 2, 3] Nil().to_list() []</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def to_list(self) -&gt; list[A]:\n    \"\"\"Convert to a Python list.\n\n    &gt;&gt;&gt; Cons(1, Cons(2, Cons(3, Nil()))).to_list()\n    [1, 2, 3]\n    &gt;&gt;&gt; Nil().to_list()\n    []\n    \"\"\"\n    return list(self)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.CList.__eq__","title":"<code>__eq__(other)</code>","text":"<p>Check if this list is equal to another list.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>The list to compare with.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the lists are equal, False otherwise.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"Check if this list is equal to another list.\n\n    Args:\n        other: The list to compare with.\n\n    Returns:\n        True if the lists are equal, False otherwise.\n    \"\"\"\n    match other:\n        case list():\n            return list(self) == other\n        case _:\n            pass\n    match self, other:\n        case Cons(sh, st), Cons(oh, ot):\n            return sh == oh and st == ot\n        case Nil(), Nil():\n            return True\n        case _:\n            return False\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons","title":"<code>funstruct.collections.cons.Cons</code>  <code>dataclass</code>","text":"<p>               Bases: <code>CList[A]</code></p> <p>Represents a non-empty list with a head element and a tail list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Cons(CList[A]):\n    \"\"\"Represents a non-empty list with a head element and a tail list.\"\"\"\n\n    head: A\n    tail: CList[A] = field(default_factory=Nil)\n\n    def __bool__(self) -&gt; bool:\n        return True\n\n    def __repr__(self) -&gt; str:\n        return f\"Cons({repr(self.head)}, {repr(self.tail)})\"\n\n    def __str__(self) -&gt; str:\n        def _fmt(elem) -&gt; str:\n            match elem:\n                case CList():\n                    return f\"[{', '.join(_fmt(e) for e in elem)}]\"\n                case _:\n                    return str(elem)\n\n        return f\"CList([{', '.join(_fmt(e) for e in self)}])\"\n\n    def append(self, other: CList) -&gt; CList:\n        \"\"\"Append another list to the end of this non-empty list.\"\"\"\n        return self.reversed().fold_left(other, lambda acc, h: Cons(h, acc))\n\n    def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n        \"\"\"Fold from right to left. Stack-safe via reverse + fold_left.\"\"\"\n        return self.reversed().fold_left(acc, lambda a, b: f(b, a))\n\n    def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n        \"\"\"Fold from left to right. Tail-recursive via @tco.\"\"\"\n        from funstruct.util.tailrec import tail_call, tco\n\n        @tco\n        def _go(current, result):\n            match current:\n                case Nil():\n                    return result\n                case Cons(h, t):\n                    return tail_call(_go)(t, f(result, h))\n\n        return _go(self, acc)\n\n    def drop(self, n: int) -&gt; CList:\n        \"\"\"Drop the first `n` elements from the non-empty list.\n\n        Args:\n            n: The number of elements to drop.\n\n        Returns:\n            A new list with the first `n` elements removed.\n        \"\"\"\n        return self if n &lt;= 0 else self.tail.drop(n - 1)\n\n    def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Drop elements from the non-empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements removed while `f` is true.\n        \"\"\"\n        return self if not f(self.head) else self.tail.drop_while(f)\n\n    def take(self, n: int) -&gt; CList:\n        \"\"\"Take the first `n` elements from the non-empty list.\n\n        Args:\n            n: The number of elements to take.\n\n        Returns:\n            A new list containing the first `n` elements.\n        \"\"\"\n        return Cons(self.head) if n &lt;= 1 else self.head &lt;&lt; self.tail.take(n - 1)\n\n    def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Take elements from the non-empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            A new list with elements taken while `f` is true.\n        \"\"\"\n        return self.head &lt;&lt; self.tail.take_while(f) if f(self.head) else Nil()\n\n    def split_at(self, i: int) -&gt; tuple[CList, CList]:\n        \"\"\"Split the non-empty list into two lists at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            A tuple of two lists: the first containing elements up to `i`,\n            and the second containing the rest.\n        \"\"\"\n        return self.take(i), self.drop(i)\n\n    def insert_at(self, i: int, value: A) -&gt; CList:\n        \"\"\"Insert an element at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            The new list\n        \"\"\"\n        if i &lt;= 0:\n            return Cons(value, self)\n        return Cons(self.head, self.tail.insert_at(i - 1, value))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.append","title":"<code>append(other)</code>","text":"<p>Append another list to the end of this non-empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def append(self, other: CList) -&gt; CList:\n    \"\"\"Append another list to the end of this non-empty list.\"\"\"\n    return self.reversed().fold_left(other, lambda acc, h: Cons(h, acc))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.fold_right","title":"<code>fold_right(acc, f)</code>","text":"<p>Fold from right to left. Stack-safe via reverse + fold_left.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n    \"\"\"Fold from right to left. Stack-safe via reverse + fold_left.\"\"\"\n    return self.reversed().fold_left(acc, lambda a, b: f(b, a))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.fold_left","title":"<code>fold_left(acc, f)</code>","text":"<p>Fold from left to right. Tail-recursive via @tco.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n    \"\"\"Fold from left to right. Tail-recursive via @tco.\"\"\"\n    from funstruct.util.tailrec import tail_call, tco\n\n    @tco\n    def _go(current, result):\n        match current:\n            case Nil():\n                return result\n            case Cons(h, t):\n                return tail_call(_go)(t, f(result, h))\n\n    return _go(self, acc)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.drop","title":"<code>drop(n)</code>","text":"<p>Drop the first <code>n</code> elements from the non-empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to drop.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with the first <code>n</code> elements removed.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop(self, n: int) -&gt; CList:\n    \"\"\"Drop the first `n` elements from the non-empty list.\n\n    Args:\n        n: The number of elements to drop.\n\n    Returns:\n        A new list with the first `n` elements removed.\n    \"\"\"\n    return self if n &lt;= 0 else self.tail.drop(n - 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.drop_while","title":"<code>drop_while(f)</code>","text":"<p>Drop elements from the non-empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements removed while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Drop elements from the non-empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements removed while `f` is true.\n    \"\"\"\n    return self if not f(self.head) else self.tail.drop_while(f)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.take","title":"<code>take(n)</code>","text":"<p>Take the first <code>n</code> elements from the non-empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to take.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list containing the first <code>n</code> elements.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take(self, n: int) -&gt; CList:\n    \"\"\"Take the first `n` elements from the non-empty list.\n\n    Args:\n        n: The number of elements to take.\n\n    Returns:\n        A new list containing the first `n` elements.\n    \"\"\"\n    return Cons(self.head) if n &lt;= 1 else self.head &lt;&lt; self.tail.take(n - 1)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.take_while","title":"<code>take_while(f)</code>","text":"<p>Take elements from the non-empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A new list with elements taken while <code>f</code> is true.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Take elements from the non-empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        A new list with elements taken while `f` is true.\n    \"\"\"\n    return self.head &lt;&lt; self.tail.take_while(f) if f(self.head) else Nil()\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.split_at","title":"<code>split_at(i)</code>","text":"<p>Split the non-empty list into two lists at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>A tuple of two lists: the first containing elements up to <code>i</code>,</p> <code>CList</code> <p>and the second containing the rest.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def split_at(self, i: int) -&gt; tuple[CList, CList]:\n    \"\"\"Split the non-empty list into two lists at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        A tuple of two lists: the first containing elements up to `i`,\n        and the second containing the rest.\n    \"\"\"\n    return self.take(i), self.drop(i)\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Cons.insert_at","title":"<code>insert_at(i, value)</code>","text":"<p>Insert an element at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The new list</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def insert_at(self, i: int, value: A) -&gt; CList:\n    \"\"\"Insert an element at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        The new list\n    \"\"\"\n    if i &lt;= 0:\n        return Cons(value, self)\n    return Cons(self.head, self.tail.insert_at(i - 1, value))\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil","title":"<code>funstruct.collections.cons.Nil</code>","text":"<p>               Bases: <code>CList</code></p> <p>A singleton representing the empty list/end of a singly linked list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>class Nil(CList):\n    \"\"\"A singleton representing the empty list/end of a singly linked list.\"\"\"\n\n    _instance = None\n\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n\n    def __repr__(self) -&gt; str:\n        return \"Nil()\"\n\n    def __str__(self) -&gt; str:\n        return \"Nil\"\n\n    def __bool__(self) -&gt; bool:\n        return False\n\n    def append(self, other: CList) -&gt; CList:\n        \"\"\"Append another list to the empty list.\n\n        Args:\n            other: The list to append.\n\n        Returns:\n            The list `other`, since appending to an empty list results in `other`.\n        \"\"\"\n        return other\n\n    def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n        \"\"\"Fold the empty list from right to left.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking an element and the current accumulator.\n\n        Returns:\n            The accumulator value, since folding an empty list results\n            in the initial value.\n        \"\"\"\n        return acc\n\n    def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n        \"\"\"Fold the empty list from left to right.\n\n        Args:\n            acc: The initial accumulator value.\n            f: A function to apply, taking the current accumulator and an element.\n\n        Returns:\n            The accumulator value, since folding an empty list results\n            in the initial value.\n        \"\"\"\n        return acc\n\n    def drop(self, n: int) -&gt; CList:\n        \"\"\"Drop the first `n` elements from the empty list.\n\n        Args:\n            n: The number of elements to drop.\n\n        Returns:\n            The empty list, since dropping elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Drop elements from the empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            The empty list, since dropping elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def take(self, n: int) -&gt; CList:\n        \"\"\"Take the first `n` elements from the empty list.\n\n        Args:\n            n: The number of elements to take.\n\n        Returns:\n            The empty list, since taking elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n        \"\"\"Take elements from the empty list as long as the predicate function\n        `f` is true.\n\n        Args:\n            f: A predicate function to apply to each element.\n\n        Returns:\n            The empty list, since taking elements from an empty list results\n            in an empty list.\n        \"\"\"\n        return self\n\n    def split_at(self, i: int) -&gt; tuple[CList, CList]:\n        \"\"\"Split the empty list into two lists at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            A tuple of two empty lists.\n        \"\"\"\n        return self, self\n\n    def insert_at(self, i: int, value: A) -&gt; CList:\n        \"\"\"Insert an element at index `i`.\n\n        Args:\n            i: The index to split at.\n\n        Returns:\n            The new list\n        \"\"\"\n        return Cons(value, Nil())\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.append","title":"<code>append(other)</code>","text":"<p>Append another list to the empty list.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>CList</code> <p>The list to append.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The list <code>other</code>, since appending to an empty list results in <code>other</code>.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def append(self, other: CList) -&gt; CList:\n    \"\"\"Append another list to the empty list.\n\n    Args:\n        other: The list to append.\n\n    Returns:\n        The list `other`, since appending to an empty list results in `other`.\n    \"\"\"\n    return other\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.fold_right","title":"<code>fold_right(acc, f)</code>","text":"<p>Fold the empty list from right to left.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[A, B], B]</code> <p>A function to apply, taking an element and the current accumulator.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The accumulator value, since folding an empty list results</p> <code>B</code> <p>in the initial value.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_right(self, acc: B, f: Callable[[A, B], B]) -&gt; B:\n    \"\"\"Fold the empty list from right to left.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking an element and the current accumulator.\n\n    Returns:\n        The accumulator value, since folding an empty list results\n        in the initial value.\n    \"\"\"\n    return acc\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.fold_left","title":"<code>fold_left(acc, f)</code>","text":"<p>Fold the empty list from left to right.</p> <p>Parameters:</p> Name Type Description Default <code>acc</code> <code>B</code> <p>The initial accumulator value.</p> required <code>f</code> <code>Callable[[B, A], B]</code> <p>A function to apply, taking the current accumulator and an element.</p> required <p>Returns:</p> Type Description <code>B</code> <p>The accumulator value, since folding an empty list results</p> <code>B</code> <p>in the initial value.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def fold_left(self, acc: B, f: Callable[[B, A], B]) -&gt; B:\n    \"\"\"Fold the empty list from left to right.\n\n    Args:\n        acc: The initial accumulator value.\n        f: A function to apply, taking the current accumulator and an element.\n\n    Returns:\n        The accumulator value, since folding an empty list results\n        in the initial value.\n    \"\"\"\n    return acc\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.drop","title":"<code>drop(n)</code>","text":"<p>Drop the first <code>n</code> elements from the empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to drop.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since dropping elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop(self, n: int) -&gt; CList:\n    \"\"\"Drop the first `n` elements from the empty list.\n\n    Args:\n        n: The number of elements to drop.\n\n    Returns:\n        The empty list, since dropping elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.drop_while","title":"<code>drop_while(f)</code>","text":"<p>Drop elements from the empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since dropping elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def drop_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Drop elements from the empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        The empty list, since dropping elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.take","title":"<code>take(n)</code>","text":"<p>Take the first <code>n</code> elements from the empty list.</p> <p>Parameters:</p> Name Type Description Default <code>n</code> <code>int</code> <p>The number of elements to take.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since taking elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take(self, n: int) -&gt; CList:\n    \"\"\"Take the first `n` elements from the empty list.\n\n    Args:\n        n: The number of elements to take.\n\n    Returns:\n        The empty list, since taking elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.take_while","title":"<code>take_while(f)</code>","text":"<p>Take elements from the empty list as long as the predicate function <code>f</code> is true.</p> <p>Parameters:</p> Name Type Description Default <code>f</code> <code>Callable[[A], bool]</code> <p>A predicate function to apply to each element.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The empty list, since taking elements from an empty list results</p> <code>CList</code> <p>in an empty list.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def take_while(self, f: Callable[[A], bool]) -&gt; CList:\n    \"\"\"Take elements from the empty list as long as the predicate function\n    `f` is true.\n\n    Args:\n        f: A predicate function to apply to each element.\n\n    Returns:\n        The empty list, since taking elements from an empty list results\n        in an empty list.\n    \"\"\"\n    return self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.split_at","title":"<code>split_at(i)</code>","text":"<p>Split the empty list into two lists at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>tuple[CList, CList]</code> <p>A tuple of two empty lists.</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def split_at(self, i: int) -&gt; tuple[CList, CList]:\n    \"\"\"Split the empty list into two lists at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        A tuple of two empty lists.\n    \"\"\"\n    return self, self\n</code></pre>"},{"location":"collections/cons/#funstruct.collections.cons.Nil.insert_at","title":"<code>insert_at(i, value)</code>","text":"<p>Insert an element at index <code>i</code>.</p> <p>Parameters:</p> Name Type Description Default <code>i</code> <code>int</code> <p>The index to split at.</p> required <p>Returns:</p> Type Description <code>CList</code> <p>The new list</p> Source code in <code>funstruct/collections/cons.py</code> <pre><code>def insert_at(self, i: int, value: A) -&gt; CList:\n    \"\"\"Insert an element at index `i`.\n\n    Args:\n        i: The index to split at.\n\n    Returns:\n        The new list\n    \"\"\"\n    return Cons(value, Nil())\n</code></pre>"},{"location":"collections/frozendict/","title":"frozendict","text":""},{"location":"collections/frozendict/#funstruct.collections.frozendict","title":"<code>funstruct.collections.frozendict</code>","text":"<p>An immutable, hashable dictionary (HAMT-backed, NOT insertion-ordered).</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.collections.frozendict import frozendict\n&gt;&gt;&gt; fd = frozendict({\"a\": 1, \"b\": 2})\n&gt;&gt;&gt; fd.map(lambda x: x * 10).get(\"a\")\n10\n&gt;&gt;&gt; fd.map(lambda x: x * 10).get(\"b\")\n20\n&gt;&gt;&gt; (fd + frozendict({\"c\": 3})).get(\"c\")\n3\n&gt;&gt;&gt; fd.get(\"a\")\n1\n</code></pre>"},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict","title":"<code>frozendict</code>","text":"<p>               Bases: <code>Generic[K, V]</code></p> <p>An immutable, persistent dictionary backed by a HAMT.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>class frozendict(Generic[K, V]):\n    \"\"\"An immutable, persistent dictionary backed by a HAMT.\"\"\"\n\n    __slots__ = (\"__root\", \"__size\", \"__hash_cache\")\n\n    def __init__(self, *args, **kwargs) -&gt; None:\n        match args:\n            case (frozendict() as other, *_):\n                object.__setattr__(self, \"_frozendict__root\", other.__root)\n                object.__setattr__(self, \"_frozendict__size\", other.__size)\n            case _:\n                root = _EMPTY\n                size = 0\n                source = dict(*args, **kwargs)\n                for k, v in source.items():\n                    root = root.put(k, v, hash(k), 0)\n                    size += 1\n                object.__setattr__(self, \"_frozendict__root\", root)\n                object.__setattr__(self, \"_frozendict__size\", size)\n        object.__setattr__(self, \"_frozendict__hash_cache\", None)\n\n    def __setattr__(self, name, value):\n        raise AttributeError(\"frozendict is immutable\")\n\n    def __delattr__(self, name):\n        raise AttributeError(\"frozendict is immutable\")\n\n    def __getitem__(self, key: K) -&gt; V:\n        result = self.__root.get(key, hash(key), 0)\n        if result is None and not self.__contains__(key):\n            raise KeyError(key)\n        return result\n\n    def get(self, key: K) -&gt; V | None:\n        return self.__root.get(key, hash(key), 0)\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case frozendict():\n                if self.__size != other.__size:\n                    return False\n                return all(other.get(k) == v for k, v in self.items())\n            case dict():\n                if self.__size != len(other):\n                    return False\n                return all(other.get(k) == v for k, v in self.items())\n            case _:\n                return False\n\n    def __contains__(self, key) -&gt; bool:\n        for k, _ in self.__root.items_iter():\n            if k == key:\n                return True\n        return False\n\n    def __len__(self) -&gt; int:\n        return self.__size\n\n    def keys(self) -&gt; KeysView[K]:\n        return dict(self.items()).keys()\n\n    def values(self) -&gt; ValuesView[V]:\n        return dict(self.items()).values()\n\n    def items(self) -&gt; ItemsView[K, V]:\n        return dict(self.__root.items_iter()).items()\n\n    def __iter__(self) -&gt; Iterator[K]:\n        for k, _ in self.__root.items_iter():\n            yield k\n\n    def __repr__(self) -&gt; str:\n        return f\"frozendict({dict(self.__root.items_iter())})\"\n\n    def __str__(self) -&gt; str:\n        return self.__repr__()\n\n    def __hash__(self) -&gt; int:\n        if self.__hash_cache is None:\n            h = 0\n            for k, v in self.__root.items_iter():\n                h ^= hash((k, v))\n            object.__setattr__(self, \"_frozendict__hash_cache\", h)\n        return self.__hash_cache\n\n    def put(self, k: K, v: V) -&gt; frozendict:\n        new_root = self.__root.put(k, v, hash(k), 0)\n        new_fd = object.__new__(frozendict)\n        new_size = self.__size if k in self else self.__size + 1\n        object.__setattr__(new_fd, \"_frozendict__root\", new_root)\n        object.__setattr__(new_fd, \"_frozendict__size\", new_size)\n        object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n        return new_fd\n\n    def remove(self, k: K) -&gt; frozendict:\n        \"\"\"Remove a key, returning a new frozendict. No-op if key absent.\"\"\"\n        if k not in self:\n            return self\n        new_root = self.__root.remove(k, hash(k), 0)\n        new_fd = object.__new__(frozendict)\n        object.__setattr__(new_fd, \"_frozendict__root\", new_root)\n        object.__setattr__(new_fd, \"_frozendict__size\", self.__size - 1)\n        object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n        return new_fd\n\n    def combine(self, other: frozendict) -&gt; frozendict:\n        result = self\n        for k, v in other.__root.items_iter():\n            result = result.put(k, v)\n        return result\n\n    def __add__(self, other: frozendict) -&gt; frozendict:\n        \"\"\"Semigroup combine (merge). Right-biased on key conflicts.\"\"\"\n        return self.combine(other)\n\n    def map(self, f: Callable) -&gt; frozendict:\n        \"\"\"Apply f to every value, preserving keys.\"\"\"\n        root = _EMPTY\n        size = 0\n        for k, v in self.__root.items_iter():\n            root = root.put(k, f(v), hash(k), 0)\n            size += 1\n        new_fd = object.__new__(frozendict)\n        object.__setattr__(new_fd, \"_frozendict__root\", root)\n        object.__setattr__(new_fd, \"_frozendict__size\", size)\n        object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n        return new_fd\n\n    @property\n    def raw(self) -&gt; dict:\n        return dict(self.__root.items_iter())\n\n    @classmethod\n    def fromkeys(cls, *args, **kwargs) -&gt; frozendict:\n        return cls(dict.fromkeys(*args, **kwargs))\n\n    @staticmethod\n    def new() -&gt; frozendict:\n        return frozendict()\n\n    @staticmethod\n    def combine_dicts(fd1: frozendict, fd2: frozendict) -&gt; frozendict:\n        return fd1.combine(fd2)\n\n    def __bool__(self) -&gt; bool:\n        return self.__size &gt; 0\n</code></pre>"},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict.remove","title":"<code>remove(k)</code>","text":"<p>Remove a key, returning a new frozendict. No-op if key absent.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>def remove(self, k: K) -&gt; frozendict:\n    \"\"\"Remove a key, returning a new frozendict. No-op if key absent.\"\"\"\n    if k not in self:\n        return self\n    new_root = self.__root.remove(k, hash(k), 0)\n    new_fd = object.__new__(frozendict)\n    object.__setattr__(new_fd, \"_frozendict__root\", new_root)\n    object.__setattr__(new_fd, \"_frozendict__size\", self.__size - 1)\n    object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n    return new_fd\n</code></pre>"},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict.__add__","title":"<code>__add__(other)</code>","text":"<p>Semigroup combine (merge). Right-biased on key conflicts.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>def __add__(self, other: frozendict) -&gt; frozendict:\n    \"\"\"Semigroup combine (merge). Right-biased on key conflicts.\"\"\"\n    return self.combine(other)\n</code></pre>"},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict.map","title":"<code>map(f)</code>","text":"<p>Apply f to every value, preserving keys.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>def map(self, f: Callable) -&gt; frozendict:\n    \"\"\"Apply f to every value, preserving keys.\"\"\"\n    root = _EMPTY\n    size = 0\n    for k, v in self.__root.items_iter():\n        root = root.put(k, f(v), hash(k), 0)\n        size += 1\n    new_fd = object.__new__(frozendict)\n    object.__setattr__(new_fd, \"_frozendict__root\", root)\n    object.__setattr__(new_fd, \"_frozendict__size\", size)\n    object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n    return new_fd\n</code></pre>"},{"location":"collections/frozendict/#api-reference","title":"API Reference","text":""},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict","title":"<code>funstruct.collections.frozendict.frozendict</code>","text":"<p>               Bases: <code>Generic[K, V]</code></p> <p>An immutable, persistent dictionary backed by a HAMT.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>class frozendict(Generic[K, V]):\n    \"\"\"An immutable, persistent dictionary backed by a HAMT.\"\"\"\n\n    __slots__ = (\"__root\", \"__size\", \"__hash_cache\")\n\n    def __init__(self, *args, **kwargs) -&gt; None:\n        match args:\n            case (frozendict() as other, *_):\n                object.__setattr__(self, \"_frozendict__root\", other.__root)\n                object.__setattr__(self, \"_frozendict__size\", other.__size)\n            case _:\n                root = _EMPTY\n                size = 0\n                source = dict(*args, **kwargs)\n                for k, v in source.items():\n                    root = root.put(k, v, hash(k), 0)\n                    size += 1\n                object.__setattr__(self, \"_frozendict__root\", root)\n                object.__setattr__(self, \"_frozendict__size\", size)\n        object.__setattr__(self, \"_frozendict__hash_cache\", None)\n\n    def __setattr__(self, name, value):\n        raise AttributeError(\"frozendict is immutable\")\n\n    def __delattr__(self, name):\n        raise AttributeError(\"frozendict is immutable\")\n\n    def __getitem__(self, key: K) -&gt; V:\n        result = self.__root.get(key, hash(key), 0)\n        if result is None and not self.__contains__(key):\n            raise KeyError(key)\n        return result\n\n    def get(self, key: K) -&gt; V | None:\n        return self.__root.get(key, hash(key), 0)\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case frozendict():\n                if self.__size != other.__size:\n                    return False\n                return all(other.get(k) == v for k, v in self.items())\n            case dict():\n                if self.__size != len(other):\n                    return False\n                return all(other.get(k) == v for k, v in self.items())\n            case _:\n                return False\n\n    def __contains__(self, key) -&gt; bool:\n        for k, _ in self.__root.items_iter():\n            if k == key:\n                return True\n        return False\n\n    def __len__(self) -&gt; int:\n        return self.__size\n\n    def keys(self) -&gt; KeysView[K]:\n        return dict(self.items()).keys()\n\n    def values(self) -&gt; ValuesView[V]:\n        return dict(self.items()).values()\n\n    def items(self) -&gt; ItemsView[K, V]:\n        return dict(self.__root.items_iter()).items()\n\n    def __iter__(self) -&gt; Iterator[K]:\n        for k, _ in self.__root.items_iter():\n            yield k\n\n    def __repr__(self) -&gt; str:\n        return f\"frozendict({dict(self.__root.items_iter())})\"\n\n    def __str__(self) -&gt; str:\n        return self.__repr__()\n\n    def __hash__(self) -&gt; int:\n        if self.__hash_cache is None:\n            h = 0\n            for k, v in self.__root.items_iter():\n                h ^= hash((k, v))\n            object.__setattr__(self, \"_frozendict__hash_cache\", h)\n        return self.__hash_cache\n\n    def put(self, k: K, v: V) -&gt; frozendict:\n        new_root = self.__root.put(k, v, hash(k), 0)\n        new_fd = object.__new__(frozendict)\n        new_size = self.__size if k in self else self.__size + 1\n        object.__setattr__(new_fd, \"_frozendict__root\", new_root)\n        object.__setattr__(new_fd, \"_frozendict__size\", new_size)\n        object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n        return new_fd\n\n    def remove(self, k: K) -&gt; frozendict:\n        \"\"\"Remove a key, returning a new frozendict. No-op if key absent.\"\"\"\n        if k not in self:\n            return self\n        new_root = self.__root.remove(k, hash(k), 0)\n        new_fd = object.__new__(frozendict)\n        object.__setattr__(new_fd, \"_frozendict__root\", new_root)\n        object.__setattr__(new_fd, \"_frozendict__size\", self.__size - 1)\n        object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n        return new_fd\n\n    def combine(self, other: frozendict) -&gt; frozendict:\n        result = self\n        for k, v in other.__root.items_iter():\n            result = result.put(k, v)\n        return result\n\n    def __add__(self, other: frozendict) -&gt; frozendict:\n        \"\"\"Semigroup combine (merge). Right-biased on key conflicts.\"\"\"\n        return self.combine(other)\n\n    def map(self, f: Callable) -&gt; frozendict:\n        \"\"\"Apply f to every value, preserving keys.\"\"\"\n        root = _EMPTY\n        size = 0\n        for k, v in self.__root.items_iter():\n            root = root.put(k, f(v), hash(k), 0)\n            size += 1\n        new_fd = object.__new__(frozendict)\n        object.__setattr__(new_fd, \"_frozendict__root\", root)\n        object.__setattr__(new_fd, \"_frozendict__size\", size)\n        object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n        return new_fd\n\n    @property\n    def raw(self) -&gt; dict:\n        return dict(self.__root.items_iter())\n\n    @classmethod\n    def fromkeys(cls, *args, **kwargs) -&gt; frozendict:\n        return cls(dict.fromkeys(*args, **kwargs))\n\n    @staticmethod\n    def new() -&gt; frozendict:\n        return frozendict()\n\n    @staticmethod\n    def combine_dicts(fd1: frozendict, fd2: frozendict) -&gt; frozendict:\n        return fd1.combine(fd2)\n\n    def __bool__(self) -&gt; bool:\n        return self.__size &gt; 0\n</code></pre>"},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict.remove","title":"<code>remove(k)</code>","text":"<p>Remove a key, returning a new frozendict. No-op if key absent.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>def remove(self, k: K) -&gt; frozendict:\n    \"\"\"Remove a key, returning a new frozendict. No-op if key absent.\"\"\"\n    if k not in self:\n        return self\n    new_root = self.__root.remove(k, hash(k), 0)\n    new_fd = object.__new__(frozendict)\n    object.__setattr__(new_fd, \"_frozendict__root\", new_root)\n    object.__setattr__(new_fd, \"_frozendict__size\", self.__size - 1)\n    object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n    return new_fd\n</code></pre>"},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict.__add__","title":"<code>__add__(other)</code>","text":"<p>Semigroup combine (merge). Right-biased on key conflicts.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>def __add__(self, other: frozendict) -&gt; frozendict:\n    \"\"\"Semigroup combine (merge). Right-biased on key conflicts.\"\"\"\n    return self.combine(other)\n</code></pre>"},{"location":"collections/frozendict/#funstruct.collections.frozendict.frozendict.map","title":"<code>map(f)</code>","text":"<p>Apply f to every value, preserving keys.</p> Source code in <code>funstruct/collections/frozendict.py</code> <pre><code>def map(self, f: Callable) -&gt; frozendict:\n    \"\"\"Apply f to every value, preserving keys.\"\"\"\n    root = _EMPTY\n    size = 0\n    for k, v in self.__root.items_iter():\n        root = root.put(k, f(v), hash(k), 0)\n        size += 1\n    new_fd = object.__new__(frozendict)\n    object.__setattr__(new_fd, \"_frozendict__root\", root)\n    object.__setattr__(new_fd, \"_frozendict__size\", size)\n    object.__setattr__(new_fd, \"_frozendict__hash_cache\", None)\n    return new_fd\n</code></pre>"},{"location":"collections/tree/","title":"Tree","text":""},{"location":"collections/tree/#funstruct.collections.tree","title":"<code>funstruct.collections.tree</code>","text":"<p>Immutable binary tree \u2014 a Functor.</p> <p>Tree[A] = Leaf(value) | Branch(value, left, right)</p> <p>Every node holds a value. map applies a function to all values, preserving structure. Tree is a Functor but not a Monad.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.collections.tree import Tree, Leaf, Branch\n&gt;&gt;&gt; from funstruct.collections.cons import Cons, Nil\n&gt;&gt;&gt; t = Branch(1, Leaf(2), Leaf(3))\n&gt;&gt;&gt; t.map(lambda x: x * 10)\nBranch(10, Leaf(20), Leaf(30))\n</code></pre> <pre><code>&gt;&gt;&gt; t.size\n3\n&gt;&gt;&gt; t.depth\n1\n</code></pre> <pre><code>&gt;&gt;&gt; big = Branch(1, Branch(2, Leaf(3), Leaf(4)), Leaf(5))\n&gt;&gt;&gt; big.map(str)\nBranch('1', Branch('2', Leaf('3'), Leaf('4')), Leaf('5'))\n&gt;&gt;&gt; big.to_list()\nCons(3, Cons(2, Cons(4, Cons(1, Cons(5, Nil())))))\n&gt;&gt;&gt; big.depth\n2\n</code></pre> <p>fold \u2014 reduce the tree:</p> <pre><code>&gt;&gt;&gt; t.fold(lambda v: v, lambda v, l, r: v + l + r)\n6\n</code></pre>"},{"location":"collections/tree/#funstruct.collections.tree.Tree","title":"<code>Tree</code>","text":"<p>               Bases: <code>Functor</code>, <code>Generic[A]</code></p> <p>Binary tree where every node holds a value.</p> <p>A Functor but NOT a Monad \u2014 map preserves structure, but there's no meaningful bind (no way to \"flatten\" a tree of trees without choosing a grafting strategy).</p> Source code in <code>funstruct/collections/tree.py</code> <pre><code>class Tree(Functor, Generic[A]):\n    \"\"\"Binary tree where every node holds a value.\n\n    A Functor but NOT a Monad \u2014 map preserves structure,\n    but there's no meaningful bind (no way to \"flatten\" a tree of trees\n    without choosing a grafting strategy).\n    \"\"\"\n\n    @abstractmethod\n    def map(self, f: Callable[[A], B]) -&gt; Tree[B]: ...\n\n    @property\n    @abstractmethod\n    def size(self) -&gt; int: ...\n\n    @property\n    @abstractmethod\n    def depth(self) -&gt; int: ...\n\n    @abstractmethod\n    def fold(self, on_leaf: Callable, on_branch: Callable): ...\n\n    @abstractmethod\n    def to_list(self) -&gt; CList[A]: ...\n</code></pre>"},{"location":"collections/tree/#funstruct.collections.tree.Leaf","title":"<code>Leaf</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Tree[A]</code></p> <p>Terminal node holding a single value.</p> Source code in <code>funstruct/collections/tree.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Leaf(Tree[A]):\n    \"\"\"Terminal node holding a single value.\"\"\"\n\n    value: A\n\n    def map(self, f: Callable[[A], B]) -&gt; Tree[B]:\n        return Leaf(f(self.value))\n\n    @property\n    def size(self) -&gt; int:\n        return 1\n\n    @property\n    def depth(self) -&gt; int:\n        return 0\n\n    def fold(self, on_leaf: Callable, on_branch: Callable):\n        return on_leaf(self.value)\n\n    def to_list(self) -&gt; CList[A]:\n        return Cons.pure(self.value)\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case Leaf(v):\n                return self.value == v\n            case _:\n                return False\n\n    def __repr__(self) -&gt; str:\n        return f\"Leaf({repr(self.value)})\"\n</code></pre>"},{"location":"collections/tree/#funstruct.collections.tree.Branch","title":"<code>Branch</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Tree[A]</code></p> <p>Internal node with a value and two children.</p> Source code in <code>funstruct/collections/tree.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Branch(Tree[A]):\n    \"\"\"Internal node with a value and two children.\"\"\"\n\n    value: A\n    left: Tree[A]\n    right: Tree[A]\n\n    def map(self, f: Callable[[A], B]) -&gt; Tree[B]:\n        return Branch(f(self.value), self.left.map(f), self.right.map(f))\n\n    @property\n    def size(self) -&gt; int:\n        return 1 + self.left.size + self.right.size\n\n    @property\n    def depth(self) -&gt; int:\n        return 1 + max(self.left.depth, self.right.depth)\n\n    def fold(self, on_leaf: Callable, on_branch: Callable):\n        return on_branch(\n            self.value,\n            self.left.fold(on_leaf, on_branch),\n            self.right.fold(on_leaf, on_branch),\n        )\n\n    def to_list(self) -&gt; CList[A]:\n        return self.left.to_list() + Cons(self.value, self.right.to_list())\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case Branch(v, l, r):\n                return self.value == v and self.left == l and self.right == r\n            case _:\n                return False\n\n    def __repr__(self) -&gt; str:\n        return f\"Branch({repr(self.value)}, {repr(self.left)}, {repr(self.right)})\"\n</code></pre>"},{"location":"functor/","title":"Functor","text":""},{"location":"functor/#funstruct.typeclasses.Functor","title":"<code>funstruct.typeclasses.Functor</code>","text":"<p>               Bases: <code>ABC</code>, <code>Generic[_A]</code></p> <p>Transform the value inside a context.</p> Type parameter <p>_A: The value type inside the functor.</p> Source code in <code>funstruct/typeclasses/_functor.py</code> <pre><code>class Functor(ABC, Generic[_A]):\n    \"\"\"Transform the value inside a context.\n\n    Type parameter:\n        _A: The value type inside the functor.\n    \"\"\"\n\n    @abstractmethod\n    def map(self, f: Callable[[_A], _B]) -&gt; Functor[_B]: ...\n</code></pre>"},{"location":"monad/","title":"Monads","text":"<p>Monads model sequenced computations with context \u2014 failure, absence, environment, state, logging, or async effects. Chain with <code>.bind()</code>, transform with <code>.map()</code>.</p> <ul> <li>Either \u2014 success or failure with a value</li> <li>Option \u2014 presence or absence</li> <li>Result \u2014 Either with Exception fixed as the error type</li> <li>Reader \u2014 shared read-only environment</li> <li>Writer \u2014 computation with accumulated output</li> <li>State \u2014 threaded mutable state</li> <li>Future \u2014 lazy async computation</li> </ul>"},{"location":"monad/#funstruct.monad","title":"<code>funstruct.monad</code>","text":"<p>Monad types.</p> Import from submodules <p>from funstruct.monad.either import Either, Right, Left from funstruct.monad.option import Option, Some, Nothing from funstruct.monad.result import Result, Ok, Err, Try, AsyncResult, TryAsync from funstruct.monad.future import Future from funstruct.monad.state import State from funstruct.monad.reader import Reader from funstruct.monad.writer import ListWriter, CListWriter, StrWriter</p>"},{"location":"monad/either/","title":"Either","text":""},{"location":"monad/either/#funstruct.monad.either","title":"<code>funstruct.monad.either</code>","text":"<p>Either monad \u2014 typed error handling.</p> <p>Either[E, A] = Right(a) | Left(e). Right-biased.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monad.either import Either, Right, Left\n&gt;&gt;&gt; Right(10).map(lambda x: x + 1)\nRight(11)\n&gt;&gt;&gt; Left(\"err\").map(lambda x: x + 1)\nLeft('err')\n&gt;&gt;&gt; Right(10).bind(lambda x: Right(x * 2))\nRight(20)\n&gt;&gt;&gt; Left(\"err\").bind(lambda x: Right(x * 2))\nLeft('err')\n</code></pre> <p>or_else \u2014 recover from Left:</p> <pre><code>&gt;&gt;&gt; Left(\"err\").or_else(lambda e: Right(\"default\"))\nRight('default')\n&gt;&gt;&gt; Right(10).or_else(lambda e: Right(\"default\"))\nRight(10)\n</code></pre> <p>do-notation:</p> <pre><code>&gt;&gt;&gt; def pipeline():\n...     x = yield Right(1)\n...     y = yield Right(x + 10)\n...     return x + y\n&gt;&gt;&gt; Either.do(pipeline)\nRight(12)\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either","title":"<code>Either</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[E, A]</code></p> <p>Either[E, A]: Right(value) or Left(error).</p> <p>Right-biased monad. bind/map/&gt;&gt; operate on the Right value and short-circuit on Left.</p> <p>No bool: Either deliberately has no truthiness. Both Right and Left carry a value \u2014 neither case is \"empty\" or \"absent.\" Use .is_right / .is_left or pattern matching instead of <code>if my_either:</code>.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>class Either(Monad, Generic[E, A]):\n    \"\"\"Either[E, A]: Right(value) or Left(error).\n\n    Right-biased monad. bind/map/&gt;&gt; operate on the Right value\n    and short-circuit on Left.\n\n    No __bool__: Either deliberately has no truthiness. Both Right and Left\n    carry a value \u2014 neither case is \"empty\" or \"absent.\" Use .is_right /\n    .is_left or pattern matching instead of `if my_either:`.\n    \"\"\"\n\n    @classmethod\n    def pure(cls, value: A) -&gt; Either[E, A]:\n        return Right(value)\n\n    @classmethod\n    def from_error(cls, error: E) -&gt; Either[E, A]:\n        \"\"\"Lift an error into Left.\"\"\"\n        return Left(error)\n\n    @classmethod\n    def do(cls, gen_fn: Callable) -&gt; Either:\n        \"\"\"Do-notation. Short-circuits on Left.\n\n        &gt;&gt;&gt; Right(1).bind(lambda x: Right(x + 10))\n        Right(11)\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield Right(1)\n        ...     y = yield Right(x + 10)\n        ...     return x + y\n        &gt;&gt;&gt; Either.do(pipeline)\n        Right(12)\n        \"\"\"\n        gen = gen_fn()\n        try:\n            monadic_val = next(gen)\n            while True:\n                match monadic_val:\n                    case Left():\n                        return monadic_val\n                    case Right(value):\n                        monadic_val = gen.send(value)\n        except StopIteration as e:\n            return Right(e.value)\n\n    @classmethod\n    def sequence(cls, eithers: CList[Either[E, A]]) -&gt; Either[E, CList[A]]:\n        \"\"\"CList[Either[E, A]] -&gt; Either[E, CList[A]].\n\n        Returns Right(clist) if all are Right, first Left otherwise.\n        \"\"\"\n        from funstruct.collections.cons import Cons, Nil\n        from funstruct.util.tailrec import tail_call, tco\n\n        @tco\n        def _go(remaining, acc):\n            match remaining:\n                case Nil():\n                    return Right(acc.reversed())\n                case Cons(head, tail):\n                    match head:\n                        case Left():\n                            return head\n                        case Right(v):\n                            return tail_call(_go)(tail, Cons(v, acc))\n\n        return _go(eithers, Nil())\n\n    @classmethod\n    def traverse(cls, values, f: Callable) -&gt; Either:\n        \"\"\"CList[A] -&gt; (A -&gt; Either[E, B]) -&gt; Either[E, CList[B]].\n\n        Applies f to each element, short-circuits on first Left.\n        \"\"\"\n        return cls.sequence(values.map(f))\n\n    def to_option(self):\n        \"\"\"Convert to Option \u2014 Right(v) \u2192 Some(v), Left(_) \u2192 Nothing.\n\n        &gt;&gt;&gt; Right(1).to_option()\n        Some(1)\n        &gt;&gt;&gt; Left(\"err\").to_option()\n        Nothing()\n        \"\"\"\n        from funstruct.monad.option import Nothing, Some\n\n        match self:\n            case Right(v):\n                return Some(v)\n            case _:\n                return Nothing()\n\n    @abstractmethod\n    def alt(self, f: Callable[[E], E]) -&gt; Either[E, A]:\n        \"\"\"Transform the error without recovering. No-op on Right.\"\"\"\n        ...\n\n    @abstractmethod\n    def or_else(self, f: Callable[[E], Either]) -&gt; Either:\n        \"\"\"Handle error \u2014 f can recover (Right) or re-fail (Left). No-op on Right.\"\"\"\n        ...\n\n    @abstractmethod\n    def get_or_else(self, default: A) -&gt; A:\n        \"\"\"Extract the value, or return default if Left.\"\"\"\n        ...\n\n    @abstractmethod\n    def fold(self, on_left: Callable, on_right: Callable):\n        \"\"\"Eliminate \u2014 apply on_left or on_right depending on the case.\"\"\"\n        ...\n\n    @abstractmethod\n    def swap(self) -&gt; Either[A, E]:\n        \"\"\"Swap Left and Right.\"\"\"\n        ...\n\n    @property\n    @abstractmethod\n    def is_right(self) -&gt; bool: ...\n\n    @property\n    def is_left(self) -&gt; bool:\n        return not self.is_right\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.from_error","title":"<code>from_error(error)</code>  <code>classmethod</code>","text":"<p>Lift an error into Left.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@classmethod\ndef from_error(cls, error: E) -&gt; Either[E, A]:\n    \"\"\"Lift an error into Left.\"\"\"\n    return Left(error)\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation. Short-circuits on Left.</p> <p>Right(1).bind(lambda x: Right(x + 10)) Right(11) def pipeline(): ...     x = yield Right(1) ...     y = yield Right(x + 10) ...     return x + y Either.do(pipeline) Right(12)</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn: Callable) -&gt; Either:\n    \"\"\"Do-notation. Short-circuits on Left.\n\n    &gt;&gt;&gt; Right(1).bind(lambda x: Right(x + 10))\n    Right(11)\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield Right(1)\n    ...     y = yield Right(x + 10)\n    ...     return x + y\n    &gt;&gt;&gt; Either.do(pipeline)\n    Right(12)\n    \"\"\"\n    gen = gen_fn()\n    try:\n        monadic_val = next(gen)\n        while True:\n            match monadic_val:\n                case Left():\n                    return monadic_val\n                case Right(value):\n                    monadic_val = gen.send(value)\n    except StopIteration as e:\n        return Right(e.value)\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.sequence","title":"<code>sequence(eithers)</code>  <code>classmethod</code>","text":"<p>CList[Either[E, A]] -&gt; Either[E, CList[A]].</p> <p>Returns Right(clist) if all are Right, first Left otherwise.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@classmethod\ndef sequence(cls, eithers: CList[Either[E, A]]) -&gt; Either[E, CList[A]]:\n    \"\"\"CList[Either[E, A]] -&gt; Either[E, CList[A]].\n\n    Returns Right(clist) if all are Right, first Left otherwise.\n    \"\"\"\n    from funstruct.collections.cons import Cons, Nil\n    from funstruct.util.tailrec import tail_call, tco\n\n    @tco\n    def _go(remaining, acc):\n        match remaining:\n            case Nil():\n                return Right(acc.reversed())\n            case Cons(head, tail):\n                match head:\n                    case Left():\n                        return head\n                    case Right(v):\n                        return tail_call(_go)(tail, Cons(v, acc))\n\n    return _go(eithers, Nil())\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.traverse","title":"<code>traverse(values, f)</code>  <code>classmethod</code>","text":"<p>CList[A] -&gt; (A -&gt; Either[E, B]) -&gt; Either[E, CList[B]].</p> <p>Applies f to each element, short-circuits on first Left.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@classmethod\ndef traverse(cls, values, f: Callable) -&gt; Either:\n    \"\"\"CList[A] -&gt; (A -&gt; Either[E, B]) -&gt; Either[E, CList[B]].\n\n    Applies f to each element, short-circuits on first Left.\n    \"\"\"\n    return cls.sequence(values.map(f))\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.to_option","title":"<code>to_option()</code>","text":"<p>Convert to Option \u2014 Right(v) \u2192 Some(v), Left(_) \u2192 Nothing.</p> <p>Right(1).to_option() Some(1) Left(\"err\").to_option() Nothing()</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def to_option(self):\n    \"\"\"Convert to Option \u2014 Right(v) \u2192 Some(v), Left(_) \u2192 Nothing.\n\n    &gt;&gt;&gt; Right(1).to_option()\n    Some(1)\n    &gt;&gt;&gt; Left(\"err\").to_option()\n    Nothing()\n    \"\"\"\n    from funstruct.monad.option import Nothing, Some\n\n    match self:\n        case Right(v):\n            return Some(v)\n        case _:\n            return Nothing()\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.alt","title":"<code>alt(f)</code>  <code>abstractmethod</code>","text":"<p>Transform the error without recovering. No-op on Right.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@abstractmethod\ndef alt(self, f: Callable[[E], E]) -&gt; Either[E, A]:\n    \"\"\"Transform the error without recovering. No-op on Right.\"\"\"\n    ...\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.or_else","title":"<code>or_else(f)</code>  <code>abstractmethod</code>","text":"<p>Handle error \u2014 f can recover (Right) or re-fail (Left). No-op on Right.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@abstractmethod\ndef or_else(self, f: Callable[[E], Either]) -&gt; Either:\n    \"\"\"Handle error \u2014 f can recover (Right) or re-fail (Left). No-op on Right.\"\"\"\n    ...\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.get_or_else","title":"<code>get_or_else(default)</code>  <code>abstractmethod</code>","text":"<p>Extract the value, or return default if Left.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@abstractmethod\ndef get_or_else(self, default: A) -&gt; A:\n    \"\"\"Extract the value, or return default if Left.\"\"\"\n    ...\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.fold","title":"<code>fold(on_left, on_right)</code>  <code>abstractmethod</code>","text":"<p>Eliminate \u2014 apply on_left or on_right depending on the case.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@abstractmethod\ndef fold(self, on_left: Callable, on_right: Callable):\n    \"\"\"Eliminate \u2014 apply on_left or on_right depending on the case.\"\"\"\n    ...\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Either.swap","title":"<code>swap()</code>  <code>abstractmethod</code>","text":"<p>Swap Left and Right.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@abstractmethod\ndef swap(self) -&gt; Either[A, E]:\n    \"\"\"Swap Left and Right.\"\"\"\n    ...\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Right","title":"<code>Right</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Either[E, A]</code></p> <p>Success case.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Right(Either[E, A]):\n    \"\"\"Success case.\"\"\"\n\n    value: A\n\n    @property\n    def is_right(self) -&gt; bool:\n        return True\n\n    def map(self, f: Callable[[A], B]) -&gt; Either[E, B]:\n        return Right(f(self.value))\n\n    def bind(self, f: Callable[[A], Either[E, B]]) -&gt; Either[E, B]:\n        return f(self.value)\n\n    def ap(self, other: Either) -&gt; Either:\n        match other:\n            case Right(val):\n                return Right((self.value, val))\n            case _:\n                return other\n\n    def alt(self, f: Callable[[E], E]) -&gt; Either[E, A]:\n        \"\"\"No-op on Right \u2014 already succeeded.\"\"\"\n        return self\n\n    def or_else(self, f: Callable[[E], Either]) -&gt; Either[E, A]:\n        \"\"\"No-op on Right \u2014 already succeeded.\"\"\"\n        return self\n\n    def get_or_else(self, default: A) -&gt; A:\n        \"\"\"Return the value (ignores default on Right).\"\"\"\n        return self.value\n\n    def fold(self, on_left: Callable, on_right: Callable):\n        \"\"\"Apply on_right to the value.\"\"\"\n        return on_right(self.value)\n\n    def swap(self) -&gt; Either[A, E]:\n        \"\"\"Swap Right(v) \u2192 Left(v).\"\"\"\n        return Left(self.value)\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case Right(val):\n                return self.value == val\n            case _:\n                return False\n\n    def __repr__(self) -&gt; str:\n        return f\"Right({repr(self.value)})\"\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Right.alt","title":"<code>alt(f)</code>","text":"<p>No-op on Right \u2014 already succeeded.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def alt(self, f: Callable[[E], E]) -&gt; Either[E, A]:\n    \"\"\"No-op on Right \u2014 already succeeded.\"\"\"\n    return self\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Right.or_else","title":"<code>or_else(f)</code>","text":"<p>No-op on Right \u2014 already succeeded.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def or_else(self, f: Callable[[E], Either]) -&gt; Either[E, A]:\n    \"\"\"No-op on Right \u2014 already succeeded.\"\"\"\n    return self\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Right.get_or_else","title":"<code>get_or_else(default)</code>","text":"<p>Return the value (ignores default on Right).</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def get_or_else(self, default: A) -&gt; A:\n    \"\"\"Return the value (ignores default on Right).\"\"\"\n    return self.value\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Right.fold","title":"<code>fold(on_left, on_right)</code>","text":"<p>Apply on_right to the value.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def fold(self, on_left: Callable, on_right: Callable):\n    \"\"\"Apply on_right to the value.\"\"\"\n    return on_right(self.value)\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Right.swap","title":"<code>swap()</code>","text":"<p>Swap Right(v) \u2192 Left(v).</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def swap(self) -&gt; Either[A, E]:\n    \"\"\"Swap Right(v) \u2192 Left(v).\"\"\"\n    return Left(self.value)\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Left","title":"<code>Left</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Either[E, A]</code></p> <p>Error case.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Left(Either[E, A]):\n    \"\"\"Error case.\"\"\"\n\n    error: E\n\n    @property\n    def is_right(self) -&gt; bool:\n        return False\n\n    def map(self, f: Callable) -&gt; Either:\n        return self\n\n    def bind(self, f: Callable) -&gt; Either:\n        return self\n\n    def ap(self, other: Either) -&gt; Either:\n        return self\n\n    def alt(self, f: Callable[[E], E]) -&gt; Either[E, A]:\n        \"\"\"Transform the error without recovering.\n\n        &gt;&gt;&gt; Left(\"oops\").alt(lambda e: e.upper())\n        Left('OOPS')\n        \"\"\"\n        return Left(f(self.error))\n\n    def or_else(self, f: Callable[[E], Either]) -&gt; Either:\n        \"\"\"Handle error: f receives the error, returns a new Either.\n\n        &gt;&gt;&gt; Left(\"oops\").or_else(lambda e: Right(f\"recovered: {e}\"))\n        Right('recovered: oops')\n        &gt;&gt;&gt; Left(\"oops\").or_else(lambda e: Left(f\"still bad: {e}\"))\n        Left('still bad: oops')\n        \"\"\"\n        return f(self.error)\n\n    def get_or_else(self, default: A) -&gt; A:\n        \"\"\"Return default (error is discarded).\"\"\"\n        return default\n\n    def fold(self, on_left: Callable, on_right: Callable):\n        \"\"\"Apply on_left to the error.\"\"\"\n        return on_left(self.error)\n\n    def swap(self) -&gt; Either[A, E]:\n        \"\"\"Swap Left(e) \u2192 Right(e).\"\"\"\n        return Right(self.error)\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case Left(err):\n                return self.error == err\n            case _:\n                return False\n\n    def __repr__(self) -&gt; str:\n        return f\"Left({repr(self.error)})\"\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Left.alt","title":"<code>alt(f)</code>","text":"<p>Transform the error without recovering.</p> <p>Left(\"oops\").alt(lambda e: e.upper()) Left('OOPS')</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def alt(self, f: Callable[[E], E]) -&gt; Either[E, A]:\n    \"\"\"Transform the error without recovering.\n\n    &gt;&gt;&gt; Left(\"oops\").alt(lambda e: e.upper())\n    Left('OOPS')\n    \"\"\"\n    return Left(f(self.error))\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Left.or_else","title":"<code>or_else(f)</code>","text":"<p>Handle error: f receives the error, returns a new Either.</p> <p>Left(\"oops\").or_else(lambda e: Right(f\"recovered: {e}\")) Right('recovered: oops') Left(\"oops\").or_else(lambda e: Left(f\"still bad: {e}\")) Left('still bad: oops')</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def or_else(self, f: Callable[[E], Either]) -&gt; Either:\n    \"\"\"Handle error: f receives the error, returns a new Either.\n\n    &gt;&gt;&gt; Left(\"oops\").or_else(lambda e: Right(f\"recovered: {e}\"))\n    Right('recovered: oops')\n    &gt;&gt;&gt; Left(\"oops\").or_else(lambda e: Left(f\"still bad: {e}\"))\n    Left('still bad: oops')\n    \"\"\"\n    return f(self.error)\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Left.get_or_else","title":"<code>get_or_else(default)</code>","text":"<p>Return default (error is discarded).</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def get_or_else(self, default: A) -&gt; A:\n    \"\"\"Return default (error is discarded).\"\"\"\n    return default\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Left.fold","title":"<code>fold(on_left, on_right)</code>","text":"<p>Apply on_left to the error.</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def fold(self, on_left: Callable, on_right: Callable):\n    \"\"\"Apply on_left to the error.\"\"\"\n    return on_left(self.error)\n</code></pre>"},{"location":"monad/either/#funstruct.monad.either.Left.swap","title":"<code>swap()</code>","text":"<p>Swap Left(e) \u2192 Right(e).</p> Source code in <code>funstruct/monad/either.py</code> <pre><code>def swap(self) -&gt; Either[A, E]:\n    \"\"\"Swap Left(e) \u2192 Right(e).\"\"\"\n    return Right(self.error)\n</code></pre>"},{"location":"monad/future/","title":"Future","text":""},{"location":"monad/future/#funstruct.monad.future","title":"<code>funstruct.monad.future</code>","text":"<p>Future \u2014 lazy async computation.</p> <p>Future[A] wraps Awaitable[A]. A generic async monad \u2014 no error semantics built in. For error handling, use AsyncResult[A] from funstruct.monad.result.</p>"},{"location":"monad/future/#funstruct.monad.future.Future","title":"<code>Future</code>","text":"<p>               Bases: <code>Generic[A]</code></p> <p>Lazy async computation that produces A when awaited.</p> <p>Build pipelines with .bind(), .map() \u2014 no await needed. Execute once at the boundary with await.</p> <p>Future[A] is a generic async monad. It does not know about errors. For error-aware async, use AsyncResult[A] from funstruct.monad.result.</p> Source code in <code>funstruct/monad/future.py</code> <pre><code>class Future(Generic[A]):\n    \"\"\"Lazy async computation that produces A when awaited.\n\n    Build pipelines with .bind(), .map() \u2014 no await needed.\n    Execute once at the boundary with await.\n\n    Future[A] is a generic async monad. It does not know about errors.\n    For error-aware async, use AsyncResult[A] from funstruct.monad.result.\n    \"\"\"\n\n    __slots__ = (\"_coro\",)\n\n    def __init__(self, coro: Awaitable[A]) -&gt; None:\n        self._coro = coro\n\n    def __del__(self):\n        if hasattr(self._coro, \"close\"):\n            getattr(self._coro, \"close\")()\n\n    def __await__(self) -&gt; Generator[None, None, A]:\n        return self._awaitable().__await__()\n\n    async def _awaitable(self) -&gt; A:\n        return await self._coro\n\n    def map(self, f: Callable[[A], B]) -&gt; Future[B]:\n        \"\"\"Transform the value without executing.\"\"\"\n\n        async def _inner():\n            result = await self._coro\n            return f(result)\n\n        return Future(_inner())\n\n    def bind(self, f: Callable[[A], Future[B]]) -&gt; Future[B]:\n        \"\"\"Chain: f receives the value, returns a new Future.\"\"\"\n\n        async def _inner():\n            result = await self._coro\n            return await f(result)\n\n        return Future(_inner())\n\n    def then(self, next_future: Future[B]) -&gt; Future[B]:\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_future)\n\n    @classmethod\n    def pure(cls, value: A) -&gt; Future[A]:\n        \"\"\"Lift a plain value into a Future.\"\"\"\n\n        async def _inner():\n            return value\n\n        return cls(_inner())\n\n    def __repr__(self) -&gt; str:\n        return f\"Future({self._coro})\"\n</code></pre>"},{"location":"monad/future/#funstruct.monad.future.Future.map","title":"<code>map(f)</code>","text":"<p>Transform the value without executing.</p> Source code in <code>funstruct/monad/future.py</code> <pre><code>def map(self, f: Callable[[A], B]) -&gt; Future[B]:\n    \"\"\"Transform the value without executing.\"\"\"\n\n    async def _inner():\n        result = await self._coro\n        return f(result)\n\n    return Future(_inner())\n</code></pre>"},{"location":"monad/future/#funstruct.monad.future.Future.bind","title":"<code>bind(f)</code>","text":"<p>Chain: f receives the value, returns a new Future.</p> Source code in <code>funstruct/monad/future.py</code> <pre><code>def bind(self, f: Callable[[A], Future[B]]) -&gt; Future[B]:\n    \"\"\"Chain: f receives the value, returns a new Future.\"\"\"\n\n    async def _inner():\n        result = await self._coro\n        return await f(result)\n\n    return Future(_inner())\n</code></pre>"},{"location":"monad/future/#funstruct.monad.future.Future.then","title":"<code>then(next_future)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monad/future.py</code> <pre><code>def then(self, next_future: Future[B]) -&gt; Future[B]:\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_future)\n</code></pre>"},{"location":"monad/future/#funstruct.monad.future.Future.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a plain value into a Future.</p> Source code in <code>funstruct/monad/future.py</code> <pre><code>@classmethod\ndef pure(cls, value: A) -&gt; Future[A]:\n    \"\"\"Lift a plain value into a Future.\"\"\"\n\n    async def _inner():\n        return value\n\n    return cls(_inner())\n</code></pre>"},{"location":"monad/option/","title":"Option","text":""},{"location":"monad/option/#funstruct.monad.option","title":"<code>funstruct.monad.option</code>","text":"<p>Option monad \u2014 presence or absence of a value.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monad.option import Option, Some, Nothing\n&gt;&gt;&gt; from funstruct.collections.cons import Cons, Nil, CList\n&gt;&gt;&gt; Some(1).map(lambda x: x + 10)\nSome(11)\n&gt;&gt;&gt; Nothing().bind(lambda x: Some(x * 2))\nNothing()\n&gt;&gt;&gt; Some(1) &gt;&gt; (lambda x: Some(x + 1))\nSome(2)\n&gt;&gt;&gt; Option.from_optional(None)\nNothing()\n</code></pre> <p>map2 \u2014 combine two Options with a function:</p> <pre><code>&gt;&gt;&gt; Some(2).map2(Some(3), lambda a, b: a + b)\nSome(5)\n&gt;&gt;&gt; Some(2).map2(Nothing(), lambda a, b: a + b)\nNothing()\n</code></pre> <p>sequence \u2014 CList[Option[A]] \u2192 Option[CList[A]]:</p> <pre><code>&gt;&gt;&gt; Option.sequence(Cons(Some(1), Cons(Some(2), Cons(Some(3), Nil()))))\nSome(Cons(1, Cons(2, Cons(3, Nil()))))\n&gt;&gt;&gt; Option.sequence(Cons(Some(1), Cons(Nothing(), Cons(Some(3), Nil()))))\nNothing()\n</code></pre> <p>traverse \u2014 map then sequence:</p> <pre><code>&gt;&gt;&gt; Option.traverse(CList.from_iterable([1, 2, 3]), lambda x: Some(x * 10))\nSome(Cons(10, Cons(20, Cons(30, Nil()))))\n&gt;&gt;&gt; safe = lambda x: Some(x) if x != 0 else Nothing()\n&gt;&gt;&gt; Option.traverse(CList.from_iterable([1, 0, 3]), safe)\nNothing()\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Option","title":"<code>Option</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[A]</code></p> <p>Option[A]: either Some(value) or Nothing.</p> Performance characteristics <ul> <li>map/bind/ap: O(1)</li> <li>pure:        O(1)</li> </ul> Source code in <code>funstruct/monad/option.py</code> <pre><code>class Option(Monad, Generic[A]):\n    \"\"\"Option[A]: either Some(value) or Nothing.\n\n    Performance characteristics:\n        - map/bind/ap: O(1)\n        - pure:        O(1)\n    \"\"\"\n\n    # Type-narrowed signatures so type checkers see Option[B], not Monad[B].\n    # Implementations live in Some/Nothing below. @abstractmethod with ...\n    # means \"must be implemented by subclasses\" without raising anything.\n    @abstractmethod\n    def bind(self, f: Callable[[A], Option[B]]) -&gt; Option[B]: ...\n\n    @abstractmethod\n    def map(self, f: Callable[[A], B]) -&gt; Option[B]: ...\n\n    @abstractmethod\n    def get_or_else(self, default: A) -&gt; A: ...\n\n    @abstractmethod\n    def or_else(self, fallback: Callable[[], Option[A]]) -&gt; Option[A]: ...\n\n    @classmethod\n    def pure(cls, value: A) -&gt; Option[A]:\n        return Some(value)\n\n    @classmethod\n    def from_optional(cls, value: A | None) -&gt; Option[A]:\n        \"\"\"Convert a Python value that might be None into an Option.\"\"\"\n        return Nothing() if value is None else Some(value)\n\n    @classmethod\n    def sequence(cls, options: CList[Option[A]]) -&gt; Option[CList[A]]:\n        \"\"\"CList[Option[A]] \u2192 Option[CList[A]].\n\n        Returns Some(clist) if all are Some, Nothing if any is Nothing.\n        Tail-recursive via @tco.\n        \"\"\"\n        from funstruct.collections.cons import Cons, Nil\n        from funstruct.util.tailrec import tail_call, tco\n\n        @tco\n        def _go(remaining, acc):\n            match remaining:\n                case Nil():\n                    return Some(acc.reversed())\n                case Cons(head, tail):\n                    match head:\n                        case Nothing():\n                            return Nothing()\n                        case Some(v):\n                            return tail_call(_go)(tail, Cons(v, acc))\n\n        return _go(options, Nil())\n\n    @classmethod\n    def traverse(cls, values: CList[A], f: Callable[[A], Option]) -&gt; Option[CList]:\n        \"\"\"CList[A] \u2192 (A \u2192 Option[B]) \u2192 Option[CList[B]].\n\n        Applies f to each element, short-circuits on first Nothing.\n        \"\"\"\n        return cls.sequence(values.map(f))\n\n    @classmethod\n    def do(cls, gen_fn: Callable) -&gt; Option:\n        \"\"\"Do-notation. Short-circuits on Nothing.\n\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield Some(1)\n        ...     y = yield Some(x + 10)\n        ...     return x + y\n        &gt;&gt;&gt; Option.do(pipeline)\n        Some(12)\n        \"\"\"\n        gen = gen_fn()\n        try:\n            monadic_val = next(gen)\n            while True:\n                match monadic_val:\n                    case Nothing():\n                        return Nothing()\n                    case Some(value):\n                        monadic_val = gen.send(value)\n        except StopIteration as e:\n            return Some(e.value)\n\n    def to_result(self, error):\n        \"\"\"Convert to Either \u2014 Some(v) \u2192 Right(v), Nothing \u2192 Left(error).\n\n        &gt;&gt;&gt; Some(1).to_result(\"missing\")\n        Right(1)\n        &gt;&gt;&gt; Nothing().to_result(\"missing\")\n        Left('missing')\n        \"\"\"\n        from funstruct.monad.either import Left, Right\n\n        match self:\n            case Some(v):\n                return Right(v)\n            case _:\n                return Left(error)\n\n    @property\n    @abstractmethod\n    def is_some(self) -&gt; bool: ...\n\n    @property\n    def is_nothing(self) -&gt; bool:\n        return not self.is_some\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Option.from_optional","title":"<code>from_optional(value)</code>  <code>classmethod</code>","text":"<p>Convert a Python value that might be None into an Option.</p> Source code in <code>funstruct/monad/option.py</code> <pre><code>@classmethod\ndef from_optional(cls, value: A | None) -&gt; Option[A]:\n    \"\"\"Convert a Python value that might be None into an Option.\"\"\"\n    return Nothing() if value is None else Some(value)\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Option.sequence","title":"<code>sequence(options)</code>  <code>classmethod</code>","text":"<p>CList[Option[A]] \u2192 Option[CList[A]].</p> <p>Returns Some(clist) if all are Some, Nothing if any is Nothing. Tail-recursive via @tco.</p> Source code in <code>funstruct/monad/option.py</code> <pre><code>@classmethod\ndef sequence(cls, options: CList[Option[A]]) -&gt; Option[CList[A]]:\n    \"\"\"CList[Option[A]] \u2192 Option[CList[A]].\n\n    Returns Some(clist) if all are Some, Nothing if any is Nothing.\n    Tail-recursive via @tco.\n    \"\"\"\n    from funstruct.collections.cons import Cons, Nil\n    from funstruct.util.tailrec import tail_call, tco\n\n    @tco\n    def _go(remaining, acc):\n        match remaining:\n            case Nil():\n                return Some(acc.reversed())\n            case Cons(head, tail):\n                match head:\n                    case Nothing():\n                        return Nothing()\n                    case Some(v):\n                        return tail_call(_go)(tail, Cons(v, acc))\n\n    return _go(options, Nil())\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Option.traverse","title":"<code>traverse(values, f)</code>  <code>classmethod</code>","text":"<p>CList[A] \u2192 (A \u2192 Option[B]) \u2192 Option[CList[B]].</p> <p>Applies f to each element, short-circuits on first Nothing.</p> Source code in <code>funstruct/monad/option.py</code> <pre><code>@classmethod\ndef traverse(cls, values: CList[A], f: Callable[[A], Option]) -&gt; Option[CList]:\n    \"\"\"CList[A] \u2192 (A \u2192 Option[B]) \u2192 Option[CList[B]].\n\n    Applies f to each element, short-circuits on first Nothing.\n    \"\"\"\n    return cls.sequence(values.map(f))\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Option.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation. Short-circuits on Nothing.</p> <p>def pipeline(): ...     x = yield Some(1) ...     y = yield Some(x + 10) ...     return x + y Option.do(pipeline) Some(12)</p> Source code in <code>funstruct/monad/option.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn: Callable) -&gt; Option:\n    \"\"\"Do-notation. Short-circuits on Nothing.\n\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield Some(1)\n    ...     y = yield Some(x + 10)\n    ...     return x + y\n    &gt;&gt;&gt; Option.do(pipeline)\n    Some(12)\n    \"\"\"\n    gen = gen_fn()\n    try:\n        monadic_val = next(gen)\n        while True:\n            match monadic_val:\n                case Nothing():\n                    return Nothing()\n                case Some(value):\n                    monadic_val = gen.send(value)\n    except StopIteration as e:\n        return Some(e.value)\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Option.to_result","title":"<code>to_result(error)</code>","text":"<p>Convert to Either \u2014 Some(v) \u2192 Right(v), Nothing \u2192 Left(error).</p> <p>Some(1).to_result(\"missing\") Right(1) Nothing().to_result(\"missing\") Left('missing')</p> Source code in <code>funstruct/monad/option.py</code> <pre><code>def to_result(self, error):\n    \"\"\"Convert to Either \u2014 Some(v) \u2192 Right(v), Nothing \u2192 Left(error).\n\n    &gt;&gt;&gt; Some(1).to_result(\"missing\")\n    Right(1)\n    &gt;&gt;&gt; Nothing().to_result(\"missing\")\n    Left('missing')\n    \"\"\"\n    from funstruct.monad.either import Left, Right\n\n    match self:\n        case Some(v):\n            return Right(v)\n        case _:\n            return Left(error)\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Some","title":"<code>Some</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Option[A]</code></p> <p>Presence of a value.</p> Source code in <code>funstruct/monad/option.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Some(Option[A]):\n    \"\"\"Presence of a value.\"\"\"\n\n    value: A\n\n    @property\n    def is_some(self) -&gt; bool:\n        return True\n\n    def map(self, f: Callable) -&gt; Option:\n        return Some(f(self.value))\n\n    def ap(self, other: Option) -&gt; Option:\n        match other:\n            case Some(val):\n                return Some((self.value, val))\n            case _:\n                return other\n\n    def bind(self, f: Callable[[A], Option]) -&gt; Option:\n        return f(self.value)\n\n    def get_or_else(self, default: A) -&gt; A:\n        return self.value\n\n    def or_else(self, fallback: Callable[[], Option[A]]) -&gt; Option[A]:\n        return self\n\n    def filter(self, f: Callable[[A], bool]) -&gt; Option[A]:\n        return self if f(self.value) else Nothing()\n\n    def fold(self, on_nothing: Callable, on_some: Callable):\n        return on_some(self.value)\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case Some(val):\n                return self.value == val\n            case _:\n                return False\n\n    def __bool__(self) -&gt; bool:\n        return True\n\n    def __repr__(self) -&gt; str:\n        return f\"Some({repr(self.value)})\"\n</code></pre>"},{"location":"monad/option/#funstruct.monad.option.Nothing","title":"<code>Nothing</code>","text":"<p>               Bases: <code>Option</code></p> <p>Absence of a value (singleton).</p> Source code in <code>funstruct/monad/option.py</code> <pre><code>class Nothing(Option):\n    \"\"\"Absence of a value (singleton).\"\"\"\n\n    _instance = None\n\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n\n    @property\n    def is_some(self) -&gt; bool:\n        return False\n\n    def map(self, f: Callable) -&gt; Option:\n        return self\n\n    def ap(self, other: Option) -&gt; Option:\n        return self\n\n    def bind(self, f: Callable) -&gt; Option:\n        return self\n\n    def get_or_else(self, default: A) -&gt; A:\n        return default\n\n    def or_else(self, fallback: Callable[[], Option[A]]) -&gt; Option[A]:\n        return fallback()\n\n    def filter(self, f: Callable) -&gt; Option:\n        return self\n\n    def fold(self, on_nothing: Callable, on_some: Callable):\n        return on_nothing()\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case Nothing():\n                return True\n            case _:\n                return False\n\n    def __bool__(self) -&gt; bool:\n        return False\n\n    def __repr__(self) -&gt; str:\n        return \"Nothing()\"\n</code></pre>"},{"location":"monad/reader/","title":"Reader","text":""},{"location":"monad/reader/#funstruct.monad.reader","title":"<code>funstruct.monad.reader</code>","text":"<p>Reader monad \u2014 computations that read from a shared environment.</p> <p>bind lets multiple computations share the same context without passing it explicitly. Each step in the chain sees the same env.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monad.reader import Reader\n&gt;&gt;&gt; get_host = Reader(lambda cfg: cfg[\"host\"])\n&gt;&gt;&gt; get_port = Reader(lambda cfg: cfg[\"port\"])\n&gt;&gt;&gt; get_path = Reader(lambda cfg: cfg.get(\"path\", \"/\"))\n&gt;&gt;&gt; build_url = (\n...     get_host\n...     .bind(lambda h: get_port\n...     .bind(lambda p: get_path\n...     .map(lambda path: f\"http://{h}:{p}{path}\")))\n... )\n&gt;&gt;&gt; build_url.run({\"host\": \"localhost\", \"port\": 8080, \"path\": \"/api\"})\n'http://localhost:8080/api'\n&gt;&gt;&gt; build_url.run({\"host\": \"prod.co\", \"port\": 443})\n'http://prod.co:443/'\n</code></pre>"},{"location":"monad/reader/#funstruct.monad.reader.Reader","title":"<code>Reader</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[_Ctx, _A]</code></p> <p>Reader: Ctx -&gt; A.</p> <p>A computation that reads from a shared environment.</p> Source code in <code>funstruct/monad/reader.py</code> <pre><code>class Reader(Monad, Generic[_Ctx, _A]):\n    \"\"\"Reader: Ctx -&gt; A.\n\n    A computation that reads from a shared environment.\n    \"\"\"\n\n    __slots__ = (\"_run\",)\n\n    def __init__(self, run: Callable) -&gt; None:\n        self._run = run\n\n    def run(self, ctx):\n        \"\"\"Execute with the given context.\"\"\"\n        return self._run(ctx)\n\n    def __call__(self, ctx):\n        return self.run(ctx)\n\n    def map(self, f: Callable) -&gt; Reader:\n        \"\"\"Transform the produced value.\"\"\"\n        return Reader(lambda ctx: f(self._run(ctx)))\n\n    def bind(self, f: Callable) -&gt; Reader:\n        \"\"\"Chain: f receives the value, returns a new Reader.\"\"\"\n        return Reader(lambda ctx: f(self._run(ctx)).run(ctx))\n\n    @classmethod\n    def do(cls, gen_fn: Callable[[], Generator[Reader, object, _R]]) -&gt; Reader:\n        \"\"\"Do-notation via generators. Flattens nested binds.\n\n        Each `yield` extracts the value from a Reader (all share the same ctx).\n        The final `return` value becomes the Reader's result.\n\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield Reader(lambda ctx: ctx[\"x\"])\n        ...     y = yield Reader(lambda ctx: ctx[\"y\"])\n        ...     return x + y\n        &gt;&gt;&gt; Reader.do(pipeline).run({\"x\": 1, \"y\": 10})\n        11\n        \"\"\"\n\n        def _run(ctx):\n            gen = gen_fn()\n            try:\n                monadic_val = next(gen)\n                while True:\n                    result = monadic_val.run(ctx)\n                    monadic_val = gen.send(result)\n            except StopIteration as e:\n                return e.value\n\n        return cls(_run)\n\n    @classmethod\n    def pure(cls, value, *args, **kwargs) -&gt; Reader:\n        \"\"\"Lift a value \u2014 ignores context.\"\"\"\n        return cls(lambda _: value)\n\n    @classmethod\n    def ask(cls) -&gt; Reader:\n        \"\"\"Return the context itself as the value.\"\"\"\n        return cls(lambda ctx: ctx)\n\n    def __repr__(self) -&gt; str:\n        return f\"Reader({self._run})\"\n</code></pre>"},{"location":"monad/reader/#funstruct.monad.reader.Reader.run","title":"<code>run(ctx)</code>","text":"<p>Execute with the given context.</p> Source code in <code>funstruct/monad/reader.py</code> <pre><code>def run(self, ctx):\n    \"\"\"Execute with the given context.\"\"\"\n    return self._run(ctx)\n</code></pre>"},{"location":"monad/reader/#funstruct.monad.reader.Reader.map","title":"<code>map(f)</code>","text":"<p>Transform the produced value.</p> Source code in <code>funstruct/monad/reader.py</code> <pre><code>def map(self, f: Callable) -&gt; Reader:\n    \"\"\"Transform the produced value.\"\"\"\n    return Reader(lambda ctx: f(self._run(ctx)))\n</code></pre>"},{"location":"monad/reader/#funstruct.monad.reader.Reader.bind","title":"<code>bind(f)</code>","text":"<p>Chain: f receives the value, returns a new Reader.</p> Source code in <code>funstruct/monad/reader.py</code> <pre><code>def bind(self, f: Callable) -&gt; Reader:\n    \"\"\"Chain: f receives the value, returns a new Reader.\"\"\"\n    return Reader(lambda ctx: f(self._run(ctx)).run(ctx))\n</code></pre>"},{"location":"monad/reader/#funstruct.monad.reader.Reader.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation via generators. Flattens nested binds.</p> <p>Each <code>yield</code> extracts the value from a Reader (all share the same ctx). The final <code>return</code> value becomes the Reader's result.</p> <p>def pipeline(): ...     x = yield Reader(lambda ctx: ctx[\"x\"]) ...     y = yield Reader(lambda ctx: ctx[\"y\"]) ...     return x + y Reader.do(pipeline).run({\"x\": 1, \"y\": 10}) 11</p> Source code in <code>funstruct/monad/reader.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn: Callable[[], Generator[Reader, object, _R]]) -&gt; Reader:\n    \"\"\"Do-notation via generators. Flattens nested binds.\n\n    Each `yield` extracts the value from a Reader (all share the same ctx).\n    The final `return` value becomes the Reader's result.\n\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield Reader(lambda ctx: ctx[\"x\"])\n    ...     y = yield Reader(lambda ctx: ctx[\"y\"])\n    ...     return x + y\n    &gt;&gt;&gt; Reader.do(pipeline).run({\"x\": 1, \"y\": 10})\n    11\n    \"\"\"\n\n    def _run(ctx):\n        gen = gen_fn()\n        try:\n            monadic_val = next(gen)\n            while True:\n                result = monadic_val.run(ctx)\n                monadic_val = gen.send(result)\n        except StopIteration as e:\n            return e.value\n\n    return cls(_run)\n</code></pre>"},{"location":"monad/reader/#funstruct.monad.reader.Reader.pure","title":"<code>pure(value, *args, **kwargs)</code>  <code>classmethod</code>","text":"<p>Lift a value \u2014 ignores context.</p> Source code in <code>funstruct/monad/reader.py</code> <pre><code>@classmethod\ndef pure(cls, value, *args, **kwargs) -&gt; Reader:\n    \"\"\"Lift a value \u2014 ignores context.\"\"\"\n    return cls(lambda _: value)\n</code></pre>"},{"location":"monad/reader/#funstruct.monad.reader.Reader.ask","title":"<code>ask()</code>  <code>classmethod</code>","text":"<p>Return the context itself as the value.</p> Source code in <code>funstruct/monad/reader.py</code> <pre><code>@classmethod\ndef ask(cls) -&gt; Reader:\n    \"\"\"Return the context itself as the value.\"\"\"\n    return cls(lambda ctx: ctx)\n</code></pre>"},{"location":"monad/result/","title":"Result","text":""},{"location":"monad/result/#funstruct.monad.result","title":"<code>funstruct.monad.result</code>","text":"<p>Result \u2014 Either with domain-oriented naming.</p> <p>Result[E, A] = Ok(a) | Err(e). Same type as Either, clearer names. AsyncResult[A] = Future[Exception, A]. Async counterpart of Result.</p> Decorators <p>@Try       : (args) -&gt; Result[A]       = Either[Exception, A] @TryAsync  : (args) -&gt; AsyncResult[A]  = Future[Exception, A]</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monad.result import Result, Ok, Err, Try\n&gt;&gt;&gt; Ok(10).map(lambda x: x + 1)\nOk(11)\n&gt;&gt;&gt; Err(\"bad\").map(lambda x: x + 1)\nErr('bad')\n&gt;&gt;&gt; Ok(10).bind(lambda x: Ok(x * 2))\nOk(20)\n</code></pre> <p>or_else \u2014 recover from Err:</p> <pre><code>&gt;&gt;&gt; Err(\"bad\").or_else(lambda e: Ok(\"default\"))\nOk('default')\n</code></pre> <p>@Try decorator:</p> <pre><code>&gt;&gt;&gt; @Try\n... def safe_div(a, b):\n...     return a / b\n&gt;&gt;&gt; safe_div(10, 2)\nOk(5.0)\n&gt;&gt;&gt; safe_div(10, 0)\nErr(ZeroDivisionError(...))\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.Result","title":"<code>Result</code>","text":"<p>               Bases: <code>Either[Exception, _A]</code>, <code>Generic[_A]</code></p> <p>Result[A] = Ok(value) | Err(exception).</p> <p>Single type parameter \u2014 E is fixed to Exception. Same as Either[Exception, A] but with 1 param for cleaner annotations.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>class Result(Either[Exception, _A], Generic[_A]):\n    \"\"\"Result[A] = Ok(value) | Err(exception).\n\n    Single type parameter \u2014 E is fixed to Exception.\n    Same as Either[Exception, A] but with 1 param for cleaner annotations.\n    \"\"\"\n\n    @abstractmethod\n    def map(self, f: Callable[[_A], _B]) -&gt; Result[_B]: ...\n    @abstractmethod\n    def bind(self, f: Callable[[_A], Result[_B]]) -&gt; Result[_B]: ...\n    @abstractmethod\n    def alt(self, f: Callable[[Exception], Exception]) -&gt; Result[_A]: ...\n    @abstractmethod\n    def or_else(self, f: Callable[[Exception], Result[_A]]) -&gt; Result[_A]: ...\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.Ok","title":"<code>Ok</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Right</code></p> <p>Success case of Result.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Ok(Right):\n    \"\"\"Success case of Result.\"\"\"\n\n    def map(self, f: Callable[[_A], _B]) -&gt; Result[_B]:\n        return Ok(f(self.value))\n\n    def bind(self, f: Callable[[_A], Result[_B]]) -&gt; Result[_B]:\n        return f(self.value)\n\n    def alt(self, f: Callable[[Exception], Exception]) -&gt; Result[_A]:\n        return self\n\n    def or_else(self, f: Callable[[Exception], Result[_A]]) -&gt; Result[_A]:\n        return self\n\n    def __repr__(self) -&gt; str:\n        return f\"Ok({repr(self.value)})\"\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.Err","title":"<code>Err</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Left</code></p> <p>Error case of Result.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>@dataclass(frozen=True, eq=False)\nclass Err(Left):\n    \"\"\"Error case of Result.\"\"\"\n\n    def map(self, f: Callable[[_A], _B]) -&gt; Result[_B]:\n        return self\n\n    def bind(self, f: Callable[[_A], Result[_B]]) -&gt; Result[_B]:\n        return self\n\n    def alt(self, f: Callable[[Exception], Exception]) -&gt; Result[_A]:\n        return Err(f(self.error))\n\n    def or_else(self, f: Callable[[Exception], Result[_A]]) -&gt; Result[_A]:\n        return f(self.error)\n\n    def __repr__(self) -&gt; str:\n        return f\"Err({repr(self.error)})\"\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult","title":"<code>AsyncResult</code>","text":"<p>               Bases: <code>Generic[_A]</code></p> <p>Lazy async computation that produces Result[A] (Ok or Err) when awaited.</p> <p>AsyncResult[User] = async computation \u2192 Ok(user) or Err(exception). Compose with .bind(), .map(), .alt(), .or_else() \u2014 no await needed. Execute once at the boundary with await.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>class AsyncResult(Generic[_A]):\n    \"\"\"Lazy async computation that produces Result[A] (Ok or Err) when awaited.\n\n    AsyncResult[User] = async computation \u2192 Ok(user) or Err(exception).\n    Compose with .bind(), .map(), .alt(), .or_else() \u2014 no await needed.\n    Execute once at the boundary with await.\n    \"\"\"\n\n    __slots__ = (\"_coro\",)\n\n    def __init__(self, coro: Awaitable[Result[_A]]) -&gt; None:\n        self._coro = coro\n\n    def __del__(self):\n        if hasattr(self._coro, \"close\"):\n            getattr(self._coro, \"close\")()\n\n    def __await__(self):\n        return self._awaitable().__await__()\n\n    async def _awaitable(self) -&gt; Result[_A]:\n        return await self._coro\n\n    def map(self, f: Callable[[_A], _B]) -&gt; AsyncResult[_B]:\n        \"\"\"Transform the success value without executing.\"\"\"\n\n        async def _inner():\n            result = await self._coro\n            return result.map(f)\n\n        return AsyncResult(_inner())\n\n    def bind(self, f: Callable[[_A], Any]) -&gt; AsyncResult:\n        \"\"\"Chain: f receives value. Short-circuits on Err.\n\n        Handles all return types from f:\n        - AsyncResult[B] \u2192 awaited, produces Result\n        - Either[E, B] / Result[B] \u2192 used directly\n        - Awaitable[B] \u2192 awaited, plain value wrapped in Ok\n        - Plain B \u2192 wrapped in Ok\n        \"\"\"\n\n        async def _inner():\n            result = await self._coro\n            match result:\n                case Right(value):\n                    inner = f(value)\n                    if inspect.isawaitable(inner):\n                        inner = await inner\n                    if isinstance(inner, Either):\n                        return inner\n                    return Ok(inner)\n                case _:\n                    return result\n\n        return AsyncResult(_inner())\n\n    def alt(self, f: Callable[[Exception], Exception]) -&gt; AsyncResult[_A]:\n        \"\"\"Transform the error without recovering.\"\"\"\n\n        async def _inner():\n            result = await self._coro\n            match result:\n                case Left(error):\n                    return Err(f(error))\n                case _:\n                    return result\n\n        return AsyncResult(_inner())\n\n    def or_else(self, f: Callable[[Exception], Any]) -&gt; AsyncResult:\n        \"\"\"Recover from error: f receives error. Short-circuits on success.\n\n        Handles all return types from f:\n        - AsyncResult[A] \u2192 awaited, produces Result\n        - Either[E, A] / Result[A] \u2192 used directly\n        - Awaitable[A] \u2192 awaited, plain value wrapped in Ok\n        - Plain A \u2192 wrapped in Ok\n        \"\"\"\n\n        async def _inner():\n            result = await self._coro\n            match result:\n                case Left(error):\n                    inner = f(error)\n                    if inspect.isawaitable(inner):\n                        inner = await inner\n                    if isinstance(inner, Either):\n                        return inner\n                    return Ok(inner)\n                case _:\n                    return result\n\n        return AsyncResult(_inner())\n\n    def then(self, next_result: AsyncResult[_B]) -&gt; AsyncResult[_B]:\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_result)\n\n    def ap(self, other: AsyncResult[_B]) -&gt; AsyncResult[tuple[_A, _B]]:\n        \"\"\"Applicative: run both, tuple the values.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    @classmethod\n    def pure(cls, value: _A) -&gt; AsyncResult[_A]:\n        \"\"\"Lift a plain value into Ok.\"\"\"\n\n        async def _inner():\n            return Ok(value)\n\n        return cls(_inner())\n\n    @classmethod\n    def from_error(cls, error: Exception) -&gt; AsyncResult:\n        \"\"\"Lift an error into Err.\"\"\"\n\n        async def _inner():\n            return Err(error)\n\n        return cls(_inner())\n\n    @classmethod\n    def from_either(cls, either: Either) -&gt; AsyncResult:\n        \"\"\"Lift a sync Either/Result into AsyncResult.\"\"\"\n\n        async def _inner():\n            return either\n\n        return cls(_inner())\n\n    def __repr__(self) -&gt; str:\n        return f\"AsyncResult({self._coro})\"\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.map","title":"<code>map(f)</code>","text":"<p>Transform the success value without executing.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; AsyncResult[_B]:\n    \"\"\"Transform the success value without executing.\"\"\"\n\n    async def _inner():\n        result = await self._coro\n        return result.map(f)\n\n    return AsyncResult(_inner())\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.bind","title":"<code>bind(f)</code>","text":"<p>Chain: f receives value. Short-circuits on Err.</p> <p>Handles all return types from f: - AsyncResult[B] \u2192 awaited, produces Result - Either[E, B] / Result[B] \u2192 used directly - Awaitable[B] \u2192 awaited, plain value wrapped in Ok - Plain B \u2192 wrapped in Ok</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>def bind(self, f: Callable[[_A], Any]) -&gt; AsyncResult:\n    \"\"\"Chain: f receives value. Short-circuits on Err.\n\n    Handles all return types from f:\n    - AsyncResult[B] \u2192 awaited, produces Result\n    - Either[E, B] / Result[B] \u2192 used directly\n    - Awaitable[B] \u2192 awaited, plain value wrapped in Ok\n    - Plain B \u2192 wrapped in Ok\n    \"\"\"\n\n    async def _inner():\n        result = await self._coro\n        match result:\n            case Right(value):\n                inner = f(value)\n                if inspect.isawaitable(inner):\n                    inner = await inner\n                if isinstance(inner, Either):\n                    return inner\n                return Ok(inner)\n            case _:\n                return result\n\n    return AsyncResult(_inner())\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.alt","title":"<code>alt(f)</code>","text":"<p>Transform the error without recovering.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>def alt(self, f: Callable[[Exception], Exception]) -&gt; AsyncResult[_A]:\n    \"\"\"Transform the error without recovering.\"\"\"\n\n    async def _inner():\n        result = await self._coro\n        match result:\n            case Left(error):\n                return Err(f(error))\n            case _:\n                return result\n\n    return AsyncResult(_inner())\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.or_else","title":"<code>or_else(f)</code>","text":"<p>Recover from error: f receives error. Short-circuits on success.</p> <p>Handles all return types from f: - AsyncResult[A] \u2192 awaited, produces Result - Either[E, A] / Result[A] \u2192 used directly - Awaitable[A] \u2192 awaited, plain value wrapped in Ok - Plain A \u2192 wrapped in Ok</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>def or_else(self, f: Callable[[Exception], Any]) -&gt; AsyncResult:\n    \"\"\"Recover from error: f receives error. Short-circuits on success.\n\n    Handles all return types from f:\n    - AsyncResult[A] \u2192 awaited, produces Result\n    - Either[E, A] / Result[A] \u2192 used directly\n    - Awaitable[A] \u2192 awaited, plain value wrapped in Ok\n    - Plain A \u2192 wrapped in Ok\n    \"\"\"\n\n    async def _inner():\n        result = await self._coro\n        match result:\n            case Left(error):\n                inner = f(error)\n                if inspect.isawaitable(inner):\n                    inner = await inner\n                if isinstance(inner, Either):\n                    return inner\n                return Ok(inner)\n            case _:\n                return result\n\n    return AsyncResult(_inner())\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.then","title":"<code>then(next_result)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>def then(self, next_result: AsyncResult[_B]) -&gt; AsyncResult[_B]:\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_result)\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.ap","title":"<code>ap(other)</code>","text":"<p>Applicative: run both, tuple the values.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>def ap(self, other: AsyncResult[_B]) -&gt; AsyncResult[tuple[_A, _B]]:\n    \"\"\"Applicative: run both, tuple the values.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a plain value into Ok.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>@classmethod\ndef pure(cls, value: _A) -&gt; AsyncResult[_A]:\n    \"\"\"Lift a plain value into Ok.\"\"\"\n\n    async def _inner():\n        return Ok(value)\n\n    return cls(_inner())\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.from_error","title":"<code>from_error(error)</code>  <code>classmethod</code>","text":"<p>Lift an error into Err.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>@classmethod\ndef from_error(cls, error: Exception) -&gt; AsyncResult:\n    \"\"\"Lift an error into Err.\"\"\"\n\n    async def _inner():\n        return Err(error)\n\n    return cls(_inner())\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.AsyncResult.from_either","title":"<code>from_either(either)</code>  <code>classmethod</code>","text":"<p>Lift a sync Either/Result into AsyncResult.</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>@classmethod\ndef from_either(cls, either: Either) -&gt; AsyncResult:\n    \"\"\"Lift a sync Either/Result into AsyncResult.\"\"\"\n\n    async def _inner():\n        return either\n\n    return cls(_inner())\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.Try","title":"<code>Try(f)</code>","text":"<p>Decorator: wraps a sync function so exceptions become Err.</p> <p>Successful calls return Ok(value), exceptions return Err(exception). Pattern match the result with Ok(v) / Err(e).</p> Source code in <code>funstruct/monad/result.py</code> <pre><code>def Try(\n    f: Callable[_P, _A],\n) -&gt; Callable[_P, Result[_A]]:\n    \"\"\"Decorator: wraps a sync function so exceptions become Err.\n\n    Successful calls return Ok(value), exceptions return Err(exception).\n    Pattern match the result with Ok(v) / Err(e).\n    \"\"\"\n\n    @wraps(f)\n    def wrapper(*args: _P.args, **kwargs: _P.kwargs) -&gt; Result[_A]:\n        try:\n            return Ok(f(*args, **kwargs))\n        except Exception as e:\n            return Err(e)\n\n    return wrapper\n</code></pre>"},{"location":"monad/result/#funstruct.monad.result.TryAsync","title":"<code>TryAsync(f)</code>","text":"<pre><code>TryAsync(\n    f: Callable[_P, Coroutine[Any, Any, _A]],\n) -&gt; Callable[_P, AsyncResult[_A]]\n</code></pre><pre><code>TryAsync(\n    f: Callable[_P, _A],\n) -&gt; Callable[_P, AsyncResult[_A]]\n</code></pre> <p>Decorator: wraps a function so exceptions become Err.</p> <p>Accepts both sync and async functions. Returns AsyncResult[A] \u2014 a lazy computation. Await at the boundary to get Result[A] (Ok or Err). Compose with .bind(), .map(), .alt() without awaiting.</p> <p>Usage::</p> <pre><code>@TryAsync\nasync def fetch_user(id: int) -&gt; User:\n    resp = await httpx.get(f\"/users/{id}\")\n    if resp.status_code != 200:\n        raise NotFoundError(f\"user {id}\")\n    return User(**resp.json())\n\n@TryAsync\ndef parse_id(raw: str) -&gt; int:\n    return int(raw)\n\n# Build pipeline \u2014 no await needed:\npipeline = (\n    parse_id(\"42\")\n    .bind(lambda id: fetch_user(id))\n    .map(lambda u: u.email)\n)\n\n# Await once at the boundary:\nresult = await pipeline  # Ok(\"alice@example.com\") or Err(...)\n</code></pre> Source code in <code>funstruct/monad/result.py</code> <pre><code>def TryAsync(\n    f: Callable[_P, _A],\n) -&gt; Callable[_P, AsyncResult[_A]]:\n    \"\"\"Decorator: wraps a function so exceptions become Err.\n\n    Accepts both sync and async functions. Returns AsyncResult[A] \u2014 a\n    lazy computation. Await at the boundary to get Result[A] (Ok or Err).\n    Compose with .bind(), .map(), .alt() without awaiting.\n\n    Usage::\n\n        @TryAsync\n        async def fetch_user(id: int) -&gt; User:\n            resp = await httpx.get(f\"/users/{id}\")\n            if resp.status_code != 200:\n                raise NotFoundError(f\"user {id}\")\n            return User(**resp.json())\n\n        @TryAsync\n        def parse_id(raw: str) -&gt; int:\n            return int(raw)\n\n        # Build pipeline \u2014 no await needed:\n        pipeline = (\n            parse_id(\"42\")\n            .bind(lambda id: fetch_user(id))\n            .map(lambda u: u.email)\n        )\n\n        # Await once at the boundary:\n        result = await pipeline  # Ok(\"alice@example.com\") or Err(...)\n    \"\"\"\n\n    @wraps(f)\n    def wrapper(*args: _P.args, **kwargs: _P.kwargs) -&gt; AsyncResult[_A]:\n        async def _inner():\n            try:\n                result = f(*args, **kwargs)\n                if inspect.isawaitable(result):\n                    result = await result\n                return Ok(result)\n            except Exception as e:\n                return Err(e)\n\n        return AsyncResult(_inner())\n\n    return wrapper\n</code></pre>"},{"location":"monad/state/","title":"State","text":"<p>A pure functional state monad. Threads state through a computation without mutation.</p> <p><code>State[S, A]</code> represents a function <code>S -&gt; (S, A)</code>.</p> <pre><code>from funstruct.monad import State\n\nincrement = State(lambda s: (s + 1, s))\n\npipeline = increment &gt;&gt; (lambda a: State(lambda s: (s + 1, a + s)))\n\nnew_state, result = pipeline.run(0)\n# new_state = 2, result = 1\n</code></pre>"},{"location":"monad/state/#api-reference","title":"API Reference","text":""},{"location":"monad/state/#funstruct.monad.state.State","title":"<code>funstruct.monad.state.State</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[_A]</code></p> <p>Pure State monad: <code>S -&gt; (S, A)</code>.</p> <p>Always succeeds. No exceptions, no Result, no error rail.</p> <p>Haskell: <code>State s a</code> Scala:   <code>State[S, A]</code></p> Source code in <code>funstruct/monad/state.py</code> <pre><code>class State(Monad, Generic[_A]):\n    \"\"\"Pure State monad: ``S -&gt; (S, A)``.\n\n    Always succeeds. No exceptions, no Result, no error rail.\n\n    Haskell: ``State s a``\n    Scala:   ``State[S, A]``\n    \"\"\"\n\n    __slots__ = (\"_run\",)\n\n    def __init__(self, run: Callable) -&gt; None:\n        self._run = run\n\n    def run(self, initial_state) -&gt; tuple:\n        \"\"\"Execute with initial state.\n\n        Returns ``(final_state, value)``.\n                &gt;&gt;&gt; State.pure(10).run(\"any\")\n                ('any', 10)\n        \"\"\"\n        return self._run(initial_state)\n\n    def bind(self, f: Callable[[_A], \"State[_B]\"]) -&gt; \"State[_B]\":\n        \"\"\"FlatMap: thread state, pass value to ``f`` returning next State.\n\n        &gt;&gt;&gt; State.pure(1).bind(lambda x: State.pure(x + 10)).run(0)\n        (0, 11)\n        &gt;&gt;&gt; (State.pure(1) &gt;&gt; (lambda x: State.pure(x + 10))).run(0)\n        (0, 11)\n        \"\"\"\n\n        def inner(s):\n            new_s, a = self._run(s)\n            return f(a).run(new_s)\n\n        return State(inner)\n\n    __rshift__ = bind\n\n    def map(self, f: Callable[[_A], _B]) -&gt; \"State[_B]\":\n        \"\"\"Transform the produced value without touching state.\n\n        &gt;&gt;&gt; State.pure(5).map(lambda x: x * 2).run(0)\n        (0, 10)\n        \"\"\"\n\n        def inner(s):\n            new_s, a = self._run(s)\n            return (new_s, f(a))\n\n        return State(inner)\n\n    def ap(self, other) -&gt; \"State\":\n        \"\"\"Applicative ap: run both, tuple the values.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def then(self, next_state: \"State[_B]\") -&gt; \"State[_B]\":\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_state)\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; \"State\":\n        \"\"\"Do-notation via generators. Flattens nested binds.\n\n        Each `yield` extracts the value from a State (state threads through).\n\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield State(lambda s: (s + 1, s))\n        ...     y = yield State(lambda s: (s + 1, s))\n        ...     return x + y\n        &gt;&gt;&gt; State.do(pipeline).run(0)\n        (2, 1)\n        \"\"\"\n\n        def _run(s):\n            gen = gen_fn()\n            try:\n                monadic_val = next(gen)\n                while True:\n                    new_s, result = monadic_val.run(s)\n                    s = new_s\n                    monadic_val = gen.send(result)\n            except StopIteration as e:\n                return (s, e.value)\n\n        return cls(_run)\n\n    @classmethod\n    def pure(cls, value) -&gt; \"State\":\n        \"\"\"Lift a value without modifying state.\n\n        &gt;&gt;&gt; State.pure(\"hello\").run(99)\n        (99, 'hello')\n        \"\"\"\n        return cls(lambda s: (s, value))\n\n    @classmethod\n    def get(cls) -&gt; \"State\":\n        \"\"\"Produce current state as the value.\n\n        &gt;&gt;&gt; State.get().run(42)\n        (42, 42)\n        \"\"\"\n        return cls(lambda s: (s, s))\n\n    @classmethod\n    def modify(cls, f: Callable) -&gt; \"State\":\n        \"\"\"Modify state, produce None.\n\n        &gt;&gt;&gt; State.modify(lambda s: s + 1).run(5)\n        (6, None)\n        \"\"\"\n        return cls(lambda s: (f(s), None))\n\n    def __repr__(self) -&gt; str:\n        return f\"State({self._run})\"\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.run","title":"<code>run(initial_state)</code>","text":"<p>Execute with initial state.</p> <p>Returns <code>(final_state, value)</code>.         &gt;&gt;&gt; State.pure(10).run(\"any\")         ('any', 10)</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>def run(self, initial_state) -&gt; tuple:\n    \"\"\"Execute with initial state.\n\n    Returns ``(final_state, value)``.\n            &gt;&gt;&gt; State.pure(10).run(\"any\")\n            ('any', 10)\n    \"\"\"\n    return self._run(initial_state)\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.bind","title":"<code>bind(f)</code>","text":"<p>FlatMap: thread state, pass value to <code>f</code> returning next State.</p> <p>State.pure(1).bind(lambda x: State.pure(x + 10)).run(0) (0, 11) (State.pure(1) &gt;&gt; (lambda x: State.pure(x + 10))).run(0) (0, 11)</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>def bind(self, f: Callable[[_A], \"State[_B]\"]) -&gt; \"State[_B]\":\n    \"\"\"FlatMap: thread state, pass value to ``f`` returning next State.\n\n    &gt;&gt;&gt; State.pure(1).bind(lambda x: State.pure(x + 10)).run(0)\n    (0, 11)\n    &gt;&gt;&gt; (State.pure(1) &gt;&gt; (lambda x: State.pure(x + 10))).run(0)\n    (0, 11)\n    \"\"\"\n\n    def inner(s):\n        new_s, a = self._run(s)\n        return f(a).run(new_s)\n\n    return State(inner)\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.map","title":"<code>map(f)</code>","text":"<p>Transform the produced value without touching state.</p> <p>State.pure(5).map(lambda x: x * 2).run(0) (0, 10)</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; \"State[_B]\":\n    \"\"\"Transform the produced value without touching state.\n\n    &gt;&gt;&gt; State.pure(5).map(lambda x: x * 2).run(0)\n    (0, 10)\n    \"\"\"\n\n    def inner(s):\n        new_s, a = self._run(s)\n        return (new_s, f(a))\n\n    return State(inner)\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.ap","title":"<code>ap(other)</code>","text":"<p>Applicative ap: run both, tuple the values.</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>def ap(self, other) -&gt; \"State\":\n    \"\"\"Applicative ap: run both, tuple the values.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.then","title":"<code>then(next_state)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>def then(self, next_state: \"State[_B]\") -&gt; \"State[_B]\":\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_state)\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation via generators. Flattens nested binds.</p> <p>Each <code>yield</code> extracts the value from a State (state threads through).</p> <p>def pipeline(): ...     x = yield State(lambda s: (s + 1, s)) ...     y = yield State(lambda s: (s + 1, s)) ...     return x + y State.do(pipeline).run(0) (2, 1)</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; \"State\":\n    \"\"\"Do-notation via generators. Flattens nested binds.\n\n    Each `yield` extracts the value from a State (state threads through).\n\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield State(lambda s: (s + 1, s))\n    ...     y = yield State(lambda s: (s + 1, s))\n    ...     return x + y\n    &gt;&gt;&gt; State.do(pipeline).run(0)\n    (2, 1)\n    \"\"\"\n\n    def _run(s):\n        gen = gen_fn()\n        try:\n            monadic_val = next(gen)\n            while True:\n                new_s, result = monadic_val.run(s)\n                s = new_s\n                monadic_val = gen.send(result)\n        except StopIteration as e:\n            return (s, e.value)\n\n    return cls(_run)\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a value without modifying state.</p> <p>State.pure(\"hello\").run(99) (99, 'hello')</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>@classmethod\ndef pure(cls, value) -&gt; \"State\":\n    \"\"\"Lift a value without modifying state.\n\n    &gt;&gt;&gt; State.pure(\"hello\").run(99)\n    (99, 'hello')\n    \"\"\"\n    return cls(lambda s: (s, value))\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.get","title":"<code>get()</code>  <code>classmethod</code>","text":"<p>Produce current state as the value.</p> <p>State.get().run(42) (42, 42)</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>@classmethod\ndef get(cls) -&gt; \"State\":\n    \"\"\"Produce current state as the value.\n\n    &gt;&gt;&gt; State.get().run(42)\n    (42, 42)\n    \"\"\"\n    return cls(lambda s: (s, s))\n</code></pre>"},{"location":"monad/state/#funstruct.monad.state.State.modify","title":"<code>modify(f)</code>  <code>classmethod</code>","text":"<p>Modify state, produce None.</p> <p>State.modify(lambda s: s + 1).run(5) (6, None)</p> Source code in <code>funstruct/monad/state.py</code> <pre><code>@classmethod\ndef modify(cls, f: Callable) -&gt; \"State\":\n    \"\"\"Modify state, produce None.\n\n    &gt;&gt;&gt; State.modify(lambda s: s + 1).run(5)\n    (6, None)\n    \"\"\"\n    return cls(lambda s: (f(s), None))\n</code></pre>"},{"location":"monad/writer/","title":"Writer","text":""},{"location":"monad/writer/#funstruct.monad.writer","title":"<code>funstruct.monad.writer</code>","text":"<p>Writer monad \u2014 computations with accumulated output.</p> <p>from funstruct.monad.writer import ListWriter w = ListWriter(1, [\"init\"]) w.map(lambda x: x + 10) ListWriter(value=11, output=['init']) w.bind(lambda x: ListWriter(x + 1, [\"inc\"])) ListWriter(value=2, output=['init', 'inc']) ListWriter.pure(99) ListWriter(value=99, output=[])</p>"},{"location":"monad/writer/#funstruct.monad.writer.Writer","title":"<code>Writer</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[_W, _A]</code></p> <p>Writer: (A, W) with output combined via a class-level Monoid.</p> <p>Subclass and set _monoid to use.</p> Source code in <code>funstruct/monad/writer.py</code> <pre><code>class Writer(Monad, Generic[_W, _A]):\n    \"\"\"Writer: (A, W) with output combined via a class-level Monoid.\n\n    Subclass and set _monoid to use.\n    \"\"\"\n\n    _monoid: Monoid\n\n    __slots__ = (\"value\", \"output\")\n\n    def __init__(self, value: _A, output: _W) -&gt; None:\n        object.__setattr__(self, \"value\", value)\n        object.__setattr__(self, \"output\", output)\n\n    def map(self, f: Callable) -&gt; Writer:\n        \"\"\"Transform the value, keep the output.\"\"\"\n        return self.__class__(f(self.value), self.output)\n\n    def bind(self, f: Callable) -&gt; Writer:\n        \"\"\"Chain: run f on the value, combine outputs via monoid.\"\"\"\n        result = f(self.value)\n        return self.__class__(\n            result.value,\n            self._monoid.combine(self.output, result.output),\n        )\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; Writer:\n        \"\"\"Do-notation for Writer. Accumulates output across yields.\n\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield ListWriter(1, [\"init\"])\n        ...     y = yield ListWriter(x + 10, [\"step\"])\n        ...     return x + y\n        &gt;&gt;&gt; ListWriter.do(pipeline)\n        ListWriter(value=12, output=['init', 'step'])\n        \"\"\"\n        gen = gen_fn()\n        try:\n            first = next(gen)\n            output = first.output\n            value = first.value\n            while True:\n                next_w = gen.send(value)\n                output = cls._monoid.combine(output, next_w.output)\n                value = next_w.value\n        except StopIteration as e:\n            return cls(e.value, output)\n\n    @classmethod\n    def pure(cls, value) -&gt; Writer:\n        \"\"\"Lift a value with empty output (uses _monoid.empty).\"\"\"\n        return cls(value, cls._monoid.empty)\n\n    @classmethod\n    def tell(cls, output: _W) -&gt; Writer:\n        \"\"\"Produce output with no meaningful value.\"\"\"\n        return cls(None, output)\n\n    def __eq__(self, other: object) -&gt; bool:\n        match other:\n            case Writer(v, o):\n                return self.value == v and self.output == o\n            case _:\n                return False\n\n    def __repr__(self) -&gt; str:\n        cls = self.__class__.__name__\n        return f\"{cls}(value={repr(self.value)}, output={repr(self.output)})\"\n\n    __match_args__ = (\"value\", \"output\")\n</code></pre>"},{"location":"monad/writer/#funstruct.monad.writer.Writer.map","title":"<code>map(f)</code>","text":"<p>Transform the value, keep the output.</p> Source code in <code>funstruct/monad/writer.py</code> <pre><code>def map(self, f: Callable) -&gt; Writer:\n    \"\"\"Transform the value, keep the output.\"\"\"\n    return self.__class__(f(self.value), self.output)\n</code></pre>"},{"location":"monad/writer/#funstruct.monad.writer.Writer.bind","title":"<code>bind(f)</code>","text":"<p>Chain: run f on the value, combine outputs via monoid.</p> Source code in <code>funstruct/monad/writer.py</code> <pre><code>def bind(self, f: Callable) -&gt; Writer:\n    \"\"\"Chain: run f on the value, combine outputs via monoid.\"\"\"\n    result = f(self.value)\n    return self.__class__(\n        result.value,\n        self._monoid.combine(self.output, result.output),\n    )\n</code></pre>"},{"location":"monad/writer/#funstruct.monad.writer.Writer.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation for Writer. Accumulates output across yields.</p> <p>def pipeline(): ...     x = yield ListWriter(1, [\"init\"]) ...     y = yield ListWriter(x + 10, [\"step\"]) ...     return x + y ListWriter.do(pipeline) ListWriter(value=12, output=['init', 'step'])</p> Source code in <code>funstruct/monad/writer.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; Writer:\n    \"\"\"Do-notation for Writer. Accumulates output across yields.\n\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield ListWriter(1, [\"init\"])\n    ...     y = yield ListWriter(x + 10, [\"step\"])\n    ...     return x + y\n    &gt;&gt;&gt; ListWriter.do(pipeline)\n    ListWriter(value=12, output=['init', 'step'])\n    \"\"\"\n    gen = gen_fn()\n    try:\n        first = next(gen)\n        output = first.output\n        value = first.value\n        while True:\n            next_w = gen.send(value)\n            output = cls._monoid.combine(output, next_w.output)\n            value = next_w.value\n    except StopIteration as e:\n        return cls(e.value, output)\n</code></pre>"},{"location":"monad/writer/#funstruct.monad.writer.Writer.pure","title":"<code>pure(value)</code>  <code>classmethod</code>","text":"<p>Lift a value with empty output (uses _monoid.empty).</p> Source code in <code>funstruct/monad/writer.py</code> <pre><code>@classmethod\ndef pure(cls, value) -&gt; Writer:\n    \"\"\"Lift a value with empty output (uses _monoid.empty).\"\"\"\n    return cls(value, cls._monoid.empty)\n</code></pre>"},{"location":"monad/writer/#funstruct.monad.writer.Writer.tell","title":"<code>tell(output)</code>  <code>classmethod</code>","text":"<p>Produce output with no meaningful value.</p> Source code in <code>funstruct/monad/writer.py</code> <pre><code>@classmethod\ndef tell(cls, output: _W) -&gt; Writer:\n    \"\"\"Produce output with no meaningful value.\"\"\"\n    return cls(None, output)\n</code></pre>"},{"location":"monadtransformer/","title":"Monad Transformers","text":"<p>Transformers combine two monads into one, so you can write a flat pipeline instead of nested pattern matching. Use <code>lift_f</code> to bring an inner monad value into the transformer.</p> <ul> <li>ReaderT \u2014 shared environment + inner monad</li> <li>StateT \u2014 threaded state + inner monad</li> <li>EitherT \u2014 error handling + inner monad</li> <li>OptionT \u2014 optionality + inner monad</li> <li>WriterT \u2014 accumulated output + inner monad</li> </ul>"},{"location":"monadtransformer/#funstruct.monadtransformer","title":"<code>funstruct.monadtransformer</code>","text":"<p>Monad transformer types.</p> <p>\"Transformer\" just means \"adds a new effect to an existing monad.\" - Applicatives compose generically (you can always combine two applicatives into one) - Monads do NOT compose generically (you need a transformer specific to each monad)</p> <p>A monad transformer combines one monad with another monadic effect.</p> <p>Nesting monads is always possible. Transformers make the nesting implicit. You write one flat pipeline; the transformer peels both layers in bind.</p> <p>This is the entire reason transformers exist \u2014 not to enable nesting (you can already do that), but to eliminate the boilerplate of unwrapping nested layers at every step.</p> <p>Pattern: - StateT[F, S, A] = S -&gt; F[(S, A)] \u2014 adds state to F - ReaderT[F, Ctx, A] = Ctx -&gt; F[A] \u2014 adds shared env to F - EitherT[F, E, A] = F[Either[E, A]] \u2014 adds typed errors to F - OptionT[F, A] = F[Option[A]] \u2014 adds \"might not exist\" to F</p>"},{"location":"monadtransformer/either_t/","title":"EitherT","text":""},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t","title":"<code>funstruct.monadtransformer.either_t</code>","text":"<p>EitherT \u2014 monad transformer that adds typed errors to any monad F.</p> <p><code>EitherT[F, E, A]</code> wraps <code>F[Either[E, A]]</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monadtransformer.either_t import EitherT\n&gt;&gt;&gt; from funstruct.monad.option import Option, Some, Nothing\n&gt;&gt;&gt; from funstruct.monad.either import Right, Left\n</code></pre> <p>EitherT over Option \u2014 combines \"might not exist\" with \"might fail\":</p> <pre><code>&gt;&gt;&gt; EitherT(Some(Right(1))).map(lambda x: x + 10).run()\nSome(Right(11))\n&gt;&gt;&gt; EitherT(Some(Left(\"err\"))).map(lambda x: x + 10).run()\nSome(Left('err'))\n&gt;&gt;&gt; EitherT(Nothing()).map(lambda x: x + 10).run()\nNothing()\n</code></pre> <p>bind \u2014 chains that short-circuit on Left OR Nothing:</p> <pre><code>&gt;&gt;&gt; inc = lambda x: EitherT(Some(Right(x + 1)))\n&gt;&gt;&gt; EitherT(Some(Right(1))).bind(inc).bind(inc).run()\nSome(Right(3))\n&gt;&gt;&gt; EitherT(Some(Left(\"stop\"))).bind(inc).run()\nSome(Left('stop'))\n</code></pre> <p>or_else \u2014 recover from Left:</p> <pre><code>&gt;&gt;&gt; EitherT(Some(Left(\"err\"))).or_else(\n...     lambda e: EitherT(Some(Right(f\"recovered: {e}\")))\n... ).run()\nSome(Right('recovered: err'))\n</code></pre> <p>lift_f \u2014 bring F[A] into EitherT (wraps value in Right):</p> <pre><code>&gt;&gt;&gt; EitherT.lift_f(Some(42)).run()\nSome(Right(42))\n</code></pre> <p>pure \u2014 lift_f a plain value into EitherT:</p> <pre><code>&gt;&gt;&gt; EitherT.pure(99, Option).run()\nSome(Right(99))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT","title":"<code>EitherT</code>","text":"<p>               Bases: <code>MonadTransformer</code>, <code>Generic[_F, _E, _A]</code></p> <p>EitherT: <code>F[Either[E, A]]</code>.</p> <p>Adds typed error handling to any monad F. Delegates composition to F's bind/map \u2014 one implementation for all monads.</p> <p>Haskell: <code>EitherT e m a</code> (aka <code>ExceptT</code>) Scala:   <code>EitherT[F[_], E, A]</code></p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>class EitherT(MonadTransformer, Generic[_F, _E, _A]):\n    \"\"\"EitherT: ``F[Either[E, A]]``.\n\n    Adds typed error handling to any monad F. Delegates composition\n    to F's bind/map \u2014 one implementation for all monads.\n\n    Haskell: ``EitherT e m a`` (aka ``ExceptT``)\n    Scala:   ``EitherT[F[_], E, A]``\n    \"\"\"\n\n    __slots__ = (\"_value\",)\n\n    def __init__(self, value) -&gt; None:\n        self._value = value\n\n    def run(self):\n        \"\"\"Unwrap to get F[Either[E, A]].\"\"\"\n        return self._value\n\n    def map(self, f: Callable[[_A], _B]) -&gt; EitherT[_F, _E, _B]:\n        \"\"\"Transform the success value inside F[Either[E, A]].\"\"\"\n        return EitherT(self._value.map(lambda either: either.map(f)))\n\n    def bind(self, f: Callable[[_A], EitherT[_F, _E, _B]]) -&gt; EitherT[_F, _E, _B]:\n        \"\"\"Chain: unwrap Either inside F, apply f if Right.\"\"\"\n\n        def _step(either):\n            match either:\n                case Right(value):\n                    return f(value).run()\n                case _:\n                    return self._value.__class__.pure(either)\n\n        return EitherT(self._value.bind(_step))\n\n    def or_else(self, f: Callable[[_E], EitherT[_F, _E, _A]]) -&gt; EitherT[_F, _E, _A]:\n        \"\"\"Recover from Left: f receives the error, returns a new EitherT.\"\"\"\n\n        def _step(either):\n            match either:\n                case Left(error):\n                    return f(error).run()\n                case _:\n                    return self._value.__class__.pure(either)\n\n        return EitherT(self._value.bind(_step))\n\n    def and_then(self, other: EitherT) -&gt; EitherT:\n        \"\"\"Kleisli composition: value from self feeds into other's context.\"\"\"\n        return self.bind(lambda _: other)\n\n    def ap(self, other: EitherT) -&gt; EitherT:\n        \"\"\"Applicative: run both, tuple the values.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def then(self, next_step: EitherT[_F, _E, _B]) -&gt; EitherT[_F, _E, _B]:\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_step)\n\n    @classmethod\n    def pure(cls, value, monad) -&gt; EitherT:\n        \"\"\"Lift a plain value into EitherT via monad.pure(Right(value)).\"\"\"\n        return cls(monad.pure(Right(value)))\n\n    @classmethod\n    def from_error(cls, error, monad) -&gt; EitherT:\n        \"\"\"Lift an error into EitherT via monad.pure(Left(error)).\"\"\"\n        return cls(monad.pure(Left(error)))\n\n    @classmethod\n    def lift_f(cls, fa) -&gt; EitherT:\n        \"\"\"Lift F[A] into EitherT \u2014 wraps the value in Right.\n\n        Haskell equivalent: ``lift :: m a -&gt; EitherT e m a``\n        \"\"\"\n        return cls(fa.map(lambda a: Right(a)))\n\n    @classmethod\n    def from_either(cls, either: Either, monad) -&gt; EitherT:\n        \"\"\"Lift a plain Either into EitherT.\"\"\"\n        return cls(monad.pure(either))\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; EitherT:\n        \"\"\"Do-notation via generators.\n\n        Each ``yield`` extracts the Right value from an EitherT.\n        Short-circuits on Left (propagated through F).\n\n        &gt;&gt;&gt; from funstruct.monad.option import Some\n        &gt;&gt;&gt; from funstruct.monad.either import Right\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield EitherT(Some(Right(1)))\n        ...     y = yield EitherT(Some(Right(x + 10)))\n        ...     return x + y\n        &gt;&gt;&gt; EitherT.do(pipeline).run()\n        Some(Right(12))\n        \"\"\"\n\n        def _bind_step(either_t, gen):\n            def _step(either):\n                match either:\n                    case Right(value):\n                        try:\n                            next_et = gen.send(value)\n                            return _bind_step(next_et, gen).run()\n                        except StopIteration as e:\n                            return either_t.run().__class__.pure(Right(e.value))\n                    case _:\n                        return either_t.run().__class__.pure(either)\n\n            return EitherT(either_t.run().bind(_step))\n\n        gen = gen_fn()\n        try:\n            first = next(gen)\n        except StopIteration:\n            raise ValueError(\"do block must yield at least once\")\n        return _bind_step(first, gen)\n\n    def __repr__(self) -&gt; str:\n        return f\"EitherT({repr(self._value)})\"\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.run","title":"<code>run()</code>","text":"<p>Unwrap to get F[Either[E, A]].</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>def run(self):\n    \"\"\"Unwrap to get F[Either[E, A]].\"\"\"\n    return self._value\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.map","title":"<code>map(f)</code>","text":"<p>Transform the success value inside F[Either[E, A]].</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; EitherT[_F, _E, _B]:\n    \"\"\"Transform the success value inside F[Either[E, A]].\"\"\"\n    return EitherT(self._value.map(lambda either: either.map(f)))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.bind","title":"<code>bind(f)</code>","text":"<p>Chain: unwrap Either inside F, apply f if Right.</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>def bind(self, f: Callable[[_A], EitherT[_F, _E, _B]]) -&gt; EitherT[_F, _E, _B]:\n    \"\"\"Chain: unwrap Either inside F, apply f if Right.\"\"\"\n\n    def _step(either):\n        match either:\n            case Right(value):\n                return f(value).run()\n            case _:\n                return self._value.__class__.pure(either)\n\n    return EitherT(self._value.bind(_step))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.or_else","title":"<code>or_else(f)</code>","text":"<p>Recover from Left: f receives the error, returns a new EitherT.</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>def or_else(self, f: Callable[[_E], EitherT[_F, _E, _A]]) -&gt; EitherT[_F, _E, _A]:\n    \"\"\"Recover from Left: f receives the error, returns a new EitherT.\"\"\"\n\n    def _step(either):\n        match either:\n            case Left(error):\n                return f(error).run()\n            case _:\n                return self._value.__class__.pure(either)\n\n    return EitherT(self._value.bind(_step))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.and_then","title":"<code>and_then(other)</code>","text":"<p>Kleisli composition: value from self feeds into other's context.</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>def and_then(self, other: EitherT) -&gt; EitherT:\n    \"\"\"Kleisli composition: value from self feeds into other's context.\"\"\"\n    return self.bind(lambda _: other)\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.ap","title":"<code>ap(other)</code>","text":"<p>Applicative: run both, tuple the values.</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>def ap(self, other: EitherT) -&gt; EitherT:\n    \"\"\"Applicative: run both, tuple the values.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.then","title":"<code>then(next_step)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>def then(self, next_step: EitherT[_F, _E, _B]) -&gt; EitherT[_F, _E, _B]:\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_step)\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.pure","title":"<code>pure(value, monad)</code>  <code>classmethod</code>","text":"<p>Lift a plain value into EitherT via monad.pure(Right(value)).</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>@classmethod\ndef pure(cls, value, monad) -&gt; EitherT:\n    \"\"\"Lift a plain value into EitherT via monad.pure(Right(value)).\"\"\"\n    return cls(monad.pure(Right(value)))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.from_error","title":"<code>from_error(error, monad)</code>  <code>classmethod</code>","text":"<p>Lift an error into EitherT via monad.pure(Left(error)).</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>@classmethod\ndef from_error(cls, error, monad) -&gt; EitherT:\n    \"\"\"Lift an error into EitherT via monad.pure(Left(error)).\"\"\"\n    return cls(monad.pure(Left(error)))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.lift_f","title":"<code>lift_f(fa)</code>  <code>classmethod</code>","text":"<p>Lift F[A] into EitherT \u2014 wraps the value in Right.</p> <p>Haskell equivalent: <code>lift :: m a -&gt; EitherT e m a</code></p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>@classmethod\ndef lift_f(cls, fa) -&gt; EitherT:\n    \"\"\"Lift F[A] into EitherT \u2014 wraps the value in Right.\n\n    Haskell equivalent: ``lift :: m a -&gt; EitherT e m a``\n    \"\"\"\n    return cls(fa.map(lambda a: Right(a)))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.from_either","title":"<code>from_either(either, monad)</code>  <code>classmethod</code>","text":"<p>Lift a plain Either into EitherT.</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>@classmethod\ndef from_either(cls, either: Either, monad) -&gt; EitherT:\n    \"\"\"Lift a plain Either into EitherT.\"\"\"\n    return cls(monad.pure(either))\n</code></pre>"},{"location":"monadtransformer/either_t/#funstruct.monadtransformer.either_t.EitherT.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation via generators.</p> <p>Each <code>yield</code> extracts the Right value from an EitherT. Short-circuits on Left (propagated through F).</p> <p>from funstruct.monad.option import Some from funstruct.monad.either import Right def pipeline(): ...     x = yield EitherT(Some(Right(1))) ...     y = yield EitherT(Some(Right(x + 10))) ...     return x + y EitherT.do(pipeline).run() Some(Right(12))</p> Source code in <code>funstruct/monadtransformer/either_t.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; EitherT:\n    \"\"\"Do-notation via generators.\n\n    Each ``yield`` extracts the Right value from an EitherT.\n    Short-circuits on Left (propagated through F).\n\n    &gt;&gt;&gt; from funstruct.monad.option import Some\n    &gt;&gt;&gt; from funstruct.monad.either import Right\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield EitherT(Some(Right(1)))\n    ...     y = yield EitherT(Some(Right(x + 10)))\n    ...     return x + y\n    &gt;&gt;&gt; EitherT.do(pipeline).run()\n    Some(Right(12))\n    \"\"\"\n\n    def _bind_step(either_t, gen):\n        def _step(either):\n            match either:\n                case Right(value):\n                    try:\n                        next_et = gen.send(value)\n                        return _bind_step(next_et, gen).run()\n                    except StopIteration as e:\n                        return either_t.run().__class__.pure(Right(e.value))\n                case _:\n                    return either_t.run().__class__.pure(either)\n\n        return EitherT(either_t.run().bind(_step))\n\n    gen = gen_fn()\n    try:\n        first = next(gen)\n    except StopIteration:\n        raise ValueError(\"do block must yield at least once\")\n    return _bind_step(first, gen)\n</code></pre>"},{"location":"monadtransformer/option_t/","title":"OptionT","text":""},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t","title":"<code>funstruct.monadtransformer.option_t</code>","text":"<p>OptionT \u2014 adds optionality to any monad F.</p> <p>OptionT[F, A] wraps F[Option[A]]. bind short-circuits on Nothing, map transforms the value inside Some.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monadtransformer.option_t import OptionT\n&gt;&gt;&gt; from funstruct.monad.either import Either, Right, Left\n&gt;&gt;&gt; from funstruct.monad.option import Some, Nothing\n</code></pre> <p>Wrapping Either \u2014 a computation that can fail OR be absent:</p> <pre><code>&gt;&gt;&gt; OptionT(Right(Some(1))).map(lambda x: x + 10).run()\nRight(Some(11))\n&gt;&gt;&gt; OptionT(Right(Nothing())).map(lambda x: x + 10).run()\nRight(Nothing())\n&gt;&gt;&gt; OptionT(Left(\"db error\")).map(lambda x: x + 10).run()\nLeft('db error')\n</code></pre> <p>bind chains \u2014 short-circuits on Nothing OR Left:</p> <pre><code>&gt;&gt;&gt; OptionT(Right(Some(1))).bind(\n...     lambda x: OptionT(Right(Some(x * 10)))\n... ).run()\nRight(Some(10))\n&gt;&gt;&gt; OptionT(Right(Nothing())).bind(\n...     lambda x: OptionT(Right(Some(x * 10)))\n... ).run()\nRight(Nothing())\n</code></pre> <p>or_else \u2014 recover from Nothing (not from Left):</p> <pre><code>&gt;&gt;&gt; OptionT(Right(Nothing())).or_else(\n...     lambda: OptionT(Right(Some(99)))\n... ).run()\nRight(Some(99))\n</code></pre> <p>lift_f \u2014 bring F[A] into OptionT (wraps in Some):</p> <pre><code>&gt;&gt;&gt; OptionT.lift_f(Right(42)).run()\nRight(Some(42))\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT","title":"<code>OptionT</code>","text":"<p>               Bases: <code>MonadTransformer</code>, <code>Generic[_F, _A]</code></p> <p>OptionT: <code>F[Option[A]]</code>.</p> <p>Adds optionality/absence to any monad F. bind delegates to F's bind, then pattern-matches the Option inside: Some(v) continues, Nothing short-circuits.</p> <p>Haskell: <code>MaybeT m a</code> Scala:   <code>OptionT[F[_], A]</code></p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>class OptionT(MonadTransformer, Generic[_F, _A]):\n    \"\"\"OptionT: ``F[Option[A]]``.\n\n    Adds optionality/absence to any monad F. bind delegates to F's bind,\n    then pattern-matches the Option inside: Some(v) continues, Nothing\n    short-circuits.\n\n    Haskell: ``MaybeT m a``\n    Scala:   ``OptionT[F[_], A]``\n    \"\"\"\n\n    __slots__ = (\"_run\",)\n\n    def __init__(self, run) -&gt; None:\n        self._run = run\n\n    def run(self):\n        \"\"\"Unwrap to get F[Option[A]].\"\"\"\n        return self._run\n\n    def bind(self, f: Callable[[_A], OptionT[_F, _B]]) -&gt; OptionT[_F, _B]:\n        \"\"\"FlatMap: unwrap F, match Option, chain on Some.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Right\n        &gt;&gt;&gt; from funstruct.monad.option import Some, Nothing\n        &gt;&gt;&gt; OptionT(Right(Some(1))).bind(\n        ...     lambda x: OptionT(Right(Some(x + 10)))\n        ... ).run()\n        Right(Some(11))\n        &gt;&gt;&gt; OptionT(Right(Nothing())).bind(\n        ...     lambda x: OptionT(Right(Some(x + 10)))\n        ... ).run()\n        Right(Nothing())\n        \"\"\"\n\n        def _handle(opt):\n            match opt:\n                case Some(v):\n                    return f(v).run()\n                case _:\n                    return self._run.__class__.pure(Nothing())\n\n        return OptionT(self._run.bind(_handle))\n\n    def map(self, f: Callable[[_A], _B]) -&gt; OptionT[_F, _B]:\n        \"\"\"Transform the value inside Some inside F.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Right\n        &gt;&gt;&gt; from funstruct.monad.option import Some\n        &gt;&gt;&gt; OptionT(Right(Some(5))).map(lambda x: x * 2).run()\n        Right(Some(10))\n        \"\"\"\n\n        def _map_opt(opt):\n            match opt:\n                case Some(v):\n                    return Some(f(v))\n                case _:\n                    return opt\n\n        return OptionT(self._run.map(_map_opt))\n\n    def or_else(self, f: Callable[[], OptionT[_F, _A]]) -&gt; OptionT[_F, _A]:\n        \"\"\"Recover from Nothing: if inner is Nothing, use fallback.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Right\n        &gt;&gt;&gt; from funstruct.monad.option import Some, Nothing\n        &gt;&gt;&gt; OptionT(Right(Nothing())).or_else(\n        ...     lambda: OptionT(Right(Some(99)))\n        ... ).run()\n        Right(Some(99))\n        &gt;&gt;&gt; OptionT(Right(Some(1))).or_else(\n        ...     lambda: OptionT(Right(Some(99)))\n        ... ).run()\n        Right(Some(1))\n        \"\"\"\n\n        def _handle(opt):\n            match opt:\n                case Nothing():\n                    return f().run()\n                case _:\n                    return self._run.__class__.pure(opt)\n\n        return OptionT(self._run.bind(_handle))\n\n    def ap(self, other: OptionT) -&gt; OptionT:\n        \"\"\"Applicative: tuple the values from both.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def and_then(self, other: OptionT) -&gt; OptionT:\n        \"\"\"Kleisli composition: value from self becomes input for other's run.\"\"\"\n        return self.bind(lambda _: other)\n\n    def then(self, next_t: OptionT[_F, _B]) -&gt; OptionT[_F, _B]:\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_t)\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; OptionT:\n        \"\"\"Do-notation via generators. Short-circuits on Nothing.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Right\n        &gt;&gt;&gt; from funstruct.monad.option import Some\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield OptionT(Right(Some(1)))\n        ...     y = yield OptionT(Right(Some(x + 10)))\n        ...     return x + y\n        &gt;&gt;&gt; OptionT.do(pipeline).run()\n        Right(Some(12))\n        \"\"\"\n\n        def _run_do():\n            gen = gen_fn()\n            try:\n                first = next(gen)\n            except StopIteration:\n                raise ValueError(\"do block must yield at least once\")\n\n            def step(opt):\n                match opt:\n                    case Nothing():\n                        return first.run().__class__.pure(Nothing())\n                    case Some(value):\n                        try:\n                            next_t = gen.send(value)\n                            return next_t.run().bind(step)\n                        except StopIteration as e:\n                            return first.run().__class__.pure(Some(e.value))\n\n            return first.run().bind(step)\n\n        return cls(_run_do())\n\n    @classmethod\n    def pure(cls, value, monad: type) -&gt; OptionT:\n        \"\"\"Lift a plain value into OptionT.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n        &gt;&gt;&gt; from funstruct.monad.option import Some\n        &gt;&gt;&gt; OptionT.pure(42, Either).run()\n        Right(Some(42))\n        \"\"\"\n        return cls(monad.pure(Some(value)))\n\n    @classmethod\n    def none(cls, monad: type) -&gt; OptionT:\n        \"\"\"Construct an OptionT holding Nothing.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n        &gt;&gt;&gt; from funstruct.monad.option import Nothing\n        &gt;&gt;&gt; OptionT.none(Either).run()\n        Right(Nothing())\n        \"\"\"\n        return cls(monad.pure(Nothing()))\n\n    @classmethod\n    def lift_f(cls, fa) -&gt; OptionT:\n        \"\"\"Lift F[A] into OptionT \u2014 wraps value in Some.\n\n        Haskell equivalent: ``lift :: m a -&gt; OptionT m a``\n\n        &gt;&gt;&gt; from funstruct.monad.either import Right, Left\n        &gt;&gt;&gt; from funstruct.monad.option import Some\n        &gt;&gt;&gt; OptionT.lift_f(Right(42)).run()\n        Right(Some(42))\n        &gt;&gt;&gt; OptionT.lift_f(Left(\"err\")).run()\n        Left('err')\n        \"\"\"\n        return cls(fa.map(lambda a: Some(a)))\n\n    def __repr__(self) -&gt; str:\n        return f\"OptionT({repr(self._run)})\"\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.run","title":"<code>run()</code>","text":"<p>Unwrap to get F[Option[A]].</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>def run(self):\n    \"\"\"Unwrap to get F[Option[A]].\"\"\"\n    return self._run\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.bind","title":"<code>bind(f)</code>","text":"<p>FlatMap: unwrap F, match Option, chain on Some.</p> <p>from funstruct.monad.either import Right from funstruct.monad.option import Some, Nothing OptionT(Right(Some(1))).bind( ...     lambda x: OptionT(Right(Some(x + 10))) ... ).run() Right(Some(11)) OptionT(Right(Nothing())).bind( ...     lambda x: OptionT(Right(Some(x + 10))) ... ).run() Right(Nothing())</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>def bind(self, f: Callable[[_A], OptionT[_F, _B]]) -&gt; OptionT[_F, _B]:\n    \"\"\"FlatMap: unwrap F, match Option, chain on Some.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Right\n    &gt;&gt;&gt; from funstruct.monad.option import Some, Nothing\n    &gt;&gt;&gt; OptionT(Right(Some(1))).bind(\n    ...     lambda x: OptionT(Right(Some(x + 10)))\n    ... ).run()\n    Right(Some(11))\n    &gt;&gt;&gt; OptionT(Right(Nothing())).bind(\n    ...     lambda x: OptionT(Right(Some(x + 10)))\n    ... ).run()\n    Right(Nothing())\n    \"\"\"\n\n    def _handle(opt):\n        match opt:\n            case Some(v):\n                return f(v).run()\n            case _:\n                return self._run.__class__.pure(Nothing())\n\n    return OptionT(self._run.bind(_handle))\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.map","title":"<code>map(f)</code>","text":"<p>Transform the value inside Some inside F.</p> <p>from funstruct.monad.either import Right from funstruct.monad.option import Some OptionT(Right(Some(5))).map(lambda x: x * 2).run() Right(Some(10))</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; OptionT[_F, _B]:\n    \"\"\"Transform the value inside Some inside F.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Right\n    &gt;&gt;&gt; from funstruct.monad.option import Some\n    &gt;&gt;&gt; OptionT(Right(Some(5))).map(lambda x: x * 2).run()\n    Right(Some(10))\n    \"\"\"\n\n    def _map_opt(opt):\n        match opt:\n            case Some(v):\n                return Some(f(v))\n            case _:\n                return opt\n\n    return OptionT(self._run.map(_map_opt))\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.or_else","title":"<code>or_else(f)</code>","text":"<p>Recover from Nothing: if inner is Nothing, use fallback.</p> <p>from funstruct.monad.either import Right from funstruct.monad.option import Some, Nothing OptionT(Right(Nothing())).or_else( ...     lambda: OptionT(Right(Some(99))) ... ).run() Right(Some(99)) OptionT(Right(Some(1))).or_else( ...     lambda: OptionT(Right(Some(99))) ... ).run() Right(Some(1))</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>def or_else(self, f: Callable[[], OptionT[_F, _A]]) -&gt; OptionT[_F, _A]:\n    \"\"\"Recover from Nothing: if inner is Nothing, use fallback.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Right\n    &gt;&gt;&gt; from funstruct.monad.option import Some, Nothing\n    &gt;&gt;&gt; OptionT(Right(Nothing())).or_else(\n    ...     lambda: OptionT(Right(Some(99)))\n    ... ).run()\n    Right(Some(99))\n    &gt;&gt;&gt; OptionT(Right(Some(1))).or_else(\n    ...     lambda: OptionT(Right(Some(99)))\n    ... ).run()\n    Right(Some(1))\n    \"\"\"\n\n    def _handle(opt):\n        match opt:\n            case Nothing():\n                return f().run()\n            case _:\n                return self._run.__class__.pure(opt)\n\n    return OptionT(self._run.bind(_handle))\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.ap","title":"<code>ap(other)</code>","text":"<p>Applicative: tuple the values from both.</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>def ap(self, other: OptionT) -&gt; OptionT:\n    \"\"\"Applicative: tuple the values from both.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.and_then","title":"<code>and_then(other)</code>","text":"<p>Kleisli composition: value from self becomes input for other's run.</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>def and_then(self, other: OptionT) -&gt; OptionT:\n    \"\"\"Kleisli composition: value from self becomes input for other's run.\"\"\"\n    return self.bind(lambda _: other)\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.then","title":"<code>then(next_t)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>def then(self, next_t: OptionT[_F, _B]) -&gt; OptionT[_F, _B]:\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_t)\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation via generators. Short-circuits on Nothing.</p> <p>from funstruct.monad.either import Right from funstruct.monad.option import Some def pipeline(): ...     x = yield OptionT(Right(Some(1))) ...     y = yield OptionT(Right(Some(x + 10))) ...     return x + y OptionT.do(pipeline).run() Right(Some(12))</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; OptionT:\n    \"\"\"Do-notation via generators. Short-circuits on Nothing.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Right\n    &gt;&gt;&gt; from funstruct.monad.option import Some\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield OptionT(Right(Some(1)))\n    ...     y = yield OptionT(Right(Some(x + 10)))\n    ...     return x + y\n    &gt;&gt;&gt; OptionT.do(pipeline).run()\n    Right(Some(12))\n    \"\"\"\n\n    def _run_do():\n        gen = gen_fn()\n        try:\n            first = next(gen)\n        except StopIteration:\n            raise ValueError(\"do block must yield at least once\")\n\n        def step(opt):\n            match opt:\n                case Nothing():\n                    return first.run().__class__.pure(Nothing())\n                case Some(value):\n                    try:\n                        next_t = gen.send(value)\n                        return next_t.run().bind(step)\n                    except StopIteration as e:\n                        return first.run().__class__.pure(Some(e.value))\n\n        return first.run().bind(step)\n\n    return cls(_run_do())\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.pure","title":"<code>pure(value, monad)</code>  <code>classmethod</code>","text":"<p>Lift a plain value into OptionT.</p> <p>from funstruct.monad.either import Either, Right from funstruct.monad.option import Some OptionT.pure(42, Either).run() Right(Some(42))</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>@classmethod\ndef pure(cls, value, monad: type) -&gt; OptionT:\n    \"\"\"Lift a plain value into OptionT.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n    &gt;&gt;&gt; from funstruct.monad.option import Some\n    &gt;&gt;&gt; OptionT.pure(42, Either).run()\n    Right(Some(42))\n    \"\"\"\n    return cls(monad.pure(Some(value)))\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.none","title":"<code>none(monad)</code>  <code>classmethod</code>","text":"<p>Construct an OptionT holding Nothing.</p> <p>from funstruct.monad.either import Either, Right from funstruct.monad.option import Nothing OptionT.none(Either).run() Right(Nothing())</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>@classmethod\ndef none(cls, monad: type) -&gt; OptionT:\n    \"\"\"Construct an OptionT holding Nothing.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n    &gt;&gt;&gt; from funstruct.monad.option import Nothing\n    &gt;&gt;&gt; OptionT.none(Either).run()\n    Right(Nothing())\n    \"\"\"\n    return cls(monad.pure(Nothing()))\n</code></pre>"},{"location":"monadtransformer/option_t/#funstruct.monadtransformer.option_t.OptionT.lift_f","title":"<code>lift_f(fa)</code>  <code>classmethod</code>","text":"<p>Lift F[A] into OptionT \u2014 wraps value in Some.</p> <p>Haskell equivalent: <code>lift :: m a -&gt; OptionT m a</code></p> <p>from funstruct.monad.either import Right, Left from funstruct.monad.option import Some OptionT.lift_f(Right(42)).run() Right(Some(42)) OptionT.lift_f(Left(\"err\")).run() Left('err')</p> Source code in <code>funstruct/monadtransformer/option_t.py</code> <pre><code>@classmethod\ndef lift_f(cls, fa) -&gt; OptionT:\n    \"\"\"Lift F[A] into OptionT \u2014 wraps value in Some.\n\n    Haskell equivalent: ``lift :: m a -&gt; OptionT m a``\n\n    &gt;&gt;&gt; from funstruct.monad.either import Right, Left\n    &gt;&gt;&gt; from funstruct.monad.option import Some\n    &gt;&gt;&gt; OptionT.lift_f(Right(42)).run()\n    Right(Some(42))\n    &gt;&gt;&gt; OptionT.lift_f(Left(\"err\")).run()\n    Left('err')\n    \"\"\"\n    return cls(fa.map(lambda a: Some(a)))\n</code></pre>"},{"location":"monadtransformer/reader_t/","title":"ReaderT","text":""},{"location":"monadtransformer/reader_t/#api-reference","title":"API Reference","text":""},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT","title":"<code>funstruct.monadtransformer.reader_t.ReaderT</code>","text":"<p>               Bases: <code>MonadTransformer</code>, <code>Generic[_Ctx, _M, _A]</code></p> <p>ReaderT: <code>Ctx -&gt; M[A]</code>.</p> <p>M is the inner monad (StateT, Result, etc.). Composition delegates to M's bind/map/lash \u2014 ReaderT just threads the context to both sides.</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>class ReaderT(MonadTransformer, Generic[_Ctx, _M, _A]):\n    \"\"\"ReaderT: ``Ctx -&gt; M[A]``.\n\n    M is the inner monad (StateT, Result, etc.). Composition delegates to M's\n    bind/map/lash \u2014 ReaderT just threads the context to both sides.\n    \"\"\"\n\n    __slots__ = (\"_run\",)\n\n    def __init__(self, run: Callable) -&gt; None:\n        self._run = run\n\n    def run(self, ctx):\n        \"\"\"Apply context, returning the inner M[A].\"\"\"\n        return self._run(ctx)\n\n    def __call__(self, ctx):\n        \"\"\"Apply context (alias for run).\"\"\"\n        return self._run(ctx)\n\n    def bind(\n        self,\n        f: Callable[[_A], ReaderT[_Ctx, _M, _B]],\n    ) -&gt; ReaderT[_Ctx, _M, _B]:\n        \"\"\"FlatMap: compose via M's bind, threading ctx.\"\"\"\n\n        def inner(ctx):\n            return self._run(ctx).bind(lambda a: f(a).run(ctx))\n\n        return ReaderT(inner)\n\n    def map(self, f: Callable[[_A], _B]) -&gt; ReaderT[_Ctx, _M, _B]:\n        \"\"\"Transform value via M's map.\"\"\"\n\n        def inner(ctx):\n            return self._run(ctx).map(f)\n\n        return ReaderT(inner)\n\n    def or_else(self, f: Callable) -&gt; ReaderT[_Ctx, _M, _A]:\n        \"\"\"Recover from failure via inner monad's or_else.\"\"\"\n\n        def inner(ctx):\n            return self._run(ctx).or_else(lambda err: f(err).run(ctx))\n\n        return ReaderT(inner)\n\n    # ap inherited from Monad (derived from bind + map)\n\n    def and_then(self, other: ReaderT) -&gt; ReaderT:\n        \"\"\"Kleisli composition: output of self becomes input (ctx) of other.\n\n        Short-circuits on inner monad failure.\n        \"\"\"\n        return ReaderT(\n            lambda ctx: self._run(ctx).bind(lambda result: other._run(result))\n        )\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; ReaderT:\n        \"\"\"Do-notation via generators. Flattens nested binds.\n\n        Each `yield` extracts the value from a ReaderT (shared ctx).\n        Short-circuits on inner monad failure.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Right\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield ReaderT(lambda ctx: Right(ctx))\n        ...     y = yield ReaderT(lambda ctx: Right(x + ctx))\n        ...     return y\n        &gt;&gt;&gt; ReaderT.do(pipeline).run(5)\n        Right(10)\n        \"\"\"\n\n        def _run(ctx):\n            gen = gen_fn()\n            try:\n                monadic_val = next(gen)\n            except StopIteration:\n                raise ValueError(\"do block must yield at least once\")\n\n            def step(value):\n                try:\n                    next_val = gen.send(value)\n                    return next_val._run(ctx).bind(step)\n                except StopIteration as e:\n                    return _pure(monadic_val._run(ctx).__class__, e.value)\n\n            return monadic_val._run(ctx).bind(step)\n\n        return cls(_run)\n\n    def then(\n        self,\n        next_step: ReaderT[_Ctx, _M, _B],\n    ) -&gt; ReaderT[_Ctx, _M, _B]:\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_step)\n\n    @classmethod\n    def pure(cls, value, monad) -&gt; ReaderT:\n        \"\"\"Lift a plain value into ReaderT via monad.pure.\"\"\"\n        return cls(lambda _: _pure(monad, value))\n\n    @classmethod\n    def lift_f(cls, m) -&gt; ReaderT:\n        \"\"\"Lift M[A] into ReaderT (ignoring context).\n\n        Haskell equivalent: ``lift :: m a -&gt; ReaderT r m a``\n        \"\"\"\n        return cls(lambda _: m)\n\n    def __repr__(self) -&gt; str:\n        return f\"ReaderT({self._run})\"\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.run","title":"<code>run(ctx)</code>","text":"<p>Apply context, returning the inner M[A].</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>def run(self, ctx):\n    \"\"\"Apply context, returning the inner M[A].\"\"\"\n    return self._run(ctx)\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.__call__","title":"<code>__call__(ctx)</code>","text":"<p>Apply context (alias for run).</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>def __call__(self, ctx):\n    \"\"\"Apply context (alias for run).\"\"\"\n    return self._run(ctx)\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.bind","title":"<code>bind(f)</code>","text":"<p>FlatMap: compose via M's bind, threading ctx.</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>def bind(\n    self,\n    f: Callable[[_A], ReaderT[_Ctx, _M, _B]],\n) -&gt; ReaderT[_Ctx, _M, _B]:\n    \"\"\"FlatMap: compose via M's bind, threading ctx.\"\"\"\n\n    def inner(ctx):\n        return self._run(ctx).bind(lambda a: f(a).run(ctx))\n\n    return ReaderT(inner)\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.map","title":"<code>map(f)</code>","text":"<p>Transform value via M's map.</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; ReaderT[_Ctx, _M, _B]:\n    \"\"\"Transform value via M's map.\"\"\"\n\n    def inner(ctx):\n        return self._run(ctx).map(f)\n\n    return ReaderT(inner)\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.or_else","title":"<code>or_else(f)</code>","text":"<p>Recover from failure via inner monad's or_else.</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>def or_else(self, f: Callable) -&gt; ReaderT[_Ctx, _M, _A]:\n    \"\"\"Recover from failure via inner monad's or_else.\"\"\"\n\n    def inner(ctx):\n        return self._run(ctx).or_else(lambda err: f(err).run(ctx))\n\n    return ReaderT(inner)\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.and_then","title":"<code>and_then(other)</code>","text":"<p>Kleisli composition: output of self becomes input (ctx) of other.</p> <p>Short-circuits on inner monad failure.</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>def and_then(self, other: ReaderT) -&gt; ReaderT:\n    \"\"\"Kleisli composition: output of self becomes input (ctx) of other.\n\n    Short-circuits on inner monad failure.\n    \"\"\"\n    return ReaderT(\n        lambda ctx: self._run(ctx).bind(lambda result: other._run(result))\n    )\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation via generators. Flattens nested binds.</p> <p>Each <code>yield</code> extracts the value from a ReaderT (shared ctx). Short-circuits on inner monad failure.</p> <p>from funstruct.monad.either import Right def pipeline(): ...     x = yield ReaderT(lambda ctx: Right(ctx)) ...     y = yield ReaderT(lambda ctx: Right(x + ctx)) ...     return y ReaderT.do(pipeline).run(5) Right(10)</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; ReaderT:\n    \"\"\"Do-notation via generators. Flattens nested binds.\n\n    Each `yield` extracts the value from a ReaderT (shared ctx).\n    Short-circuits on inner monad failure.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Right\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield ReaderT(lambda ctx: Right(ctx))\n    ...     y = yield ReaderT(lambda ctx: Right(x + ctx))\n    ...     return y\n    &gt;&gt;&gt; ReaderT.do(pipeline).run(5)\n    Right(10)\n    \"\"\"\n\n    def _run(ctx):\n        gen = gen_fn()\n        try:\n            monadic_val = next(gen)\n        except StopIteration:\n            raise ValueError(\"do block must yield at least once\")\n\n        def step(value):\n            try:\n                next_val = gen.send(value)\n                return next_val._run(ctx).bind(step)\n            except StopIteration as e:\n                return _pure(monadic_val._run(ctx).__class__, e.value)\n\n        return monadic_val._run(ctx).bind(step)\n\n    return cls(_run)\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.then","title":"<code>then(next_step)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>def then(\n    self,\n    next_step: ReaderT[_Ctx, _M, _B],\n) -&gt; ReaderT[_Ctx, _M, _B]:\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_step)\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.pure","title":"<code>pure(value, monad)</code>  <code>classmethod</code>","text":"<p>Lift a plain value into ReaderT via monad.pure.</p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>@classmethod\ndef pure(cls, value, monad) -&gt; ReaderT:\n    \"\"\"Lift a plain value into ReaderT via monad.pure.\"\"\"\n    return cls(lambda _: _pure(monad, value))\n</code></pre>"},{"location":"monadtransformer/reader_t/#funstruct.monadtransformer.reader_t.ReaderT.lift_f","title":"<code>lift_f(m)</code>  <code>classmethod</code>","text":"<p>Lift M[A] into ReaderT (ignoring context).</p> <p>Haskell equivalent: <code>lift :: m a -&gt; ReaderT r m a</code></p> Source code in <code>funstruct/monadtransformer/reader_t.py</code> <pre><code>@classmethod\ndef lift_f(cls, m) -&gt; ReaderT:\n    \"\"\"Lift M[A] into ReaderT (ignoring context).\n\n    Haskell equivalent: ``lift :: m a -&gt; ReaderT r m a``\n    \"\"\"\n    return cls(lambda _: m)\n</code></pre>"},{"location":"monadtransformer/state_t/","title":"StateT","text":"<p>A monad transformer that adds state-threading to any monad.</p> <p><code>StateT[S, F, A]</code> represents <code>S -&gt; F[(S, A)]</code>.</p> <pre><code>from funstruct.monad import StateT, Either, Right, Left\n\n\ndef divide_state(divisor):\n    def run(s):\n        if divisor == 0:\n            return Left(\"division by zero\")\n        return Right((s, s / divisor))\n\n    return StateT(run)\n\n\nresult = divide_state(2).run(10)  # Right((10, 5.0))\nresult = divide_state(0).run(10)  # Left('division by zero')\n</code></pre>"},{"location":"monadtransformer/state_t/#api-reference","title":"API Reference","text":""},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t","title":"<code>funstruct.monadtransformer.state_t</code>","text":"<p>StateT \u2014 state monad transformer over any monad.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monadtransformer import StateT\n&gt;&gt;&gt; from funstruct.monad.either import Either, Right, Left\n&gt;&gt;&gt; inc = StateT(lambda s: Right((s + 1, s)))\n&gt;&gt;&gt; inc.run(0)\nRight((1, 0))\n&gt;&gt;&gt; StateT.pure(42, Either).run(0)\nRight((0, 42))\n&gt;&gt;&gt; inc.then(inc).run(0)\nRight((2, 1))\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT","title":"<code>StateT</code>","text":"<p>               Bases: <code>MonadTransformer</code>, <code>Generic[_F, _A]</code></p> <p>Generic state transformer: <code>S -&gt; F[(S, A)]</code>.</p> <p><code>F</code> is the wrapping monad (Result, FutureResult, Maybe, etc.). Composition uses duck-typed <code>.bind()</code> / <code>.map()</code> / <code>.or_else()</code> on whatever <code>F</code> returns \u2014 one implementation for all monads.</p> <p>Haskell: <code>StateT m s a</code> Scala:   <code>StateT[F[_], S, A]</code></p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>class StateT(MonadTransformer, Generic[_F, _A]):\n    \"\"\"Generic state transformer: ``S -&gt; F[(S, A)]``.\n\n    ``F`` is the wrapping monad (Result, FutureResult, Maybe, etc.).\n    Composition uses duck-typed ``.bind()`` / ``.map()`` / ``.or_else()``\n    on whatever ``F`` returns \u2014 one implementation for all monads.\n\n    Haskell: ``StateT m s a``\n    Scala:   ``StateT[F[_], S, A]``\n    \"\"\"\n\n    __slots__ = (\"_run\",)\n\n    def __init__(self, run: Callable) -&gt; None:\n        self._run = run\n\n    def run(self, initial_state):\n        \"\"\"Execute with initial state.\n\n        Returns ``F[(final_state, value)]``.\n        \"\"\"\n        return self._run(initial_state)\n\n    def bind(self, f: Callable[[_A], \"StateT[_F, _B]\"]) -&gt; \"StateT[_F, _B]\":\n        \"\"\"FlatMap: thread state, pass value to ``f``.\n\n        &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n        &gt;&gt;&gt; StateT.pure(1, Option).bind(lambda x: StateT.pure(x + 10, Option)).run(0)\n        Some((0, 11))\n        \"\"\"\n\n        def inner(s):\n            return self._run(s).bind(lambda sa: f(sa[1]).run(sa[0]))\n\n        return StateT(inner)\n\n    def map(self, f: Callable[[_A], _B]) -&gt; \"StateT[_F, _B]\":\n        \"\"\"Transform the produced value without touching state.\n\n        &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n        &gt;&gt;&gt; StateT.pure(5, Option).map(lambda x: x * 2).run(0)\n        Some((0, 10))\n        \"\"\"\n\n        def inner(s):\n            return self._run(s).map(lambda sa: (sa[0], f(sa[1])))\n\n        return StateT(inner)\n\n    def or_else(self, f: Callable) -&gt; \"StateT\":\n        \"\"\"Recover from failure.\n\n        ``f`` receives the error, returns a recovery StateT.\n        Only works when ``F`` supports ``.or_else()``.\n        \"\"\"\n\n        def inner(s):\n            return self._run(s).or_else(lambda err: f(err).run(s))\n\n        return StateT(inner)\n\n    def ap(self, other) -&gt; \"StateT\":\n        \"\"\"Applicative ap: run both, tuple the values.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def and_then(self, other: \"StateT\") -&gt; \"StateT\":\n        \"\"\"Kleisli composition: value from self becomes initial state for other.\n\n        Short-circuits on inner monad failure.\n        \"\"\"\n        return StateT(lambda s: self._run(s).bind(lambda sa: other._run(sa[1])))\n\n    def then(self, next_state: \"StateT[_F, _B]\") -&gt; \"StateT[_F, _B]\":\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_state)\n\n    # Constructors \u2014 monad class passed explicitly, StateT knows nothing about it\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; \"StateT\":\n        \"\"\"Do-notation via generators. Flattens nested binds.\n\n        Each `yield` extracts the value from a StateT.\n        State threads through, short-circuits on inner monad failure.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield StateT(lambda s: Right((s + 1, s)))\n        ...     y = yield StateT(lambda s: Right((s + 1, s)))\n        ...     return x + y\n        &gt;&gt;&gt; StateT.do(pipeline).run(0)\n        Right((2, 1))\n        \"\"\"\n\n        def _run(s):\n            gen = gen_fn()\n            try:\n                first = next(gen)\n            except StopIteration:\n                raise ValueError(\"do block must yield at least once\")\n\n            def step(sa):\n                new_s, value = sa\n                try:\n                    next_val = gen.send(value)\n                    return next_val.run(new_s).bind(step)\n                except StopIteration as e:\n                    monad_cls = first.run(s).__class__\n                    return _pure(monad_cls, (new_s, e.value))\n\n            return first.run(s).bind(step)\n\n        return cls(_run)\n\n    @classmethod\n    def pure(cls, value, monad: type) -&gt; \"StateT\":\n        \"\"\"Lift a value into StateT. State unchanged.\n\n        &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n        &gt;&gt;&gt; StateT.pure(\"hello\", Option).run(99)\n        Some((99, 'hello'))\n        \"\"\"\n        return cls(lambda s: _pure(monad, (s, value)))\n\n    @classmethod\n    def fail(cls, err, monad) -&gt; \"StateT\":\n        \"\"\"Lift an error. Uses ``monad.from_error``.\"\"\"\n        return cls(lambda _: monad.from_error(err))\n\n    @classmethod\n    def get(cls, monad) -&gt; \"StateT\":\n        \"\"\"Produce current state as the value.\n\n        &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n        &gt;&gt;&gt; StateT.get(Option).run(42)\n        Some((42, 42))\n        \"\"\"\n        return cls(lambda s: _pure(monad, (s, s)))\n\n    @classmethod\n    def modify(cls, f: Callable, monad) -&gt; \"StateT\":\n        \"\"\"Modify state, produce None.\n\n        &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n        &gt;&gt;&gt; StateT.modify(lambda s: s + 1, Option).run(5)\n        Some((6, None))\n        \"\"\"\n        return cls(lambda s: _pure(monad, (f(s), None)))\n\n    @classmethod\n    def lift_f(cls, inner) -&gt; \"StateT\":\n        \"\"\"Lift F[A] into StateT \u2014 state unchanged.\n\n        Haskell equivalent: ``lift :: m a -&gt; StateT s m a``\n\n        &gt;&gt;&gt; from funstruct.monad.option import Option, Some, Nothing\n        &gt;&gt;&gt; StateT.lift_f(Some(42)).run(0)\n        Some((0, 42))\n        &gt;&gt;&gt; StateT.lift_f(Nothing()).run(0)\n        Nothing()\n        \"\"\"\n        return cls(lambda s: inner.map(lambda a: (s, a)))\n\n    def __repr__(self) -&gt; str:\n        return f\"StateT({self._run})\"\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.run","title":"<code>run(initial_state)</code>","text":"<p>Execute with initial state.</p> <p>Returns <code>F[(final_state, value)]</code>.</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>def run(self, initial_state):\n    \"\"\"Execute with initial state.\n\n    Returns ``F[(final_state, value)]``.\n    \"\"\"\n    return self._run(initial_state)\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.bind","title":"<code>bind(f)</code>","text":"<p>FlatMap: thread state, pass value to <code>f</code>.</p> <p>from funstruct.monad.option import Option, Some StateT.pure(1, Option).bind(lambda x: StateT.pure(x + 10, Option)).run(0) Some((0, 11))</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>def bind(self, f: Callable[[_A], \"StateT[_F, _B]\"]) -&gt; \"StateT[_F, _B]\":\n    \"\"\"FlatMap: thread state, pass value to ``f``.\n\n    &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n    &gt;&gt;&gt; StateT.pure(1, Option).bind(lambda x: StateT.pure(x + 10, Option)).run(0)\n    Some((0, 11))\n    \"\"\"\n\n    def inner(s):\n        return self._run(s).bind(lambda sa: f(sa[1]).run(sa[0]))\n\n    return StateT(inner)\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.map","title":"<code>map(f)</code>","text":"<p>Transform the produced value without touching state.</p> <p>from funstruct.monad.option import Option, Some StateT.pure(5, Option).map(lambda x: x * 2).run(0) Some((0, 10))</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; \"StateT[_F, _B]\":\n    \"\"\"Transform the produced value without touching state.\n\n    &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n    &gt;&gt;&gt; StateT.pure(5, Option).map(lambda x: x * 2).run(0)\n    Some((0, 10))\n    \"\"\"\n\n    def inner(s):\n        return self._run(s).map(lambda sa: (sa[0], f(sa[1])))\n\n    return StateT(inner)\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.or_else","title":"<code>or_else(f)</code>","text":"<p>Recover from failure.</p> <p><code>f</code> receives the error, returns a recovery StateT. Only works when <code>F</code> supports <code>.or_else()</code>.</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>def or_else(self, f: Callable) -&gt; \"StateT\":\n    \"\"\"Recover from failure.\n\n    ``f`` receives the error, returns a recovery StateT.\n    Only works when ``F`` supports ``.or_else()``.\n    \"\"\"\n\n    def inner(s):\n        return self._run(s).or_else(lambda err: f(err).run(s))\n\n    return StateT(inner)\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.ap","title":"<code>ap(other)</code>","text":"<p>Applicative ap: run both, tuple the values.</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>def ap(self, other) -&gt; \"StateT\":\n    \"\"\"Applicative ap: run both, tuple the values.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.and_then","title":"<code>and_then(other)</code>","text":"<p>Kleisli composition: value from self becomes initial state for other.</p> <p>Short-circuits on inner monad failure.</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>def and_then(self, other: \"StateT\") -&gt; \"StateT\":\n    \"\"\"Kleisli composition: value from self becomes initial state for other.\n\n    Short-circuits on inner monad failure.\n    \"\"\"\n    return StateT(lambda s: self._run(s).bind(lambda sa: other._run(sa[1])))\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.then","title":"<code>then(next_state)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>def then(self, next_state: \"StateT[_F, _B]\") -&gt; \"StateT[_F, _B]\":\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_state)\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation via generators. Flattens nested binds.</p> <p>Each <code>yield</code> extracts the value from a StateT. State threads through, short-circuits on inner monad failure.</p> <p>from funstruct.monad.either import Either, Right def pipeline(): ...     x = yield StateT(lambda s: Right((s + 1, s))) ...     y = yield StateT(lambda s: Right((s + 1, s))) ...     return x + y StateT.do(pipeline).run(0) Right((2, 1))</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; \"StateT\":\n    \"\"\"Do-notation via generators. Flattens nested binds.\n\n    Each `yield` extracts the value from a StateT.\n    State threads through, short-circuits on inner monad failure.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield StateT(lambda s: Right((s + 1, s)))\n    ...     y = yield StateT(lambda s: Right((s + 1, s)))\n    ...     return x + y\n    &gt;&gt;&gt; StateT.do(pipeline).run(0)\n    Right((2, 1))\n    \"\"\"\n\n    def _run(s):\n        gen = gen_fn()\n        try:\n            first = next(gen)\n        except StopIteration:\n            raise ValueError(\"do block must yield at least once\")\n\n        def step(sa):\n            new_s, value = sa\n            try:\n                next_val = gen.send(value)\n                return next_val.run(new_s).bind(step)\n            except StopIteration as e:\n                monad_cls = first.run(s).__class__\n                return _pure(monad_cls, (new_s, e.value))\n\n        return first.run(s).bind(step)\n\n    return cls(_run)\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.pure","title":"<code>pure(value, monad)</code>  <code>classmethod</code>","text":"<p>Lift a value into StateT. State unchanged.</p> <p>from funstruct.monad.option import Option, Some StateT.pure(\"hello\", Option).run(99) Some((99, 'hello'))</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>@classmethod\ndef pure(cls, value, monad: type) -&gt; \"StateT\":\n    \"\"\"Lift a value into StateT. State unchanged.\n\n    &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n    &gt;&gt;&gt; StateT.pure(\"hello\", Option).run(99)\n    Some((99, 'hello'))\n    \"\"\"\n    return cls(lambda s: _pure(monad, (s, value)))\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.fail","title":"<code>fail(err, monad)</code>  <code>classmethod</code>","text":"<p>Lift an error. Uses <code>monad.from_error</code>.</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>@classmethod\ndef fail(cls, err, monad) -&gt; \"StateT\":\n    \"\"\"Lift an error. Uses ``monad.from_error``.\"\"\"\n    return cls(lambda _: monad.from_error(err))\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.get","title":"<code>get(monad)</code>  <code>classmethod</code>","text":"<p>Produce current state as the value.</p> <p>from funstruct.monad.option import Option, Some StateT.get(Option).run(42) Some((42, 42))</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>@classmethod\ndef get(cls, monad) -&gt; \"StateT\":\n    \"\"\"Produce current state as the value.\n\n    &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n    &gt;&gt;&gt; StateT.get(Option).run(42)\n    Some((42, 42))\n    \"\"\"\n    return cls(lambda s: _pure(monad, (s, s)))\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.modify","title":"<code>modify(f, monad)</code>  <code>classmethod</code>","text":"<p>Modify state, produce None.</p> <p>from funstruct.monad.option import Option, Some StateT.modify(lambda s: s + 1, Option).run(5) Some((6, None))</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>@classmethod\ndef modify(cls, f: Callable, monad) -&gt; \"StateT\":\n    \"\"\"Modify state, produce None.\n\n    &gt;&gt;&gt; from funstruct.monad.option import Option, Some\n    &gt;&gt;&gt; StateT.modify(lambda s: s + 1, Option).run(5)\n    Some((6, None))\n    \"\"\"\n    return cls(lambda s: _pure(monad, (f(s), None)))\n</code></pre>"},{"location":"monadtransformer/state_t/#funstruct.monadtransformer.state_t.StateT.lift_f","title":"<code>lift_f(inner)</code>  <code>classmethod</code>","text":"<p>Lift F[A] into StateT \u2014 state unchanged.</p> <p>Haskell equivalent: <code>lift :: m a -&gt; StateT s m a</code></p> <p>from funstruct.monad.option import Option, Some, Nothing StateT.lift_f(Some(42)).run(0) Some((0, 42)) StateT.lift_f(Nothing()).run(0) Nothing()</p> Source code in <code>funstruct/monadtransformer/state_t.py</code> <pre><code>@classmethod\ndef lift_f(cls, inner) -&gt; \"StateT\":\n    \"\"\"Lift F[A] into StateT \u2014 state unchanged.\n\n    Haskell equivalent: ``lift :: m a -&gt; StateT s m a``\n\n    &gt;&gt;&gt; from funstruct.monad.option import Option, Some, Nothing\n    &gt;&gt;&gt; StateT.lift_f(Some(42)).run(0)\n    Some((0, 42))\n    &gt;&gt;&gt; StateT.lift_f(Nothing()).run(0)\n    Nothing()\n    \"\"\"\n    return cls(lambda s: inner.map(lambda a: (s, a)))\n</code></pre>"},{"location":"monadtransformer/writer_t/","title":"WriterT","text":""},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t","title":"<code>funstruct.monadtransformer.writer_t</code>","text":"<p>WriterT \u2014 writer monad transformer over any monad.</p> <p>Adds accumulated output/logging to any monad F. WriterT[F, W, A] wraps F[(A, W)].</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.monadtransformer.writer_t import WriterT\n&gt;&gt;&gt; from funstruct.monad.either import Either, Right, Left\n&gt;&gt;&gt; from funstruct.typeclasses import Monoid\n</code></pre> <pre><code>&gt;&gt;&gt; list_monoid = Monoid(typ=list, combine=lambda a, b: a + b, empty=[])\n&gt;&gt;&gt; class LogT(WriterT):\n...     _monoid = list_monoid\n</code></pre> <p>bind accumulates output:</p> <pre><code>&gt;&gt;&gt; w = LogT.pure(1, Either).bind(lambda x: LogT(Right((x + 1, [\"inc\"]))))\n&gt;&gt;&gt; w.run()\nRight((2, ['inc']))\n</code></pre> <p>map doesn't touch output:</p> <pre><code>&gt;&gt;&gt; LogT(Right((5, [\"init\"]))).map(lambda x: x * 2).run()\nRight((10, ['init']))\n</code></pre> <p>tell produces output:</p> <pre><code>&gt;&gt;&gt; LogT.tell([\"hello\"], Either).run()\nRight((None, ['hello']))\n</code></pre> <p>lift_f wraps F[A] with empty output:</p> <pre><code>&gt;&gt;&gt; LogT.lift_f(Right(42)).run()\nRight((42, []))\n</code></pre> <p>short-circuits on Left:</p> <pre><code>&gt;&gt;&gt; LogT(Left(\"err\")).map(lambda x: x + 1).run()\nLeft('err')\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT","title":"<code>WriterT</code>","text":"<p>               Bases: <code>MonadTransformer</code>, <code>Generic[_F, _W, _A]</code></p> <p>Generic writer transformer: <code>F[(A, W)]</code>.</p> <p><code>F</code> is the wrapping monad (Either, Option, etc.). <code>W</code> is the output type, combined via a class-level Monoid.</p> <p>Subclass and set <code>_monoid</code> to use.</p> <p>Haskell: <code>WriterT w m a</code> Scala:   <code>WriterT[F[_], W, A]</code></p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>class WriterT(MonadTransformer, Generic[_F, _W, _A]):\n    \"\"\"Generic writer transformer: ``F[(A, W)]``.\n\n    ``F`` is the wrapping monad (Either, Option, etc.).\n    ``W`` is the output type, combined via a class-level Monoid.\n\n    Subclass and set ``_monoid`` to use.\n\n    Haskell: ``WriterT w m a``\n    Scala:   ``WriterT[F[_], W, A]``\n    \"\"\"\n\n    _monoid: Monoid\n\n    __slots__ = (\"_run\",)\n\n    def __init__(self, run) -&gt; None:\n        self._run = run\n\n    def run(self):\n        \"\"\"Execute, returning F[(A, W)].\"\"\"\n        return self._run\n\n    def map(self, f: Callable[[_A], _B]) -&gt; WriterT[_F, _W, _B]:\n        \"\"\"Transform the value, keep the output.\"\"\"\n        cls = self.__class__\n        return cls(self._run.map(lambda aw: (f(aw[0]), aw[1])))\n\n    def bind(self, f: Callable[[_A], WriterT]) -&gt; WriterT:\n        \"\"\"Chain: run f on the value, combine outputs via monoid.\"\"\"\n        cls = self.__class__\n        monoid = cls._monoid\n\n        def _step(aw):\n            a, w1 = aw\n            return f(a).run().map(lambda bw: (bw[0], monoid.combine(w1, bw[1])))\n\n        return cls(self._run.bind(_step))\n\n    def then(self, next_wt: WriterT) -&gt; WriterT:\n        \"\"\"Sequence: run self, discard value, run next.\"\"\"\n        return self.bind(lambda _: next_wt)\n\n    def ap(self, other: WriterT) -&gt; WriterT:\n        \"\"\"Applicative: run both, tuple the values, combine outputs.\"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def or_else(self, f: Callable) -&gt; WriterT:\n        \"\"\"Recover from failure via inner monad's or_else.\"\"\"\n        cls = self.__class__\n        return cls(self._run.or_else(lambda err: f(err).run()))\n\n    @classmethod\n    def pure(cls, value, monad: type) -&gt; WriterT:\n        \"\"\"Lift a value with empty output.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n        &gt;&gt;&gt; from funstruct.typeclasses._monoid import Monoid\n        &gt;&gt;&gt; list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[])\n        &gt;&gt;&gt; class LT(WriterT):\n        ...     _monoid = list_m\n        &gt;&gt;&gt; LT.pure(42, Either).run()\n        Right((42, []))\n        \"\"\"\n        return cls(monad.pure((value, cls._monoid.empty)))\n\n    @classmethod\n    def tell(cls, output, monad: type) -&gt; WriterT:\n        \"\"\"Produce output with no meaningful value.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n        &gt;&gt;&gt; from funstruct.typeclasses._monoid import Monoid\n        &gt;&gt;&gt; list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[])\n        &gt;&gt;&gt; class LT(WriterT):\n        ...     _monoid = list_m\n        &gt;&gt;&gt; LT.tell([\"hello\"], Either).run()\n        Right((None, ['hello']))\n        \"\"\"\n        return cls(monad.pure((None, output)))\n\n    @classmethod\n    def lift_f(cls, fa) -&gt; WriterT:\n        \"\"\"Lift F[A] into WriterT \u2014 output is empty.\n\n        Haskell equivalent: ``lift :: m a -&gt; WriterT w m a``\n        \"\"\"\n        return cls(fa.map(lambda a: (a, cls._monoid.empty)))\n\n    @classmethod\n    def do(cls, gen_fn) -&gt; WriterT:\n        \"\"\"Do-notation via generators. Accumulates output across yields.\n\n        &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n        &gt;&gt;&gt; from funstruct.typeclasses import Monoid\n        &gt;&gt;&gt; list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[])\n        &gt;&gt;&gt; class LogT(WriterT):\n        ...     _monoid = list_m\n        &gt;&gt;&gt; def pipeline():\n        ...     x = yield LogT(Right((1, [\"init\"])))\n        ...     y = yield LogT(Right((x + 10, [\"step\"])))\n        ...     return x + y\n        &gt;&gt;&gt; LogT.do(pipeline).run()\n        Right((12, ['init', 'step']))\n        \"\"\"\n\n        def _unwrap(first_run):\n            gen = gen_fn()\n            next(gen)\n\n            monoid = cls._monoid\n\n            def step(aw):\n                a, w_acc = aw\n                try:\n                    next_wt = gen.send(a)\n                    return next_wt.run().bind(\n                        lambda bw: step((bw[0], monoid.combine(w_acc, bw[1])))\n                    )\n                except StopIteration as e:\n                    return first_run.__class__.pure((e.value, w_acc))\n\n            return step\n\n        def _run_do():\n            gen = gen_fn()\n            first_wt = next(gen)\n            first_fa = first_wt.run()\n            return first_fa.bind(_unwrap(first_fa))\n\n        return cls(_run_do())\n\n    def and_then(self, other: WriterT) -&gt; WriterT:\n        \"\"\"Kleisli composition: value from self feeds into other.\"\"\"\n        cls = self.__class__\n        monoid = cls._monoid\n\n        def _step(aw):\n            a, w1 = aw\n            return other.run().map(lambda bw: (bw[0], monoid.combine(w1, bw[1])))\n\n        return cls(self._run.bind(_step))\n\n    def __repr__(self) -&gt; str:\n        return f\"WriterT({self._run})\"\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.run","title":"<code>run()</code>","text":"<p>Execute, returning F[(A, W)].</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>def run(self):\n    \"\"\"Execute, returning F[(A, W)].\"\"\"\n    return self._run\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.map","title":"<code>map(f)</code>","text":"<p>Transform the value, keep the output.</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>def map(self, f: Callable[[_A], _B]) -&gt; WriterT[_F, _W, _B]:\n    \"\"\"Transform the value, keep the output.\"\"\"\n    cls = self.__class__\n    return cls(self._run.map(lambda aw: (f(aw[0]), aw[1])))\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.bind","title":"<code>bind(f)</code>","text":"<p>Chain: run f on the value, combine outputs via monoid.</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>def bind(self, f: Callable[[_A], WriterT]) -&gt; WriterT:\n    \"\"\"Chain: run f on the value, combine outputs via monoid.\"\"\"\n    cls = self.__class__\n    monoid = cls._monoid\n\n    def _step(aw):\n        a, w1 = aw\n        return f(a).run().map(lambda bw: (bw[0], monoid.combine(w1, bw[1])))\n\n    return cls(self._run.bind(_step))\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.then","title":"<code>then(next_wt)</code>","text":"<p>Sequence: run self, discard value, run next.</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>def then(self, next_wt: WriterT) -&gt; WriterT:\n    \"\"\"Sequence: run self, discard value, run next.\"\"\"\n    return self.bind(lambda _: next_wt)\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.ap","title":"<code>ap(other)</code>","text":"<p>Applicative: run both, tuple the values, combine outputs.</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>def ap(self, other: WriterT) -&gt; WriterT:\n    \"\"\"Applicative: run both, tuple the values, combine outputs.\"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.or_else","title":"<code>or_else(f)</code>","text":"<p>Recover from failure via inner monad's or_else.</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>def or_else(self, f: Callable) -&gt; WriterT:\n    \"\"\"Recover from failure via inner monad's or_else.\"\"\"\n    cls = self.__class__\n    return cls(self._run.or_else(lambda err: f(err).run()))\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.pure","title":"<code>pure(value, monad)</code>  <code>classmethod</code>","text":"<p>Lift a value with empty output.</p> <p>from funstruct.monad.either import Either, Right from funstruct.typeclasses._monoid import Monoid list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[]) class LT(WriterT): ...     _monoid = list_m LT.pure(42, Either).run() Right((42, []))</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>@classmethod\ndef pure(cls, value, monad: type) -&gt; WriterT:\n    \"\"\"Lift a value with empty output.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n    &gt;&gt;&gt; from funstruct.typeclasses._monoid import Monoid\n    &gt;&gt;&gt; list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[])\n    &gt;&gt;&gt; class LT(WriterT):\n    ...     _monoid = list_m\n    &gt;&gt;&gt; LT.pure(42, Either).run()\n    Right((42, []))\n    \"\"\"\n    return cls(monad.pure((value, cls._monoid.empty)))\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.tell","title":"<code>tell(output, monad)</code>  <code>classmethod</code>","text":"<p>Produce output with no meaningful value.</p> <p>from funstruct.monad.either import Either, Right from funstruct.typeclasses._monoid import Monoid list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[]) class LT(WriterT): ...     _monoid = list_m LT.tell([\"hello\"], Either).run() Right((None, ['hello']))</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>@classmethod\ndef tell(cls, output, monad: type) -&gt; WriterT:\n    \"\"\"Produce output with no meaningful value.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n    &gt;&gt;&gt; from funstruct.typeclasses._monoid import Monoid\n    &gt;&gt;&gt; list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[])\n    &gt;&gt;&gt; class LT(WriterT):\n    ...     _monoid = list_m\n    &gt;&gt;&gt; LT.tell([\"hello\"], Either).run()\n    Right((None, ['hello']))\n    \"\"\"\n    return cls(monad.pure((None, output)))\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.lift_f","title":"<code>lift_f(fa)</code>  <code>classmethod</code>","text":"<p>Lift F[A] into WriterT \u2014 output is empty.</p> <p>Haskell equivalent: <code>lift :: m a -&gt; WriterT w m a</code></p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>@classmethod\ndef lift_f(cls, fa) -&gt; WriterT:\n    \"\"\"Lift F[A] into WriterT \u2014 output is empty.\n\n    Haskell equivalent: ``lift :: m a -&gt; WriterT w m a``\n    \"\"\"\n    return cls(fa.map(lambda a: (a, cls._monoid.empty)))\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.do","title":"<code>do(gen_fn)</code>  <code>classmethod</code>","text":"<p>Do-notation via generators. Accumulates output across yields.</p> <p>from funstruct.monad.either import Either, Right from funstruct.typeclasses import Monoid list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[]) class LogT(WriterT): ...     _monoid = list_m def pipeline(): ...     x = yield LogT(Right((1, [\"init\"]))) ...     y = yield LogT(Right((x + 10, [\"step\"]))) ...     return x + y LogT.do(pipeline).run() Right((12, ['init', 'step']))</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>@classmethod\ndef do(cls, gen_fn) -&gt; WriterT:\n    \"\"\"Do-notation via generators. Accumulates output across yields.\n\n    &gt;&gt;&gt; from funstruct.monad.either import Either, Right\n    &gt;&gt;&gt; from funstruct.typeclasses import Monoid\n    &gt;&gt;&gt; list_m = Monoid(typ=list, combine=lambda a, b: a + b, empty=[])\n    &gt;&gt;&gt; class LogT(WriterT):\n    ...     _monoid = list_m\n    &gt;&gt;&gt; def pipeline():\n    ...     x = yield LogT(Right((1, [\"init\"])))\n    ...     y = yield LogT(Right((x + 10, [\"step\"])))\n    ...     return x + y\n    &gt;&gt;&gt; LogT.do(pipeline).run()\n    Right((12, ['init', 'step']))\n    \"\"\"\n\n    def _unwrap(first_run):\n        gen = gen_fn()\n        next(gen)\n\n        monoid = cls._monoid\n\n        def step(aw):\n            a, w_acc = aw\n            try:\n                next_wt = gen.send(a)\n                return next_wt.run().bind(\n                    lambda bw: step((bw[0], monoid.combine(w_acc, bw[1])))\n                )\n            except StopIteration as e:\n                return first_run.__class__.pure((e.value, w_acc))\n\n        return step\n\n    def _run_do():\n        gen = gen_fn()\n        first_wt = next(gen)\n        first_fa = first_wt.run()\n        return first_fa.bind(_unwrap(first_fa))\n\n    return cls(_run_do())\n</code></pre>"},{"location":"monadtransformer/writer_t/#funstruct.monadtransformer.writer_t.WriterT.and_then","title":"<code>and_then(other)</code>","text":"<p>Kleisli composition: value from self feeds into other.</p> Source code in <code>funstruct/monadtransformer/writer_t.py</code> <pre><code>def and_then(self, other: WriterT) -&gt; WriterT:\n    \"\"\"Kleisli composition: value from self feeds into other.\"\"\"\n    cls = self.__class__\n    monoid = cls._monoid\n\n    def _step(aw):\n        a, w1 = aw\n        return other.run().map(lambda bw: (bw[0], monoid.combine(w1, bw[1])))\n\n    return cls(self._run.bind(_step))\n</code></pre>"},{"location":"typeclass/","title":"Type Classes","text":"<p>Abstract type definitions that describe what operations a type supports. A type class defines the interface; concrete types provide the implementation.</p> <pre><code>Semigroup               Functor\n    \u2502                      \u2502\n Monoid                Applicative\n                           \u2502\n                         Monad\n                           \u2502\n                    MonadTransformer\n</code></pre> <ul> <li>Semigroup</li> <li>Monoid</li> <li>Functor</li> <li>Applicative</li> <li>Monad</li> <li>MonadTransformer</li> </ul>"},{"location":"typeclass/applicative/","title":"Applicative","text":""},{"location":"typeclass/applicative/#funstruct.typeclasses.Applicative","title":"<code>funstruct.typeclasses.Applicative</code>","text":"<p>               Bases: <code>Functor[_A]</code></p> <p>Combine independent computations.</p> Type parameter <p>_A: The value type inside the applicative.</p> Source code in <code>funstruct/typeclasses/_applicative.py</code> <pre><code>class Applicative(Functor[_A]):\n    \"\"\"Combine independent computations.\n\n    Type parameter:\n        _A: The value type inside the applicative.\n    \"\"\"\n\n    @classmethod\n    @abstractmethod\n    def pure(cls, value: _A, *args, **kwargs) -&gt; Applicative[_A]:\n        \"\"\"Lift a value into the context.\"\"\"\n        ...\n\n    @abstractmethod\n    def ap(self, other: Applicative[_B]) -&gt; Applicative[tuple[_A, _B]]: ...\n\n    def __add__(self, other: Applicative[_B]) -&gt; Applicative[tuple[_A, _B]]:\n        \"\"\"Alias for ap.\"\"\"\n        return self.ap(other)\n</code></pre>"},{"location":"typeclass/applicative/#funstruct.typeclasses.Applicative.pure","title":"<code>pure(value, *args, **kwargs)</code>  <code>abstractmethod</code> <code>classmethod</code>","text":"<p>Lift a value into the context.</p> Source code in <code>funstruct/typeclasses/_applicative.py</code> <pre><code>@classmethod\n@abstractmethod\ndef pure(cls, value: _A, *args, **kwargs) -&gt; Applicative[_A]:\n    \"\"\"Lift a value into the context.\"\"\"\n    ...\n</code></pre>"},{"location":"typeclass/applicative/#funstruct.typeclasses.Applicative.__add__","title":"<code>__add__(other)</code>","text":"<p>Alias for ap.</p> Source code in <code>funstruct/typeclasses/_applicative.py</code> <pre><code>def __add__(self, other: Applicative[_B]) -&gt; Applicative[tuple[_A, _B]]:\n    \"\"\"Alias for ap.\"\"\"\n    return self.ap(other)\n</code></pre>"},{"location":"typeclass/monad/","title":"Monad","text":""},{"location":"typeclass/monad/#funstruct.typeclasses.Monad","title":"<code>funstruct.typeclasses.Monad</code>","text":"<p>               Bases: <code>Applicative[_A]</code></p> <p>Sequence computations that produce new contexts.</p> Type parameter <p>_A: The value type inside the monad.</p> Source code in <code>funstruct/typeclasses/_monad.py</code> <pre><code>class Monad(Applicative[_A]):\n    \"\"\"Sequence computations that produce new contexts.\n\n    Type parameter:\n        _A: The value type inside the monad.\n    \"\"\"\n\n    @abstractmethod\n    def bind(self, f: Callable[[_A], Monad[_B]]) -&gt; Monad[_B]: ...\n\n    @classmethod\n    @abstractmethod\n    def do(cls, gen_fn: Callable) -&gt; Monad[_A]:\n        \"\"\"Do-notation via generators. Flattens nested binds.\"\"\"\n        ...\n\n    def ap(self, other: Monad[_B]) -&gt; Monad[tuple[_A, _B]]:\n        \"\"\"Default ap derived from bind + map.\n\n        Every Monad is an Applicative, and ap can always be derived from\n        bind + map. This can't live on Applicative itself because Applicative\n        doesn't have bind \u2014 only Monad does. Standalone Applicatives\n        must implement ap directly.\n        \"\"\"\n        return self.bind(lambda a: other.map(lambda b: (a, b)))\n\n    def map2(self, other: Monad[_B], f: Callable) -&gt; Monad:\n        \"\"\"Combine two monadic values with a function.\n\n        map2(fa, fb, f) = fa.bind(a =&gt; fb.map(b =&gt; f(a, b)))\n\n        Like ap, but you choose the combiner instead of always tupling.\n        \"\"\"\n        return self.bind(lambda a: other.map(lambda b: f(a, b)))\n\n    def __rshift__(self, f: Callable[[_A], Monad[_B]]) -&gt; Monad[_B]:\n        \"\"\"Alias for bind.\"\"\"\n        return self.bind(f)\n</code></pre>"},{"location":"typeclass/monad/#funstruct.typeclasses.Monad.do","title":"<code>do(gen_fn)</code>  <code>abstractmethod</code> <code>classmethod</code>","text":"<p>Do-notation via generators. Flattens nested binds.</p> Source code in <code>funstruct/typeclasses/_monad.py</code> <pre><code>@classmethod\n@abstractmethod\ndef do(cls, gen_fn: Callable) -&gt; Monad[_A]:\n    \"\"\"Do-notation via generators. Flattens nested binds.\"\"\"\n    ...\n</code></pre>"},{"location":"typeclass/monad/#funstruct.typeclasses.Monad.ap","title":"<code>ap(other)</code>","text":"<p>Default ap derived from bind + map.</p> <p>Every Monad is an Applicative, and ap can always be derived from bind + map. This can't live on Applicative itself because Applicative doesn't have bind \u2014 only Monad does. Standalone Applicatives must implement ap directly.</p> Source code in <code>funstruct/typeclasses/_monad.py</code> <pre><code>def ap(self, other: Monad[_B]) -&gt; Monad[tuple[_A, _B]]:\n    \"\"\"Default ap derived from bind + map.\n\n    Every Monad is an Applicative, and ap can always be derived from\n    bind + map. This can't live on Applicative itself because Applicative\n    doesn't have bind \u2014 only Monad does. Standalone Applicatives\n    must implement ap directly.\n    \"\"\"\n    return self.bind(lambda a: other.map(lambda b: (a, b)))\n</code></pre>"},{"location":"typeclass/monad/#funstruct.typeclasses.Monad.map2","title":"<code>map2(other, f)</code>","text":"<p>Combine two monadic values with a function.</p> <p>map2(fa, fb, f) = fa.bind(a =&gt; fb.map(b =&gt; f(a, b)))</p> <p>Like ap, but you choose the combiner instead of always tupling.</p> Source code in <code>funstruct/typeclasses/_monad.py</code> <pre><code>def map2(self, other: Monad[_B], f: Callable) -&gt; Monad:\n    \"\"\"Combine two monadic values with a function.\n\n    map2(fa, fb, f) = fa.bind(a =&gt; fb.map(b =&gt; f(a, b)))\n\n    Like ap, but you choose the combiner instead of always tupling.\n    \"\"\"\n    return self.bind(lambda a: other.map(lambda b: f(a, b)))\n</code></pre>"},{"location":"typeclass/monad/#funstruct.typeclasses.Monad.__rshift__","title":"<code>__rshift__(f)</code>","text":"<p>Alias for bind.</p> Source code in <code>funstruct/typeclasses/_monad.py</code> <pre><code>def __rshift__(self, f: Callable[[_A], Monad[_B]]) -&gt; Monad[_B]:\n    \"\"\"Alias for bind.\"\"\"\n    return self.bind(f)\n</code></pre>"},{"location":"typeclass/monadtransformer/","title":"MonadTransformer","text":""},{"location":"typeclass/monadtransformer/#funstruct.typeclasses.MonadTransformer","title":"<code>funstruct.typeclasses.MonadTransformer</code>","text":"<p>               Bases: <code>Monad</code>, <code>Generic[_F, _A]</code></p> <p>Base for monad transformers.</p> <p>Mathematically, a monad transformer is a type constructor (Monad -&gt; Monad). Haskell equivalent: <code>class MonadTrans t where lift :: Monad m =&gt; m a -&gt; t m a</code>. In funstruct this is <code>lift_f</code> to distinguish from function-lifting combinators. Python lacks higher-kinded types, so this class is a pragmatic marker.</p> <p>          Class Type Parameters:        </p> Name Bound or Constraints Description Default <code>_F</code> <p>The inner monad type. Must support .bind(), .map(), .pure(). Duck-typed at runtime.</p> required <code>_A</code> <p>The value type produced by the transformer.</p> required Source code in <code>funstruct/typeclasses/_monad_transformer.py</code> <pre><code>class MonadTransformer(Monad, Generic[_F, _A]):\n    \"\"\"Base for monad transformers.\n\n    Mathematically, a monad transformer is a type constructor (Monad -&gt; Monad).\n    Haskell equivalent: `class MonadTrans t where lift :: Monad m =&gt; m a -&gt; t m a`.\n    In funstruct this is `lift_f` to distinguish from function-lifting combinators.\n    Python lacks higher-kinded types, so this class is a pragmatic marker.\n\n    Type parameters:\n        _F: The inner monad type. Must support .bind(), .map(),\n            .pure(). Duck-typed at runtime.\n        _A: The value type produced by the transformer.\n    \"\"\"\n\n    @classmethod\n    @abstractmethod\n    def lift_f(cls, inner: _F) -&gt; MonadTransformer[_F, _A]:\n        \"\"\"Lift an inner monad value into the transformer.\"\"\"\n        ...\n\n    @abstractmethod\n    def and_then(self, other) -&gt; MonadTransformer[_F, _A]:\n        \"\"\"Kleisli composition: output of self becomes input of other.\"\"\"\n        ...\n</code></pre>"},{"location":"typeclass/monadtransformer/#funstruct.typeclasses.MonadTransformer.lift_f","title":"<code>lift_f(inner)</code>  <code>abstractmethod</code> <code>classmethod</code>","text":"<p>Lift an inner monad value into the transformer.</p> Source code in <code>funstruct/typeclasses/_monad_transformer.py</code> <pre><code>@classmethod\n@abstractmethod\ndef lift_f(cls, inner: _F) -&gt; MonadTransformer[_F, _A]:\n    \"\"\"Lift an inner monad value into the transformer.\"\"\"\n    ...\n</code></pre>"},{"location":"typeclass/monadtransformer/#funstruct.typeclasses.MonadTransformer.and_then","title":"<code>and_then(other)</code>  <code>abstractmethod</code>","text":"<p>Kleisli composition: output of self becomes input of other.</p> Source code in <code>funstruct/typeclasses/_monad_transformer.py</code> <pre><code>@abstractmethod\ndef and_then(self, other) -&gt; MonadTransformer[_F, _A]:\n    \"\"\"Kleisli composition: output of self becomes input of other.\"\"\"\n    ...\n</code></pre>"},{"location":"typeclass/monoid/","title":"Monoid","text":""},{"location":"typeclass/monoid/#funstruct.typeclasses.Monoid","title":"<code>funstruct.typeclasses.Monoid</code>  <code>dataclass</code>","text":"<p>               Bases: <code>Semigroup</code></p> <p>A Semigroup with an identity element (empty).</p> Source code in <code>funstruct/typeclasses/_monoid.py</code> <pre><code>@final\n@dataclass(frozen=True)\nclass Monoid(Semigroup):\n    \"\"\"A Semigroup with an identity element (empty).\"\"\"\n\n    typ: type\n    combine: Callable\n    empty: object\n</code></pre>"},{"location":"typeclass/semigroup/","title":"Semigroup","text":""},{"location":"typeclass/semigroup/#funstruct.typeclasses.Semigroup","title":"<code>funstruct.typeclasses.Semigroup</code>  <code>dataclass</code>","text":"<p>An associative binary operation over a type.</p> Source code in <code>funstruct/typeclasses/_semigroup.py</code> <pre><code>@dataclass(frozen=True)\nclass Semigroup:\n    \"\"\"An associative binary operation over a type.\"\"\"\n\n    typ: type\n    combine: Callable\n</code></pre>"},{"location":"util/tailrec/","title":"tailrec","text":"<p>Trampoline-based tail recursion for Python. Eliminates stack overflow on deep recursion by converting tail-recursive functions into iterative loops.</p>"},{"location":"util/tailrec/#example","title":"Example","text":"<pre><code>from funstruct.util.tailrec import tco, tail_call\n\n\n@tco\ndef sum_up_to(n, acc=0):\n    if n == 0:\n        return acc\n    return tail_call(sum_up_to)(n - 1, acc + n)\n\n\nsum_up_to(10000)  # no stack overflow\n</code></pre>"},{"location":"util/tailrec/#api-reference","title":"API Reference","text":""},{"location":"util/tailrec/#funstruct.util.tailrec","title":"<code>funstruct.util.tailrec</code>","text":"<p>Trampoline-based tail recursion.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from funstruct.util.tailrec import tco, tail_call\n&gt;&gt;&gt; @tco\n... def sum_up_to(n, acc=0):\n...     if n == 0:\n...         return acc\n...     return tail_call(sum_up_to)(n - 1, acc + n)\n&gt;&gt;&gt; sum_up_to(100)\n5050\n</code></pre>"},{"location":"util/tailrec/#funstruct.util.tailrec.tco","title":"<code>tco</code>","text":"<p>Marks a function as tail-call optimized.</p> <p>Use with tail_call to avoid blowing the call stack on recursive functions.</p> Source code in <code>funstruct/util/tailrec.py</code> <pre><code>class tco:\n    \"\"\"Marks a function as tail-call optimized.\n\n    Use with tail_call to avoid blowing the call stack on recursive functions.\n    \"\"\"\n\n    def __init__(self, f):\n        self.f = f\n\n    def __call__(self, *args, **kwargs):\n        ret = self.f(*args, **kwargs)\n        while type(ret) is _tail_call:\n            ret = ret.handle()\n        return ret\n</code></pre>"},{"location":"util/tailrec/#funstruct.util.tailrec.tail_call","title":"<code>tail_call(f)</code>","text":"<p>Call a tail-recursive function.</p> <p>Use in conjunction with @tco.</p> <p>Example::</p> <pre><code>@tco\ndef sum_up_to(n, acc=0):\n    if n == 0:\n        return acc\n    return tail_call(sum_up_to)(n - 1, acc + n)\n\nsum_up_to(10000)  # no stack overflow\n</code></pre> Source code in <code>funstruct/util/tailrec.py</code> <pre><code>def tail_call(f):\n    \"\"\"Call a tail-recursive function.\n\n    Use in conjunction with @tco.\n\n    Example::\n\n        @tco\n        def sum_up_to(n, acc=0):\n            if n == 0:\n                return acc\n            return tail_call(sum_up_to)(n - 1, acc + n)\n\n        sum_up_to(10000)  # no stack overflow\n    \"\"\"\n\n    def _f(*args, **kwargs):\n        return _tail_call(f, *args, **kwargs)\n\n    return _f\n</code></pre>"}]}