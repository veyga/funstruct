# Semigroup

A type with an associative binary combine operation (`+`).

```
A ─┐
    ├──( + )──> A
A ─┘
```

**Law:** `(a + b) + c == a + (b + c)`

Built-in semigroups: `list`, `str`, `tuple`

## API Reference

::: funstruct.typeclass.semigroup.Semigroup
