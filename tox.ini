[tox]
env_list = py310, py311, py312, py313, py314
requires = tox-uv

[testenv]
extras = auth, logging, constants, functional
deps =
    pytest
    pytest-parametrization
    freezegun
commands = pytest tests/ {posargs}
